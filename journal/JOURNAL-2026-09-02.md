# JOURNAL-2026-09-02

- E88 #23685 merge (4x packed Q8_1 MMVQ for Q4_K/Q5_K/Q6_K), 2026-09-02:
  branch perf-test-mmvq-23685 off dflash2 (f22c06c28),
  commit 7dc25a9f5, 5 files +937/-156.
  PR head c54d684d7 (8 commits), merge-base 039e20a2d.
  Method: git merge-file 3-way per file (merge 2026-09-01, 22:53):
  9 conflicts in mmvq.cu only, resolved by content-asserting resolver
  (merge-23685 in /tmp); other 4 files (common.cuh, quantize.cu,
  quantize.cuh, vecdotq.cuh) merged clean, PR symbols verified in all 5.
  Resolution design:
  - PR architecture kept: mmvq_layout_policy (standard vs packed q8_1,
    block sizes 32/64/128/256), packed kernel body + packed vecdot
    (vecdotq.cuh +403), host layout select + quantize_row_q8_1_layout_
    cuda switch (quantize.cu +112, block_q8_1_layout struct,
    extern templates 64/128/256).
  - Fork threading restored: small_k + halve_iters through policy
    nwarps() and the 6-param kernel template; GB10/DGX-Spark table id +
    nwarps doubling; MOE-fusion has_fusion incl. NVFP4 x_scale/gate_
    scale + glu_limit; fork kernel-launch wrapper kept over PR raw
    launches.
  - case 1 (ncols_dst==1): if constexpr packed branch with early
    return (packed owns the ncols==1 geometry), else fork tag-dispatch
    (small_k/halve_iters) with q8_1 threaded.
  - fork hand-written type switch replaced by PR macro dispatch
    (MMVQ_SWITCH_TYPE); Q2_0 added to the list (precedes PR base,
    absent there). All 23 fork types present, audited.
  Audit: diff-vs-pr all fork-side lines; diff-vs-ours "absent" lines
    = superseded dispatch/signature regions only, no fork logic drops
    (counts: GB10 x8, DGX_SPARK x3, small_k x25, halve_iters x19,
    crossover x25, x_scale x22, glu_limit x9, Q2_0 x3; PR side
    q8_1_layout x98, policy x16, macro x25).
  Build: build-mmvq23685 (FRESH, D11 recipe via build-dflash-novega.sh,
    TARGETS llama-bench then llama-server, VEGA_TUNE all ON per cache).
    First pass 11 errors, single root cause: resolver left
    "static constexpr int c_ncols_dst = 1;" declared after the PR's
    inserted if-constexpr that uses it in case 1 - moved the decl to
    the top of the case block (matches cases 2-4), amended commit.
    Final build EXIT clean, 0 errors; llama-server + libggml-hip.so
    produced. Version smoke: 0.3.0-dev, commit 7dc25a9f5.
  Packed-path verification (A/B will exercise it):
  - code: mmvq_q8_1_packed_k_layout_policy sets uses_layout=true,
    supported=true only for Q4_K/Q5_K/Q6_K at 64/128/256 (32 static-
    assert-excluded); per-type selector returns 128 for Q6_K ncols==1.
  - object: mmvq.cu.o fatbin contains mul_mat_vec_q<ggml_type 14=Q6_K,
    1, false, false, false, 128> (+ 64/256 variants, 32 standard-only
    for types 12/13/14); every other type stays standard layout.
    Enum confirmed Q4_K=12, Q5_K=13, Q6_K=14 (ggml.h:402-404).
  A/B plan (user-run, agent must not touch servers):
  llama-bench infeasible on this model (FINDINGS.md:138-141: whole
    model to device 0 despite -ts/-sm, single-buffer OOM; 27B i1-Q6_K
    does not fit one 16 GiB VII). ab-bench.sh (server path) is the
    reliable tool. Two trials, only BIN differs:
      cd bench
      MODEL=/home/srcds/ai/ai/Qwen3.8-27B.i1-Q6_K.gguf \
        CTX=250000 TS=35,20,45 SPEC_TYPE=draft-dflash \
        BIN=$PWD/../build-dflash-novega/bin/llama-server \
        ./ab-bench.sh mmvq23685-A
      (same with BIN=$PWD/../build-mmvq23685/bin/llama-server,
       trial name mmvq23685-B)
  Reading: tg_tps delta = packed Q6_K mat-vec effect (decode only);
    pp_tps should be flat (prefill GEMM untouched); check sha/ok for
    numerics drift. Both builds share the same D11/VEGA_TUNE config;
    trees differ only by the 5-file merge.
  Push + trial runs: user. Results appended here.
  First run failed both trials (raw harness env, no extras): harness
    defaults (f16 KV, -sm cost, -ub 448, no drafter) do not fit 250k -
    ROCm1 cudaMalloc OOM at compute-buffer reserve, 320 MiB short.
    Fix: bench/ab-mmvq23685.sh passes the production-fit set via the
    extra-args slot (Q4_K_M drafter + depth 4 + ROCm0 pin + -ngld 99,
    -ctk f16 -ctv q8_0, -sm layer, -ub 384, -cram 28000, PP=off,
    HIP_GRAPH/P2P/force_convert env) - same fit set as repro-crash.sh
    (proven 250k load) minus mmproj. Q8_0 drafter avoided on purpose
    (E80 crash window). Rerun pending.
  A/B result (2026-09-02, user-run via bench/ab-mmvq23685.sh, both
  trials n_ctx 250112, Q4_K_M drafter depth 4, acc 0.652, load 20 s):
    A control (build-dflash-novega): pp1 374.0  tg 16.6  sha e54019ff6b42
    B merge   (build-mmvq23685):     pp1 375.2  tg 16.5  sha e54019ff6b42
  Verdict: numerically identical (same sha; same draft acceptance
  0.47989 = 167/348 in both logs); perf delta within noise (tg -0.6%,
  pp1 +0.3%). No end-to-end gain on the production config.
  Interpretation: the packed layout is ncols_dst==1 only. With spec
  decoding always on, the main model verifies draft chains in batches
  (ncols>1 -> standard kernels), so the packed path mainly serves the
  small Q4_K_M drafter's single-token steps - too small a slice of the
  round to move tg. A SPEC_TYPE=none pair would isolate it on the main
  model if ever needed. Decision: keep perf-test-mmvq-23685 for
  reference, do not promote to dflash2; revisit when #23685 leaves
  draft upstream and arrives via the regular sync.

- E89 2UD launcher rebalance: mmproj->CPU, ts 40,20,40, c 256000
  (2026-09-02, dual-gpu-context-balancing skill, all lanes
  build-dflash-novega, lane-dflash.sh PROBE + full protocol):
  - Starting point: user's 2UD script (UD-Q6_K_L, ts 36,18,46, c 250000,
    mmproj moved to CPU earlier in the session) FAILED to boot on the
    release build: ROCm1 compute-reserve OOM (needs 755 MiB, 210 free).
    Root causes stacked: (1) all my first-round probes (A0/P1-P4) were
    INVALID - lane-dflash.sh defaults MODEL=i1-Q6_K, not the 2UD's
    UD-Q6_K_L (+2.6 GiB loaded); caught via log diff (model path line).
    (2) old UD production (35,20,45 @250k) fit only on the stale
    pre-selector build-dflash (E70 shadowing) with smaller compute
    buffers; on the release build even 35,20,45 is ~-700 MiB on ROCm1.
    (3) explicit --pipeline-parallel off matters for honest fit tables:
    fit-pass compute estimates are ~1.8x bigger in pipeline-auto mode
    (1302 vs 722 MiB) even though the gate always rejects at this ctx.
  - Search (UD model, PROBE runs, -lv 4): R0 36,18,46@250k FAIL (ROCm1
    fit-free -1008). GDN chain ceiling: b0<=27, b1<=40 (contiguity, 3
    GDN movable per boundary before attn layers). R2 ts 40,20,40
    (b0=27,b1=40, attn 6/4/6) @250112 LISTENS: frees +2047/+156/+1060.
    R4 @262144 (= qwen35.context_length metadata cap, confirmed in GGUF)
    LISTENS: +1799/+3/+812 - but see cliff below. R3 40,21,39@270k FAIL
    (VK1 compute 311 MiB OOM; direction error: raising ts1 GIVES VK1 a
    layer). 7/3/6-family dead: ROCm0 drafter floor (fit-free >= ~1100
    real 2100) blocks attn onto ROCm0.
  - PP-vs-ctx curve (server print, first 16384-batch): 195072: 376.4;
    230144: 375.4; 250112: 375.1; 256000: 375.1; 262144: 334.9.
    Flat plateau to 256k, then an -11% CLIFF exactly where VK1 fit-free
    hits +3 MiB (143 real pre-drafter). No spill logged (Vulkan defers
    binding silently) but the mechanism is VK1 memory-pressure degrade.
    40,19,41 (6/3/7, attn off the 3080) @240k: 379.5 - fastest shape but
    ROCm1 margin +70 = below runtime-safety class; rejected.
  - UD model composition measured (gguf-py): Q4_K 12,709 MiB / Q8_0
    8,766 (incl. output 1288 + full-Q8_0 FFN layers 90.3x3) / Q3_K 1,533
    / Q2_K 45. "Q6_K_L" contains ZERO Q6_K. Loaded 22.7 vs i1 20.1 GiB:
    PP is bandwidth-bound -> ~13% slower than i1 at equal ctx. The
    393-408 client PP anchor (2llama-start-iq6v-dflash2.sh) is the i1
    model; UD cannot reach 360 client at ANY ctx (ceiling ~358 at the
    plateau). Per user rule (ctx bends for speed below 360) the plateau
    edge is the optimum: speed is identical from 195k to 256k, so take
    max ctx = 256000. Route to 360+: port upstream #21698 (gfx906 q8_0
    load_tiles two-phase; +25-36% on Q8_0-weight models; UD is 38%
    Q8_0-mass) - kernel work, not config.
  - Final validation lane (udb-win, 40,20,40 @256000, full protocol):
    client pp16384 350.7 (server 375.1), fill120k 236.8, TG1024@120k
    12.8, acc .676, repro True, sha 6b38a21df2bf (same greedy stream as
    the 262k lane - placement is numerics-preserving). vs the 262k lane:
    +5.4 client pp, +2.6 fill, same TG.
  - Vision (bench/vision-bench.py, 1920x1080 png, 2086 image tokens):
    CPU mmproj (winner cfg): ~40.3-44.6 s prompt-eval per image
    (~52 tok/s steady, 9 threads). GPU mmproj control at 230k: model
    loads + listens, then clip_encode CRASHES on first image
    ("ROCm error: out of memory", clip ctx ~1.16 GiB on ROCm0) -
    GPU vision is impossible at this ctx; CPU offload is required, and
    the crash explains any past image-request instability at 250k.
  - Launcher updated: 2UDllama-start-iq6v-dflash2.sh = ts 40,20,40
    -sm layer -c 256000 --no-mmproj-offload --pipeline-parallel off,
    everything else untouched (KV quants, drafter, ub/b, threads).
    Header rewritten (i1-era PP claims replaced with measured UD class;
    W6 -ot opt-in marked obsolete; sm-cost/pipeline verdicts cited).
   - Reusable artifacts: bench/vision-bench.py (+ vision-test-1080p.png),
     lane logs bench/logs/lane-udb-*.log, this E-entry = the skill's
     result record (date/build/model/GPUs/splits/overrides/frees/ctx/
     throughput/vision/risks all above).

- E90 store.piffa.net/lm/bug review (Eaman patch store, refreshed
  2026-08-31, base 662a0b01 = our dflash2 merge point; HEAD is 102
  commits ahead), 2026-09-02:
  - Already covered locally: per-device fixed-layout fit + --pipeline-
    parallel (commit 165fc79cb), fitted-MTP ctx + server refinement,
    HIP quantized-KV VEC dispatch (our fattn.cu evolved further:
    GGML_CUDA_VEGA_TUNE_FATTN prefers GCN native q8_0 tile kernels,
    VEC only fallback), adaptive drafting (our draft-mtp-adaptive spec
    type ~= their --spec-draft-adaptive). Upstream 563-commit sync also
    absorbed their MTP-fitting work (their README admits this).
  - Genuinely NEW on store: MTP Compact Rollback (mtp_compact_rollback_
    662a0b01.patch): --spec-mtp-cr-depth decouples rollback-state depth
    from --spec-draft-n-max for recurrent models (1 live + D snapshots
    + 1 on-device replay checkpoint; dense Q6 saves ~449 MiB at n-max 5;
    their MoE matched test 233k -> 262k ctx). NOT applicable to our
    production: DFlash drafter explicitly excluded by the patch.
  - --hip-fa-force-vec on|off (default off): Eaman found always-VEC
    costs PP on RDNA2. Our dispatch is already shape/GCN-aware, so this
    is an RDNA2 answer to a problem we tuned away differently. No action.
  - Config finding worth remembering if drafter KV is ever re-tuned on
    HIP: their Q4_0 draft-KV totalled MORE VRAM than F16 draft-KV
    (F16 conversion workspace, -92 MiB). Finding is gfx1030-specific;
    our GCN native-tile path changes the tradeoff.
  - Verdict: nothing drop-in for the current dflash@256k lane. The only
    future-relevant item is MTP CR - port via summary_eaman.md (written
    as LLM-ingest reapply recipe) if we ever A/B draft-mtp again; raw
    patch will need 3-way (102 commits of drift, fattn.cu diverged).
    PP route to 360+ stays upstream #21698 kernel work, per E89.

- E91 UD per-type MMQ attribution (user-run rocprof pmc-a pass, out-ud-a,
  2026-09-02): 200,684 dispatches, 290.8 G busy cycles (both VIIs; window =
  load + PP16384 + partial TG). Server crashed ~2 min in - GPU0 dispatch
  stream went silent 1098 ms before GPU1's last kernel (asymmetric GPU0
  death, stderr not captured; suspect interposer/E80-class, see below).
  Shares of GPU busy cycles:
    mmq_Q6_K   40.5%   (per-byte cost 0.74 rel.)
    mmq_Q8_0   23.5% + 2.0% fallback tiles (act/GUI 2.3 = poor edge tiles)
    flash_attn_tile 14.3%  (native tile FA - #2 kernel class)
    mmq_Q5_K    8.6%   (per-byte 1.30 = ~1.8x Q6_K - worst per byte)
    gdn/recurrent 4.9%, quantize_q8_1 1.0% (41k dispatches), rest ~5%
  Placement: Q5_K all on GPU0 (16% of GPU0 busy), Q8_0 32%/16% GPU1/GPU0.
  Implications:
  - Biggest absolute lever = Q6_K inner loop (55% bytes, 40% cycles);
    chain-shortening (dual accumulators) targets it first.
  - #21698 (q8_0 loader) ceiling: Q8_0 = 25.5% of cycles -> even +30%
    on it = ~7-8% total PP. Pro-rate the "+25-36% on Q8_0-weight models"
    claim accordingly for UD (38% mass, not 100%).
  - Q5_K ffn_gate (21 layers): 1.8x per-byte cost - candidate for either
    vec-dot bit-op cleanup or requant to Q6_K in a future UD rev.
  - flash_attn_tile 14.3% = second optimization target class.
  rocprof-server.sh rewritten to current UD production twin (old 155k-era
  paths were stale); ab-bench.sh BIN default still build-vega20 (noted).
  Crash followup: need server stderr from user; PP-only protocol (kill
  after first 16k batch) avoids the TG-phase crash zone - PP attribution
  is unaffected, data was complete through PP.

- E92 MMQ dual-accumulator chains (commit 2ecfe9852), 2026-09-02:
  Implementation of the E91/PMU lever: mul_mat_q_process_tile (mmq.cuh)
  feeds the two per-kb0 vec_dot calls into independent accumulators
  (sum/sum_b, merged before write_back), halving the serial
  dot4->I2F->FMUL->FADD chain per element. Gated by new CMake option
  GGML_CUDA_VEGA_TUNE_MMQ_DUALACC, default OFF; foreach wiring exists in
  BOTH ggml-cuda and ggml-hip CMakeLists (first build silently missed the
  define - HIP target has its own copy of the vega block; caught by
  checking flags.make, not just CMakeCache).
  Build: build-dualacc (fresh dir, D11 recipe + DUALACC=ON, llama-server
  + llama-perplexity). Existing build dirs untouched.
  CPU-side verification (ISA, --cuda-device-only -S on template
  instances):
  - hot configs gain exactly +n_sum VGPR: Q6_K J64 152->184, Q8_0 J64
    119->151; Q6_K J64 FB 168->199. All instances <= 249/256, zero new
    scratch/spill refs (outliers Q8_0 J48 126->249, Q6_K J24 136->225 =
    scheduler opportunism, watch in A/B).
  - vec_dot functions untouched; only the destination pointer alternates
    (phase-split lesson: operand load grouping preserved).
  Numerics: reassociation only - temp-0 sha WILL move; gates are
  perplexity delta (bench/perplexity-dualacc.sh, f16 KV to isolate the
  weight matmul, log.txt as fixed text) + acc-rate within noise.
  User-run handoff (server must be down):
    bench/ab-dualacc.sh                 # A/B lane, UD @256k, ~30-40 min/trial
    bench/perplexity-dualacc.sh         # numerics gate, ~10 min/side
  Expected: pp up (projection 55-65% issue density -> ~370-410 pp1 class
  on i1 mix; UD pro-rata similar), tg/acc within noise. If J48/J24-shaped
  GEMMs regress, gate DUALACC to J==64 in a follow-up.

- E93 dual-accumulator A/B result: REJECTED, 2026-09-02 (user-run
  bench/ab-dualacc.sh + bench/perplexity-dualacc.sh):
    A (build-dflash-novega)      pp1 350.3  tg 16.4  acc .664  sha 6b38a21df2bf
    B (build-dualacc, DUALACC=1) pp1 304.3  tg 16.6  acc .664  sha 6b38a21df2bf
  -13.1% pp1. #23685 confound excluded (same-day rows 374.0/375.2 flat).
  Identical sha across A/B: reassociation never flipped a greedy argmax
  in 1024 tokens; numerics moot, perf verdict stands.
  E91 CSV re-check: 96.9% of mul_mat_q cycles are J=64 fb=0 (the clean
  +32-VGPR shapes) - gating DUALACC to J==64 would not save it; closed.
  MMQ C++-level restructure route now closed after 5 rejections (see
  FINDINGS experiment 4 entry). Updated PP lever ranking: (1) #21698
  q8_0 loader port, cap +7-8% on UD -> ~377 client, crosses the 360
  rule; (2) UD v2 Q5_K->Q6_K requant (~+2-3%, +300 MiB); (3) fattn_tile
  14.3% class. build-dualacc kept; option stays default-OFF.

- E94 #21698 q8_0 loader port (GGML_CUDA_VEGA_TUNE_MMQ_Q8LDR, default
  OFF), 2026-09-02: upstream PR #21698 (iacopPBK, OPEN, unmerged) -
  "better vram to lds loading pipeline in load_tiles_q8_0". MI50
  (= gfx906) benches: +28.6-36.2% pp on pure-Q8_0 models, tg neutral,
  RDNA2 flat. Mechanism: threads_per_row 32 -> 16 (GCN5) + unrolled
  k loop covering all 2*MMQ_TILE_NE_K ints/row = 4 independent loads
  per thread per row-iteration (vram -> regs -> lds pipeline).
  Port: mmq-load-tiles.cuh q8_0 loader only, non-MMA branch. Value
  mapping verified by hand: dest j <- block j/8 int j%8 identical to
  the 32-thread form (kbx folded into bxi) -> bit-identical writes,
  sha-stable, no perplexity gate needed.
  CMake: option + foreach in BOTH ggml-cuda and ggml-hip (E92 lesson).
  Build: build-q8ldr (fresh, Q8LDR=ON, DUALACC=OFF). flags.make
  verified carry the define. ISA check:
  - q6_k instance BYTE-IDENTICAL to baseline (isolation proven)
  - q8_0 J64 fb0 VGPR 119 -> 128 (+9); static load/ds_write counts
    equal (same 32 load sites, regrouped 4-per-iteration - the win is
    dynamic memory-level parallelism, static analysis cannot show it)
  User-run: bench/ab-q8ldr.sh (UD @256k lane). Expect ~+7-8% pp
  (350.7 -> ~377 client), sha 6b38a21df2bf unchanged, tg/acc noise.
  If sha moves, the port has a bug - halt and report.
