# JOURNAL-2026-09-02

- E88 #23685 merge (4x packed Q8_1 MMVQ for Q4_K/Q5_K/Q6_K), 2026-09-02:
  branch perf-test-mmvq-23685 off dflash2 (f22c06c28),
  commit 7dc25a9f5, 5 files +937/-156.
  PR head c54d684d7 (8 commits), merge-base 039e20a2d.
  Method: git merge-file 3-way per file (merge 2026-09-01, 22:53):
  9 conflicts in mmvq.cu only, resolved by content-asserting resolver
  (merge-23685 in /tmp); other 4 files (common.cuh, quantize.cu,
  quantize.cuh, vecdotq.cuh) merged clean, PR symbols verified in all 5.
  Resolution design:
  - PR architecture kept: mmvq_layout_policy (standard vs packed q8_1,
    block sizes 32/64/128/256), packed kernel body + packed vecdot
    (vecdotq.cuh +403), host layout select + quantize_row_q8_1_layout_
    cuda switch (quantize.cu +112, block_q8_1_layout struct,
    extern templates 64/128/256).
  - Fork threading restored: small_k + halve_iters through policy
    nwarps() and the 6-param kernel template; GB10/DGX-Spark table id +
    nwarps doubling; MOE-fusion has_fusion incl. NVFP4 x_scale/gate_
    scale + glu_limit; fork kernel-launch wrapper kept over PR raw
    launches.
  - case 1 (ncols_dst==1): if constexpr packed branch with early
    return (packed owns the ncols==1 geometry), else fork tag-dispatch
    (small_k/halve_iters) with q8_1 threaded.
  - fork hand-written type switch replaced by PR macro dispatch
    (MMVQ_SWITCH_TYPE); Q2_0 added to the list (precedes PR base,
    absent there). All 23 fork types present, audited.
  Audit: diff-vs-pr all fork-side lines; diff-vs-ours "absent" lines
    = superseded dispatch/signature regions only, no fork logic drops
    (counts: GB10 x8, DGX_SPARK x3, small_k x25, halve_iters x19,
    crossover x25, x_scale x22, glu_limit x9, Q2_0 x3; PR side
    q8_1_layout x98, policy x16, macro x25).
  Build: build-mmvq23685 (FRESH, D11 recipe via build-dflash-novega.sh,
    TARGETS llama-bench then llama-server, VEGA_TUNE all ON per cache).
    First pass 11 errors, single root cause: resolver left
    "static constexpr int c_ncols_dst = 1;" declared after the PR's
    inserted if-constexpr that uses it in case 1 - moved the decl to
    the top of the case block (matches cases 2-4), amended commit.
    Final build EXIT clean, 0 errors; llama-server + libggml-hip.so
    produced. Version smoke: 0.3.0-dev, commit 7dc25a9f5.
  Packed-path verification (A/B will exercise it):
  - code: mmvq_q8_1_packed_k_layout_policy sets uses_layout=true,
    supported=true only for Q4_K/Q5_K/Q6_K at 64/128/256 (32 static-
    assert-excluded); per-type selector returns 128 for Q6_K ncols==1.
  - object: mmvq.cu.o fatbin contains mul_mat_vec_q<ggml_type 14=Q6_K,
    1, false, false, false, 128> (+ 64/256 variants, 32 standard-only
    for types 12/13/14); every other type stays standard layout.
    Enum confirmed Q4_K=12, Q5_K=13, Q6_K=14 (ggml.h:402-404).
  A/B plan (user-run, agent must not touch servers):
  llama-bench infeasible on this model (FINDINGS.md:138-141: whole
    model to device 0 despite -ts/-sm, single-buffer OOM; 27B i1-Q6_K
    does not fit one 16 GiB VII). ab-bench.sh (server path) is the
    reliable tool. Two trials, only BIN differs:
      cd bench
      MODEL=/home/srcds/ai/ai/Qwen3.8-27B.i1-Q6_K.gguf \
        CTX=250000 TS=35,20,45 SPEC_TYPE=draft-dflash \
        BIN=$PWD/../build-dflash-novega/bin/llama-server \
        ./ab-bench.sh mmvq23685-A
      (same with BIN=$PWD/../build-mmvq23685/bin/llama-server,
       trial name mmvq23685-B)
  Reading: tg_tps delta = packed Q6_K mat-vec effect (decode only);
    pp_tps should be flat (prefill GEMM untouched); check sha/ok for
    numerics drift. Both builds share the same D11/VEGA_TUNE config;
    trees differ only by the 5-file merge.
  Push + trial runs: user. Results appended here.
  First run failed both trials (raw harness env, no extras): harness
    defaults (f16 KV, -sm cost, -ub 448, no drafter) do not fit 250k -
    ROCm1 cudaMalloc OOM at compute-buffer reserve, 320 MiB short.
    Fix: bench/ab-mmvq23685.sh passes the production-fit set via the
    extra-args slot (Q4_K_M drafter + depth 4 + ROCm0 pin + -ngld 99,
    -ctk f16 -ctv q8_0, -sm layer, -ub 384, -cram 28000, PP=off,
    HIP_GRAPH/P2P/force_convert env) - same fit set as repro-crash.sh
    (proven 250k load) minus mmproj. Q8_0 drafter avoided on purpose
    (E80 crash window). Rerun pending.
  A/B result (2026-09-02, user-run via bench/ab-mmvq23685.sh, both
  trials n_ctx 250112, Q4_K_M drafter depth 4, acc 0.652, load 20 s):
    A control (build-dflash-novega): pp1 374.0  tg 16.6  sha e54019ff6b42
    B merge   (build-mmvq23685):     pp1 375.2  tg 16.5  sha e54019ff6b42
  Verdict: numerically identical (same sha; same draft acceptance
  0.47989 = 167/348 in both logs); perf delta within noise (tg -0.6%,
  pp1 +0.3%). No end-to-end gain on the production config.
  Interpretation: the packed layout is ncols_dst==1 only. With spec
  decoding always on, the main model verifies draft chains in batches
  (ncols>1 -> standard kernels), so the packed path mainly serves the
  small Q4_K_M drafter's single-token steps - too small a slice of the
  round to move tg. A SPEC_TYPE=none pair would isolate it on the main
  model if ever needed. Decision: keep perf-test-mmvq-23685 for
  reference, do not promote to dflash2; revisit when #23685 leaves
  draft upstream and arrives via the regular sync.
