# Session journal - 2026-08-26 (session 2: overnight-follow-up, PR reading, tuning campaign)

Living document. Every discovery, decision and knowledge item from this session,
in order. Companion docs: PR27210-ADAPTIVE-MTP-2026-08-25.md (yesterday's
integration + PP-tax investigation), RESTORE-POINTS-2026-08-25.md, bench/FINDINGS.md.

## Session constraints (user-set, binding for all work)

- C1 Testing guideline, every config lane: PP 16384 first batch, fill to 120k,
  fill to 180k on the SAME server, then TG 1024. Plus one temp-0
  reproducibility check before/after.
- C2 temp 1.0 is the correct production sampling temp (0.6 in legacy
  clean-tree scripts is outdated). Bench TG lanes at 1.0; temp 0 only for the
  repro gate.
- C3 No context-size fallback. -c 210000 stays.
- C4 No subagent delegation this session (user: agent infra runs on a server
  that is down). Everything runs in the main shell.
- C5 Journal mandate: record every discovery/decision/knowledge item here.
- C6 uf2 and uf3 trees are 1:1 copies made today; uf3 is the designated
  working tree. Production server was launched from uf2 scripts overnight.
- C7 No clock wedge currently (user-confirmed). GPUs idle at check.

## Discoveries

- D1 CORRECTION of my earlier claim: the overnight binary DID contain the
  t_min revert. uf3 reflog + mtimes: delta-net-base.cpp edited 00:59:16,
  .o compiled 00:59:18, binary linked 00:59:56, revert committed 01:30:36
  (commit 0991f4ea2). Sequence was edit -> rebuild -> bench (dn-full.log:
  PP 326.4 / TG 19.3) -> docs -> commit. uf2 is a copy of this state.
  My "stale binary" hypothesis from the first turn of this session was wrong.
- D2 User-observed overnight: q6v.sh PP faster than before (consistent with
  326 vs the 297-311 in old script headers: revert + upstream sync).
  TG improvement imperceptible. q6v-mix.sh initially OOMed on start, later
  ran fine and "way faster" at same ctx (user report, unquantified - lane
  pending). OOM likely transient (residual VRAM from prior kill).
- D3 Why overnight TG gain was invisible (4 stacked reasons):
  a) deep-context TG gain is small by our own measurement (+9% @120k fill
     vs +89% fresh, temp 0);
  b) temp 1.0 sampling lowers draft acceptance vs the temp-0 benchmarks;
  c) workload lanes: adaptive wins are code (+5.6%) and recall (+63%),
     NOT reasoning (-4.6%) / prose (-5.9%) per marcusds's same-model run
     (Qwen3.8-27B, RTX 5090, PR 27210 thread);
  d) we dropped ngram-mod based on a fresh-text bench; PR-thread data shows
     adaptive+ngram-mod = 321 t/s recall vs 147 adaptive-only.
- D4 CONFIG BUG FOUND: --cache-reuse 256 in ALL our launchers is a no-op.
  tools/server/server-context.cpp:1129 zeroes n_cache_reuse whenever mmproj
  is loaded ("cache_reuse is not supported by multimodal"). Every launcher
  (q6v, q6v-mix, triage-s25, run-qwen38-160k-fast) loads mmproj-F16.
  Implication: "bigger cache-reuse" gains nothing today; the knob is dead
  while vision is enabled. Enabling it requires dropping --mmproj (or an
  upstream change). Verify by grepping any server startup log for the
  warning (todo in next lane run).
- D5 Checkpoint mechanics (code read, server-context.cpp):
  - --ctx-checkpoints (default 32, we pass 30) + --checkpoint-min-step
    (default 8192): rollback points per slot, PARTIAL state only
    (LLAMA_STATE_SEQ_FLAGS_PARTIAL_ONLY, tgt+dft+spec state).
  - With min-step 8192, a 210k ctx produces ~25 checkpoints max, so our cap
    of 30 is never the limiter: RAISING n_ctx_checkpoints alone changes
    nothing. The real density knob is checkpoint-min-step.
  - Separate machinery: per-verify-round spec_ckpt (line ~2937) updates the
    partial state stash whenever drafts run deeper than n_rs_seq - with
    adaptive n10 this fires on deep drafts, cost scales with depth.
- D6 PR 27210 thread (API read, restored access): author declared PR final
  08-26 ("I don't intend to work on this PR further"), still awaiting
  maintainer review. Key data points:
  - marcusds same-model matrix (see D3c).
  - am17an: recurrent-state slots cost real VRAM per n_max (Qwen3.5+ ~250MB
    each; ours is 1 GDN nextn layer, smaller but nonzero).
  - author found a SECOND latent GDN bug (full MTP-buffer scan on short
    drafts, ~2.6% tax at high n_max) - that fix IS in our tree (08-23 tip),
    distinct from the t_min hunk we reverted.
  - author: no scenario where --spec-draft-p-min helps adaptive; high p-min
    trashes prose 20-30%.
  - author on GGML_CUDA_DISABLE_GRAPHS=1: ~zero difference on AMD/ROCm
    (his measurement), graphs are not the AMD bottleneck.
  - bucketed-adaptive-mtp experimental branch linked (martindevans
    discussion) - see D8.
- D7 PR 27173 (chained drafting) verdict: REJECT for our topology.
  - Gains are launch/sync-overhead removal; they pay where drafting is
    CPU/launch-bound. Author: 2x5090 -sm tensor +12.8% mixed (code_echo
    +28%, prose only +1.9%). pich: 1x Blackwell +21%.
  - Against us: sswtodo single-GPU 7900 XTX Vulkan neutral-to-negative
    (64.5 -> 63.1); Flo5k5 2x V100 multi-GPU thinking-heavy: 3.4x REGRESSION
    (50 -> 14.7 t/s, drafts nearly doubled) - profile matches our nightly
    workload (multi-GPU, reasoning-heavy, deep ctx).
  - PR stalled: am17an "create 1 PR for each change". Flo5k5's minimal
    21-line variant (delta-net snapshot preservation + speculative.cpp
    carryover reset on non-extending prompts) fixes #27296 stream-cancel /
    tool-call corruption, BUT remeh reports prompt poisoning persists with
    only that variant. Keep as reference cure if we ever see #27296
    symptoms; would need careful merge with our t_min revert (same function).
- D8 bucketed-adaptive-mtp branch (stew675, experimental):
  - Base: upstream master + dc6f2aaf76d7 (adaptive MTP, standalone rebased)
    + 1207c1c248af (bucket carry + ngram feed gate). Tip 1207c1c248af,
    2026-08-24.
  - Controller (common/speculative-adaptive.h, 109 lines): single bucket B;
    update adds (n_accepted - depth), full-accept flips to max(1,
    n_accepted-1); climb budget flat 20 (costs ceil(20/max(1,depth-1)) full
    accepts: 20 at d1-2, 10 at d3, 5 at d5, 3 at d8, 2 at d11); drop
    pressure max(20, 4*depth) (deep depths fall in ~4 total misses);
    surplus/deficit CARRIES across depth changes; no truncated-draft special
    case; ngram cross-credit gated to n_accepted >= n_cur.
  - vs our shipped hysteresis (climb-counter + cost table with hardened
    3->4 barrier of 10, drop budget max(depth*5,20), reset on change):
    bucketed is simpler, wastes less signal on depth changes, climbs faster
    at depth, falls faster from depth, credits ngram strong runs.
  - dc6f2aaf76d7 CONTAINS the t_min hunk in delta-net-base.cpp (same 6+1
    lines we reverted). Port MUST exclude it.
  - Port surface: speculative-adaptive.h (new, pure), call-site wiring +
    ngram gate in speculative.cpp (~9 lines diff vs our integrated version
    which has more fixes), arg/common/server-context plumbing we already
    have from 27210. Small, surgical.
- D9 Harness inventory for tonight: bench/triage-s25.sh = protocol launcher
  (SPEC_ARGS/PORT/TRIAL env, sync25 binary, Q6 Qwen3.8, mmproj, temp 0.6
  server-side default - lanes override per-request). bench/bench-client.py =
  PP16384-first-batch + greedy TG256 client (needs extension for the
  120k/180k/TG1024/temp-1.0 protocol). Old skill-dir run.sh/capture.sh and
  most t-*.sh are Q3.6-era legacy - do not use.

## Decisions

- DEC1 Adopt overnight state as production baseline: uf3 master f8ca44a11,
  build-sync25 binary (adaptive n10 floor3 + t_min revert + author's
  buffer-scan fix). L0 lane re-measures it under the full protocol at
  temp 1.0 for tonight's reference numbers.
- DEC2 Cache-reuse question answered by D4: knob is dead with mmproj. No
  lanes for it. If replay-prompt re-processing cost ever matters, the
  options are dropping vision or upstream work. Journal and move on.
- DEC3 Checkpoint lanes: test --ctx-checkpoints 0 (pure cost check) and a
  dense variant (--ctx-checkpoints 64 --checkpoint-min-step 2048) to find
  whether density costs PP/TG. Expectation: near-zero effect in single-fill
  bench; real effect only in multi-turn rollback scenarios. Honest negative
  results are acceptable.
- DEC4 Port bucketed controller behind the SAME spec-type name is NOT ok
  for A/B; port plan: add controller as common/speculative-adaptive.h +
  wiring, selectable via env LLAMA_SPEC_BUCKETED=1 (default 0 = shipped
  hysteresis) so one binary runs both lanes. Exclude the t_min hunk.
- DEC5 ngram-mod re-add lane (adaptive + ngram-mod) - the only
  evidence-backed TG-at-depth lever that costs nothing (D3d, D6).
- DEC6 27173 rejected (D7). Not porting. Reference kept.
- DEC7 Reproducibility gate interpretation: in-lane temp-0 256-token sha
  before/after (sanity: coherent, deterministic within same server state);
  true cross-restart determinism = L0 run twice (L0a/L0b) and compare shas
  on identical fresh-server prompts.

## Knowledge items (durable)

- K1 With mmproj loaded, server silently disables: ctx_shift AND cache_reuse
  (server-context.cpp ~1124-1132). Check startup warnings before tuning
  either knob.
- K2 --ctx-checkpoints density is governed by checkpoint-min-step (default
  8192), not the count cap, at 210k ctx (25 < 30).
- K3 Adaptive MTP acceptance DROP vs fixed is by design (controller trims
  wasted depth); judge it on t/s, not acceptance rate.
- K4 At temp 1.0 all temp-0 acceptance/TPS figures are upper bounds.
- K5 The t_min revert is load-bearing on this 3-GPU layer-split topology:
  any future sync that reintroduces the delta-net conv-state hunk must
  re-run the PP16384 lane or PP collapses 326 -> ~202.
- K6 PR27210-UPSTREAM-COMMENT-DRAFT.md still unposted (user to review/post;
  agent must never post). Content remains accurate per D6.

## Open questions / pending

- Q1 Quantify q6v vs q6v-mix under full protocol at temp 1.0 (mix reported
  "way faster"; only deltas are -ts 39,21,40 vs 40,20,40 and -cram).
- Q2 Bucketed vs hysteresis at temp 1.0, deep fill (after port, DEC4).
- Q3 Does ngram-mod re-add help TG@180k on realistic replay-heavy chat
  (needs replay-style workload, not fresh-fill TG)?
- Q4 spec_ckpt per-round cost scaling with adaptive depth (D5): visible in
  lane TG deltas between n_max 10 and lower?

## Measurement campaign (2026-08-26 night)

Protocol per lane (C1): server start -> health -> temp0-repro-BEFORE ->
PP16384 -> fill 120k -> fill 180k -> TG1024 @ temp 1.0 -> temp0-repro-AFTER
-> server stop. Runner: bench/lane.sh + bench/lane-client.py. Results
appended to bench/logs/lane-results.jsonl; server logs bench/logs/lane-*.log.

| lane | config delta vs L0 | status | result |
|---|---|---|---|
| L0a | none (baseline) | DONE (attempt 4) | PP 328.2; f1 175.8; f2 104.9; TG@180k 7.9 acc .556; repro n/a (see note) |
| L0b | none (repeat, repro gate) | PENDING | |
| L1 | --ctx-checkpoints 0 | INCOMPLETE (2x silent server kill mid-fill2, cause unidentified - kernel OOM suspected, logs permission-gated; user stopped 2nd attempt) | |
| L2 | --ctx-checkpoints 64 --checkpoint-min-step 2048 | NOT RUN (same risk, low value; see K2) | |
| L3 | + ngram-mod (24/22/60) | DONE | PP 327.4; f1 169.3 (-3.7%); f2 104.8; TG@180k 7.9 acc .624 (parity with L0a) |
| L4 | LLAMA_SPEC_BUCKETED=1 (after port) | DONE | PP 327.6; f1 177.4; f2 106.1; TG@180k 7.6* acc .608 (tg_n 418: early EOS; mean len 2.53 = shallower drafting) |
| L5 | OLD config: draft-mtp,ngram-mod n10 (pre-adaptive prod) | DONE | PP 328.1; f1 175.6; f2 104.9; TG@180k 6.3 acc .690 |

- L3 RESULT: adaptive+ngram-mod on novel-content protocol = TG@180k 7.9 t/s
  (parity with adaptive-only L0a), identical TG sha, fill1 169.3 (-3.7% =
  ngram map maintenance cost during prefill). CONCLUSION: ngram-mod adds
  nothing for novel content at depth; our 08-25 "inert" verdict generalizes
  to the full protocol. PR-thread recall wins (147->321) require replayed
  content which this bench does not generate; nightly real sessions with
  tool-output replay MAY still benefit - unmeasurable with current harness.
  Production recommendation stays adaptive-only (avoid the fill tax).
- D11 (build pitfall): configuring a new build dir WITHOUT
  -DCMAKE_HIP_COMPILER=/opt/rocm-6.1.0/lib/llvm/bin/clang silently links
  against /opt/rocm (symlink -> ROCm 7.x, libhipblas.so.3) and the binary
  fails to start under our LD_LIBRARY_PATH (6.1 has libhipblas.so.2).
  Full flag set replicated from build-sync25 CMakeCache (see lane.sh
  BIN_DIR launch + journal). build-bucketed now correctly pinned.
- D12 (fit warning semantics): "failed to fit params ... n_gpu_layers
  already set by user to 333, abort" appears in EVERY lane's startup (fit
  cannot auto-adjust a user-pinned -ngl) and is NON-FATAL when the config
  actually fits. It only kills the server when free VRAM is genuinely
  insufficient (L4 attempt 2 died this way from residue of the just-killed
  previous attempt; GPUs verified clean afterward; retry succeeded). Do not
  panic on this warning; panic only on a following load abort.
- L4 port COMPLETE in source: common/speculative-adaptive.h gains
  common_speculative_adaptive_bucketed (verbatim from stew675
  bucketed-adaptive-mtp tip 1207c1c248af); speculative.cpp selects it via
  LLAMA_SPEC_BUCKETED=1 (ctor flag, reset sites, n_cap read, accept() feed
  incl. the branch's ngram cross-credit gate: feed if !is_other OR
  (bucketed AND n_accepted >= n_cur)). Default 0 = shipped hysteresis,
  byte-identical behavior when unset. t_min hunk NOT reintroduced (delta-net
  untouched). lane.sh now takes BIN_DIR override.
- L4 runs on build-bucketed (rebuilt 3x: see D11; final configure pins
  CMAKE_HIP_COMPILER + all six *_DIR package paths to /opt/rocm-6.1.0).
  Startup banner "adaptive controller: bucketed" = port verified live.

L4 RESULT: bucketed controller = NO WIN on this workload. TG@180k 7.6 vs
hysteresis 7.9 (and its TG hit EOS at 418 tokens; mean draft len 2.53 vs
2.67 -> bucketed ran shallower on prose, consistent with its design:
flatter climb from floor, no hardened 3->4 barrier, but prose content does
not reward depth either way). Fill1/fill2 marginally better (+0.9%/+1.1%,
at noise edge). DECISION DEC8: keep hysteresis as default; bucketed stays
available via LLAMA_SPEC_BUCKETED=1 for code-heavy workload days (untested
on code - Q6). Campaign conclusion so far: adaptive(hysteresis) IS the
best measured config for the nightly workload; nothing else beat it.

## DFlash2 integration (PR 27342) - 2026-08-26 late-night session 3

- D13 (PR 27342 read): DFlash2 = grouped dynamic depthwise conv + candidate
  selector on top of DFlash; selected automatically by GGUF (flag stays
  --spec-type draft-dflash). Evidence FOR us: lance0 5090 our exact target
  UD-Q6_K_XL: beats MTP at BOTH ctx ends (short 191 vs 132, 63k 100 vs 85)
  because drafter sliding_window=2048 keeps draft cost FLAT with depth;
  author clean rewrite f7aadef 1.82x vs +16.5% over best MTP (H200, t1.0);
  gfx1100 HIP +12.8%; Vulkan 7900 XTX +6.2% TG. Risks: vision broken at
  head (zective: image 500s - BLOCKS production adoption), -sm tensor
  assert (we are layer mode, N/A), z-lab GGUF loader crash
  (vector::_M_range_check, DaKev/russovincenzo: NaN in splits
  normalization when a device reports 0 free mem -> upper_bound end() ->
  devices.at() OOB; fixed upstream in #27657 - we did NOT hit this).
- D14 (integration): branch dflash2 = master + PR head f7aadef09, clean
  merge, 0 conflicts (our 27210/bucketed lives in common/speculative*.h/cpp
  which the PR touches but merges cleanly; bucketed port STASHED as
  'bucketed-adaptive port + session WIP', not on this branch). Build
  build-dflash with pinned 6.1 flags (D11 recipe). Drafter GGUF
  Qwen3.8-27B-DFlash2-Q4_K_M.gguf (1090MB) downloaded to ~/ai/ai (z-lab,
  post-08-24 = matches PR head format).
- D15 (crash root cause, 5 smoke attempts): draft ctx init GGML_ABORT
  "pre-allocated tensor (output.weight) in a buffer (ROCm1) that cannot
  run the operation (NONE)". Chain: DFlash2 GGUF has NO output.weight
  (full-vocab head borrowed) -> llama-context.cpp:153 sees
  model.output==nullptr -> draft ctx takes ctx_other=ctx_tgt -> draft
  reserve graph references TARGET output.weight (placed on ROCm1 = last
  device of target split) -> draft ctx sched lacks ROCm1 backend -> abort.
  --spec-draft-device DOES restrict draft model placement (verified:
  draft layers on Vulkan1 with -devd vulkan1) but ALSO restricts the draft
  ctx sched backends, which is exactly what breaks. Working recipe:
  NO -devd, all-backends draft ctx + --spec-draft-override-tensor
  '.*=ROCm0' + -ngld 99 -> server LISTENS (dflash-diag2.log). Note -otd
  did NOT fully pin draft layers (3,4,5 landed on ROCm1/Vulkan1 anyway) -
  placement suboptimal but functional; investigate later if draft
  round-trip latency matters.
- D16 (protocol change, user): lanes now ONE 120k fill then TG (FILL2=0
  env in lane-client.py). User allows ctx reduction for the drafter.
- L6 = winner config (L0a production flags) + draft-dflash n7 + drafter,
  -c 200000, build-dflash binary. RUNNING.

## DFlash2 verdict + validity check (session 3 close)

| metric @120k, temp1.0, -c200000 | adaptive (L0c) | DFlash2 n7 (L6) | delta |
|---|---|---|---|
| PP16384 | 325.2 | 335.3 | +3.1% |
| fill 120k | 175.3 | 193.5 | +10.4% |
| TG@120k | 8.6 | 8.4 | parity |
| acc / mean len | 0.592 / ~2.7 | 0.686 / 3.15 | - |

- VALIDITY (temp-0, same prompt/seed, 256 tok): adaptive MTP vs no-spec =
  BYTE-IDENTICAL (0 diff) on this rig; DFlash2 vs no-spec = diverges from
  char 266 (~70 tok in), single near-tie flip + cascade, outputs stay
  coherent and physically correct. Prime suspect: PR disables backend
  sampling for dflash2 (CPU argmax path flips near-ties); upstream
  gfx1100 user verified byte-identical losslessness in single-GPU setups.
  NOT proven lossy acceptance; treat as sampler-path numerics. Practical:
  temp-1.0 nightly chat unaffected; if bit-exact repro needed, adaptive
  MTP is the verified-exact config.
- FINAL CONFIG RECOMMENDATION: DFlash2 wins prefill (+3% PP, +10% deep
  fill) at TG parity; adaptive MTP wins exactness. Nightly launcher is
  mmproj-based and DFlash2+vision is BROKEN at PR head (D13) -> keep
  adaptive MTP in production; DFlash2 ready on branch dflash2 + build-dflash
  for text-only workloads (scripts documented in D15 recipe).
- Infra lesson D17: any script that LAUNCHES + WAITS in one shell command
  dies on user abort (setsid on the child does not help; the wrapper's
  process group is killed). Two-phase pattern mandatory: launch command,
  then poll/collect command. Also curl -sf for health (curl -s exits 0 on
  503-while-loading). GPU2 iGPU 75% VRAM with no process = leaked kfd
  allocation after kills; clears on its own, not a blocker.

## DFlash2 vision port + model survey (session 4)

- D18 (vision port COMPLETE): ported fishlikeX/sm70-attn main @2b611ea87
  (commit 5f93fe6a8 "restore mtmd + dflash M-RoPE position-hole fix",
  upstream #27408 family) onto our dflash2 branch. Three surgical hunks in
  common/speculative.cpp, all inside the dflash impl (our 27210/adaptive
  code untouched; their fork predates 27210 so the diff's other hunks were
  ignore-side):
  1. process(): pure-embd batches -> early return true (skip; hole left)
  2. process(): per-seq gap fill - zero-feature rows for [pos_max+1,
     first_pos), M-RoPE 4-row positions, ubatch-chunked encode+decode
  3. draft(): noise-block base = ctx_dft pos_max+1 (not dp.n_past; token
     count vs position scale diverge after images)
  Note: their begin() pos_max check demotion (mentioned in their PR
  comment) is NOT in their final code - gap-fill makes it moot. Skipped.
- D19 (validation): build-dflash rebuilt clean. Server with mmproj +
  DFlash2: text 200; IMAGE request 200 with correct description of solid-
  blue test square; log shows "zero-filled 3 draft-cache hole rows for
  seq 0 (skipped mtmd chunk / reused prefix)" - mechanism confirmed live;
  acceptance during image-span generation 0.318 (expected dip, output
  exact per lossless verification). VISION + DFLASH2 NOW WORKS on our
  rig. Bonus: gap fill also covers cached-prefix reuse holes (laidick's
  tool-call case).
- D20 (model survey): only two DFlash2 GGUF repos exist - z-lab (08-24,
  current, matches PR head loader; ours) and incoai (08-18, old format,
  crashes loader per cuiyuxin-gif - avoid). Quants BF16/Q8_0/Q4_K_M;
  author's own bench: 19.31/18.43/18.89 t/s and accept 4.92/5.08/5.03 -
  differences within noise, Q4 >= Q8 in t/s. Q8_0 costs +310MB VRAM for
  ~nothing, BF16 +1.1GB. DECISION DEC9: keep z-lab Q4_K_M (already have
  it); no better drafter exists today. Watch for DFlash2-v2/DSPARK
  checkpoints later.
- NOT committed (policy): vision port sits as working-tree changes on
  dflash2 branch; build-dflash binary is current with them.

## CAMPAIGN CLOSE (2026-08-26 late, superseded by DFlash2 verdict above)

Final league table (TG@180k temp 1.0, the nightly-relevant metric):
adaptive-hysteresis 7.9 > adaptive+ngram 7.9 (tie, but ngram taxes fill
-3.7%) > bucketed 7.6 (early-EOS run) > old-prod mtp+ngram 6.3 (-20%).
PP16384 and fills identical across all lanes (327.4-328.2 / 169-177 / 105-106).

FINAL RECOMMENDATION: production launcher stays EXACTLY as shipped
(uf2/uf3 llama-start-q6v.sh, adaptive n10 floor3, build-sync25 binary).
No config change from this campaign improves it. The absolute TG@180k
ceiling ~8 t/s is bandwidth/attention-bound; spec-decode tuning cannot
move it further on this hardware (K7).

Loose ends: L1/L2 checkpoint lanes incomplete (low value per K2: count
cap 30 never binds at 210k/min-step 8192; only min-step changes density);
L1 server died silently twice mid-fill2 (unidentified, suspected host-RAM
OOM; if it recurs, check `journalctl -k` with root); L4's early EOS means
bucketed-on-code remains unmeasured (Q6). NEXT SESSION CANDIDATES: none
blocking; optional code-lane bench for bucketed, optional L1 retry.

L5 RESULT (the critical A/B): old prod config TG@180k temp1.0 = 6.3 t/s
vs L0a adaptive 7.9 t/s = ADAPTIVE +25% AT DEPTH. PP and fills at parity.
Old config acc 0.690 vs adaptive 0.556: higher acceptance + lower t/s =
wasted-depth signature (deep fixed drafts cost more than they return;
adaptive trims to profitable depth). User's overnight impression reconciled:
+25% on ~8 t/s base = +1.6 t/s absolute, imperceptible subjectively but
real and measured. L5 repro-after acceptance 0.206 (ngram at deep filler
context degrades greedy repro drafting - noted, not investigated).

L0a FINAL (attempt 4, correct deep TG): load 24s, PP16384 328.2 t/s,
fill1 103,612 @ 175.8 t/s, fill2 60,004 @ 104.9 t/s, TG1024 temp1.0 @180k
depth (cache_n 179,999 verified) = 7.9 t/s, acceptance 0.556, mean len 2.67.
Attempt-to-attempt repeatability: PP 328.2/327.3/328.2, f1 176.0/175.4/175.8,
f2 105.1/105.0/104.9 - all within 0.6%. Depth TG curve on this stack:
fresh ~19 -> 120k ~11 (08-25, temp0) -> 180k 7.9 (temp1.0). Q5 resolved:
08-25's 11.2@120k is consistent; nothing regressed.
INTERPRETATION: nightly deep sessions run at ~8 t/s; that is the number
the campaign must move. L5 (old prod config) is the critical A/B.


### Lane notes

- L0a attempt 1 FAILED (infra, not config): launched the runner with plain
  `nohup ... &` inside a monitored command; when that command hit the shell
  tool's timeout, the harness killed the process group - runner and client
  died silently, server followed; 0-byte artifacts. LESSON (D10): long jobs
  must be launched as `setsid` (own session) with status marker files
  (/tmp/opencode/lane-*.status RUNNING/DONE/FAILED), polled with SHORT
  commands only. Also fixed lane-client.py to tokenize the multi-MB filler
  once and slice per stage (was re-tokenizing per fill, minutes of waste).
  Attempt 2 (setsid) running clean.
- L0a attempt 2 COMPLETE (protocol bug found + fixed mid-run):
  - BUG (mine): TG payload had cache_prompt False -> slot cleared -> TG ran
    at ~fresh depth, NOT at 180k. Fixed: TG now cache_prompt True (extends
    the filled slot; tg_depth_cached records cache_n). PP/fills were valid.
  - Valid numbers from attempt 2 run 1: PP16384 first batch 328.2 t/s
    (best-ever parity; binary + revert healthy), fill1 103,612 proc @
    176.0 t/s (16,391 cached), fill2 60,004 proc @ 105.1 t/s (119,999
    cached), TG(shallow, invalid as deep metric) 18.2 t/s, fresh greedy
    repro 19.0 t/s acc 0.538 len 2.60.
  - repro_ok FALSE (sha before != after): expected nondeterminism, not
    corruption. Known effect (08-25 session saw it too): greedy + spec
    decode is not bit-deterministic when draft patterns differ; after 180k
    of filler the slot's MTP carryover state differs from fresh, drafts
    differ, verify-batch composition differs -> FP reduction order flips
    occasional tokens. Also exactly the #27296 carryover mechanism Flo5k5
    described (stale h-row paired with new prompt). Not a regression; the
    true determinism gate is fresh-server L0a vs L0b task-1 sha.
  - OPEN Q5: 08-25 doc claims TG@120k deep fill = 11.2 t/s (temp 0); today
    TG@~fresh = 18.2-18.9 t/s. Deep TG from the fixed client will tell
    whether 08-25's 11.2 was a different measurement path (bench-client
    TG_FILL re-process) or real degradation this stack no longer has.
  - Attempt 3 (fixed TG depth) = final L0a, running.


