# Journal 2026-09-05

## E119.6 - 2026-09-05

- E119.6 mirror promoted to production, 2026-09-05 (~00:15):
  User asked whether run scripts use the latest build; audit said no:
  every default (prod launcher, v23 twin, ab-bench.sh, cmp-lane.sh
  FORK_BIN) pointed at build-dflash-novega built 09-03 21:31 (E116
  promotion, pre-mirror, 0 hits for LLAMA_DFLASH_MIRROR_OUTPUT in
  libllama.so). Fix, chosen = rebuild in place (all script defaults
  stay valid) instead of pointing the launcher at build-mirror:
  1. Rebuilt build-dflash-novega incrementally from HEAD 5e51ecad9
     (only src delta vs 09-03 build is the mirror, e9494022b).
     Verified: libllama.so.0.3.0 relinked 09-05 00:14, mirror symbol
     2 hits, llama-server --version prints commit 5e51ecad9.
  2. Launcher 2llama-start-iq6v-dflash2.sh flipped: env
     LLAMA_DFLASH_MIRROR_OUTPUT=1 + "--spec-draft-device ROCm0" next
     to the existing --spec-draft-override-tensor (exactly the
     validated m8 lane config: lane base args + EXTRA flag). Header
     note added: -42% draft rounds / +11% TG at depth (E119.3),
     ~1 GiB host-staged mirror, +2-10 s load.
  NOT committed: launcher file also carries the user's pre-session
  -c 257000 experiment; committing needs user go (or revert to
  250000 first). build-mirror kept as the validated reference
  binary. A running prod server keeps old inodes until next launch.
  v23 twin left untouched (untracked user file, still points at
  build-dflash-novega = now current anyway).

- E120 unturned-lever audit (user request: "any levers unturned?"), 2026-09-05:
  Sources: README adopted/rejected tables, FINDINGS.md, journals E1-E119
  (full reads 09-03..09-05 + targeted 08-26/27), build-dflash-novega.sh,
  prod launcher, upstream delta 95ef7fc16..origin/master (28 commits).
  CLOSED (measured dead, do not re-lane): MMQ restructures x5 (41pc issue
  ceiling structural), fattn schedule/geometry, ts rebalance, -ctk q8_0,
  mix surgery, SCHED_POOL/defer-catch-up, checkpoint density, gdn chunk,
  q8_1 cache, mx layout cache, n_max 5/6 as-is.
  UNTURNED, ranked by expected value:
  1. Target-pass fixed overhead (77pc of TG round, ~130 of ~150 ms @120k;
     batch/depth/graphs-insensitive = kernel-chain latency + 3-stage
     pipeline handoffs). Attacks named in E119.1, all unattempted: decode
     graph kernel-chain fusion, stage-handoff batching, MoE MUL_MAT_ID
     batch<=5 path (verify batch = exactly 5 rows at n4 = the MMVQ/MMQ
     threshold boundary; same family as the n5 cliff). 15pc off the pass
     = ~+2 t/s at depth.
  2. TG-phase in-process round tracer (rocprof TG interposer segfaulted
     2x, E91 + E119.1.4). Not perf itself; the gate for 1. Tracer hook
     lives next to the existing dur(b,g,a) timers.
  3. 6-row verify cliff root cause (E119.4: +50 ms at n5; suspects MoE
     MUL_MAT_ID threshold crossing or f16-shadow convert geometry at 6
     rows). Fixed -> n_max=5 reopens (pos-5 acc .133 ~ +1 free tok/round).
  4. Drafter-side MMVQ: #23685 (packed Q8_1 MMVQ) tested target-side
     only; #26705 (branchless Q4_K/Q5_K MMVQ) screened but never laned.
     Drafter = Q4_K_M at batch-1 MMVQ; draft now 18.8 ms/round (~12.5pc
     of round) post-mirror. README/E113 parked exactly this.
  5. HIP_FAST_MATH=1: sole remaining unmeasured build opt-in (E71 item 1;
     build script header). One lane pair sizes it. Sha-breaking: policy
     gate before any adoption.
  User-gated pending (not new levers): 256k/260k ctx variant validation
  (E105/W6), fa1s-auto250b + native250 verify lanes (NEXT-LANES), E80
  Q8_0-drafter@250k crash root cause, launcher commit (carries user
  -c 257000 experiment). Upstream since sync: 28 commits, none
  perf-relevant (Metal/SYCL/OpenCL/UI + renames); nothing to cherry-pick.

- E120 prep, 2026-09-05 (server up, no GPU lanes; user approved the
  campaign):
  1. TRACER: bench/rd-trace.c + rd-trace.map + rd-trace-analyze.py +
     rd-trace-lane.sh. LD_PRELOAD shim on the E115 rd2d pattern:
     version script hip_4.2 (hipLaunchKernel, hipMemcpy/Async,
     hipMemsetAsync) + hip_4.3 (hipGraphLaunch); 48 B records into an
     mmap ring (RD_TRACE_MB, 768 default in the lane wrapper - content
     survives SIGKILL); amdgpu sysfs busy sampler (card3/card4 = the
     VIIs) starts on first HIP call. Self-test PASS (found + fixed
     now_ns missing -t0.tv_nsec: up to 1 s of timestamp garbage).
     Tracer lanes run GGML_CUDA_DISABLE_GRAPHS=1 - individual launches
     visible, graphs TG-neutral (E119.1.1); hipGraphLaunch still
     traced for graphs-on runs (round markers).
  2. E88 CORRECTION (supersedes E120 item 4): #23685/#26705 drafter
     MMVQ is NOT open. E88's A/B ran the full prod config - the packed
     layout serves exactly the drafter's ncols==1 steps - tg -0.6%
     noise, and that draft slice was 2x today's post-mirror size.
     Both dropped.
  3. NEW PROBE (replacement): GCN MMVQ-for-MUL_MAT_ID table (mmvq.cu
     get_mmvq_mmid_max_batch_gcn) caps Q6_K at 4; the n4 verify batch
     is 5 rows -> every MoE verify crosses to MMQ-for-ids. RDNA2/4
     tables allow 5. Worktree ../uf3-wt-mmid5, branch exp/mmid-q6k5,
     one-line change (Q6_K 4->5, uncommitted). Verify-pass-only: PP
     batch 384 >> 8 and batch-1 decode unaffected; tg is the metric.
  4. BUILDS chained: log /tmp/opencode/build-e120-chain.log, markers
     /tmp/opencode/build-{fastmath,mmid5}.{OK,FAIL}; build-fastmath
     (HIP_FAST_MATH=1, E71 item 1) then build-mmid5 (worktree).
     JOBS=12 TARGETS=llama-server (prod server up - CPU courtesy).
     First launch died to an agent-shell timeout group kill (nohup +
     disown is not enough, SIGTERM reaches the group); relaunch uses
     setsid - same discipline as lane-dflash server launches.
     Disk 5.6G free pre-build, ~1.5G needed; the 7 stale build dirs
     (~9.7G, E114 list) still await user cleanup approval.
  5. A/B harness: bench/ab-fastmath.sh + bench/ab-mmid5.sh (ab-mmvq23685
     pattern, i1 @250k prod shape; mirror parity BOTH sides per E119.6:
     LLAMA_DFLASH_MIRROR_OUTPUT=1 + --spec-draft-device ROCm0; ab-bench
     launches the server bare - exports inherit). Gates: fastmath
     pp/tg/acc with sha expected to MOVE (numerics class, adoption =
     user policy call); mmid5 tg/acc, pp flat expected, sha either way
     (E93 precedent).
  6. WINDOW PLAN (~2.5 h, server down, game closed): e120t0 tracer
     validation (FILL1=16000) -> e120t1 deep (FILL1=120000, n4) pass
     decomposition -> e120t2 n5 tracer diff (cliff kernel) -> ab-mmid5
     -> ab-fastmath. Server restart after = user action. Commits of
     any winners pending user approval (AGENTS.md).
  7. BUILDS GREEN + RUNNER ARMED (2026-09-05 22:16): build-fastmath OK
     (cache CMAKE_HIP_FLAGS=-ffast-math -fno-math-errno verified; binary
     --version build 10949 commit 29bece8d2) and build-mmid5 OK (worktree
     exp/mmid-q6k5, same config + the one-line table; binary same build
     id, exit 0). bench/run-window-e120.sh ARMED at 22:17 (PENDING, fires
     when the rig frees; both .OK markers gate it). mmid5 engagement
     proof rides on the tracer grid-fingerprint diff A-vs-B at verify
     time (MMVQ vs MMQ-for-ids have distinct launch shapes; a tg delta
     alone cannot distinguish faster from never-engaged). Disk freed
     externally mid-session (5.3 -> 80G free); the 7 stale build dirs
     are still present (~9.7G, E114 list) - cleanup approval still
     open. Scripting lesson banked: the write path dropped a quote
     inside the nested "$(dirname "$(readlink ...)")" idiom in
     run-window-e120.sh; bash -n caught it and bisect-by-prefix +
     od localized it faster than re-reading the file.

- E120.1 WINDOW RESULTS + TG DECOMPOSITION (2026-09-05 22:31-23:15,
  user-stopped prod for the window):
  1. A/B VERDICTS (ab-bench, i1 @250k, mirror parity, both sha canonical
     e54019ff6b42):
       mmid5-A (table 4): pp1 399.6 tg 18.6 acc .656
       mmid5-B (table 5): pp1 398.2 tg 18.3 acc .656
       fastmath-A:        pp1 398.8 tg 18.4 acc .656
       fastmath-B:        pp1 398.4 tg 18.2 acc .652
     mmid5: NEVER ENGAGED - tracer fingerprints of e120t0b (stock) and
     e120t3 (mmid5 bin) are BYTE-IDENTICAL (same 534037 launches, same
     grids, same counts): the GCN mmid table does not gate this
     workload's MUL_MAT_ID shapes (ne2 at verify is not 5; likely >8 or
     the fused MoE path bypasses). Dead lever, wrong shape assumption.
     fastmath: PERF-NEUTRAL AND SHA-NEUTRAL (256-token greedy identical
     under -ffast-math). E71 item 1 CLOSED; no adoption (nothing gained,
     nothing at risk). Build kept for reference.
  2. TRACER INFRA, three bugs found+fixed mid-window (all banked):
     a. e120t0 crash: tracer lanes omitted --no-mmproj-offload -> only
        250k-fit lane loading vision on GPU; force_convert shadow did
        not fit -> ggml_cuda_pool_leg abort at first decode. Fixed in
        rd-trace-lane.sh.
     b. run-window trace_lane: "$@"-expanded VAR=VAL words are NOT env
        prefixes in bash (command-not-found); env(1) wrapper fixes it.
     c. THE WIPE: constructor-time open(O_TRUNC) ran in EVERY preloaded
        child (bash wrappers, python lane client) - the client runs
        after the server and destroyed its trace (1 marker, 0 records).
        Fix: lazy_init on first interposed HIP call; non-HIP processes
        never touch the file. Validated: --version creates no file.
  3. TG DECOMPOSITION (rd-trace, graphs off - TG-neutral E119.1.1;
     t0b/t3 n4@16k, t2 n5@16k, t1b n4@120k; sysfs cards: card2+3 =
     VIIs 0x66af, card4 = APU iGPU 0x1638, game off):
     - Round shape: ONE ~1.9 ms host-launch burst (~800 kernels)
       + ~49 ms quiet @16k / ~69 ms quiet @120k. Bursts 9.3/s vs
       ~7.9 rounds/s => ~1.18 bursts/round.
     - VIIs ~40-45 pct busy on average THROUGH BOTH phases (busy-inside
       35-46 pct vs busy-quiet 37-42 pct): GPUs chew the async chain
       while the host waits. => ~55-60 pct of VII wall time during TG
       is IDLE, waiting on host machinery + serial pipeline handoffs,
       NOT on kernels. Launch overhead is ruled out (1.9 ms bursts).
     - n4 vs n5 SAME kernel families (grids just scale gy 5->6: (20,5)
       ->(20,6), MoE (100,1,1,128)->(120,1,1,128)), memcpys ~13.4/round
       both, and +19 ms/round of QUIET at n5 => the E119.4 "6-row
       kernel cliff" is actually HOST-SIDE (prime suspect: CPU dflash
       lattice walk growing with depth). n5 stays dead end-to-end
       (-28 pct TG, E119.4) but the fix class is CPU code, not kernels.
     - Top verify fingerprints (n4): (20,5,256)x29.5k, MoE batch-scaled
       (100,1,1,128)x38.3k, (5,1,1024,shm128)x27.2k - the baseline set
       for any future kernel-side comparison.
  4. LEVER-1 VERDICT: the big TG pool (~55 pct VII idle) is CONFIRMED
     and MEASURED, but the attack surface is host spec machinery +
     pipeline serialization (CPU-side scheduling, event syncs, lattice
     walk), not GPU kernels or dispatch tables. Config space stays
     exhausted; kernel-family space measured clean at these shapes.
     Any next step = speculative.cpp-side restructure (invasive,
     uncertain) - parked pending user interest.
  5. Rig free since 23:15; prod restart = user action. Artifacts:
     traces /tmp/opencode/rd-trace-e120t{0b,1b,2,3}.bin (768 MB each,
     tmpfs - delete freely), analyses bench/logs/rd-trace-*.analysis.txt,
     6 lane rows + 4 ab trial rows. Builds build-fastmath/build-mmid5
     kept; branch exp/mmid-q6k5 closed (patch uncommitted in worktree).

- Same session, README fact-check + survey table (commit 5e51ecad9):
  added "Upstream PRs and projects evaluated" table (19 rows, PR
  authors fetched live from GitHub API: PatrickWalther, iacopPBK,
  ravel7524 x2, pwilkin x2, GiuseppeCapaldo93, ynankani x2, praneshgo,
  am17an, ggerganov, mxxm-t x2, kilofox, uaruss, avialallon, Geramy).
  Two fact fixes: "n_max 5/6" -> n_max=5 (only 5 tested; 6-row = 5
  drafts + target), "compute ~5x smaller" -> 4.7x (E32 measured).
  Dedup: PR rows moved out of "What did not work" into the survey
   table. All README numbers re-verified against lane-results.jsonl:
   r1/m8 exact, composed ~410/~264/~15.1 arithmetic confirmed, sync
   95ef7fc16/65 commits confirmed.

- E120.2 SYNC-SPLIT + PERF: WHERE THE TG WALL LIVES (2026-09-05
  23:23-23:45, "go on then" continuation; rig still window-owned):
  1. TRACER SYNC EXTENSION: rd-trace.c now interposes hipStreamSync+
     hipEventSynchronize (new RK_SYNC=6, duration in a0), hipMemcpy
     duration in a1; map exports 7 symbols; analyzer prints sync
     report. Lazy-init re-validated (no file on --version).
  2. e120t4 lane (n4 parity, 120k fill, graphs off): of a 90 s TG
     window, HOST SPENDS 65 pct (58.1 s) BLOCKED INSIDE HIP SYNCS.
     30,856 sync calls but only 1,040 calls >=1 ms carry 57.7 s
     (~1.3 big waits/round, p50=0.00 ms, p99=50.8 ms = the round
     quiet). By stream: two VII streams hold 31.0 s + 20.5 s (the
     draft-side stream the larger), third stream 3.9 s, null/event
     2.7 s. Sync hipMemcpy total ~0 s (copies never block).
  3. e120t5c perf attach (cycles:u, 399 Hz, 40 s inside TG @16k,
     parity config, bench/perf-tg.sh reusable): ~89 pct of host
     cycles = spin loop inside libhsa-runtime64.so (the ROCm sync
     busy-wait), 7.4 pct ggml_vk_wait_for_fence (libggml-vulkan,
     waiting on the 3080 segment). ALL llama.cpp machinery COMBINED
     <1 pct (common_sampler_sample 0.33 pct, memmove 0.42 pct).
     => HOST MACHINERY IS NOT A TG LEVER. E120.1's "CPU lattice
     walk prime suspect" for the n5 cliff is WEAKENED (walk is
     <1 pct here); n5 quiet growth stays host-side but small-scale.
  4. FINAL TG MODEL (three independent measurements agree): round
     wall = rocm0 segment + 3080 segment + rocm1 segment + draft,
     serial per token; VIIs busy 41/46 pct = exactly their ts layer
     shares (35/45 + draft on rocm0); host parked in syncs 65 pct
     of wall, spinning (HSA busy-wait) rather than working; 3080
     does its 20 pct in only ~9 ms/round = fastest per-layer of
     the three devices. The "~55 pct VII idle pool" is structural
     pipeline serialization, not a recoverable kernel/launch/CPU
     pool. Config, kernel-family, dispatch-table, host-machinery
     spaces: all measured, all closed.
  5. ONE LIVE LEVER REMAINS: ts REBALANCE TOWARD THE 3080 (it is
     per-layer fastest and holds only 20 pct). Moving VII layers
     onto it equalizes segment times AND frees VII VRAM (headroom
     = ctx upside, cf. dual-gpu-context-balancing skill). Trial
     matrix @16k (~4 min/lane): 35,20,45 base / 30,30,40 / 25,35,40.
     NOT RUN: user window etiquette (each trial needs the GPU).
     Caveat: README row "ts rebalance (E105 + 08-15 sweeps) decode
     is overhead-bound" predates the mirror + this decomposition;
     re-test under current build.
   6. Session hygiene: two more shell-timeout kills of launch
     wrappers (state re-verified after each, no lost lanes: e120t4
     DONE, e120t5b DONE n7-default-config discard, e120t5c DONE
     perf). perf-tg.sh DELAY timing: load 22 + PP16384 ~39 + fill16k
     ~41 = ~102 s to TG start; attach +108 s.

- E120.3 TS-REBALANCE LEVER DEAD AT 250k: VRAM-WALLED (2026-09-05
  23:48-00:05, user asked "can we improve that issue with the
  stalls?"):
  1. e120t6a (ts 30,30,40, parity env, C=250000): OOM AT LOAD -
     ggml_vulkan "Device memory allocation of size 784351232
     failed" -> kv cache buffer alloc fail on Vulkan1. e120t6b
     (25,35,40) CANCELLED before run: strictly more on Vulkan1,
     would OOM harder. (Lane-client hangs forever on a dead
     server - killed manually; candidate for a client timeout.)
  2. Baseline memory breakdown @35,20,45/250k (from e120t5c log)
     explains why the split is rigid:
       ROCm0  16368 total, 13218 used (7147 model + 4768 ctx +
              1302 compute), 2699 free - hosts drafter + mirror
              in that slack
       Vulkan1 8192 total (8 GB CARD, not 16), 7165 used @20 pct,
              482 free vs 1024 target - already over-committed
       ROCm1 16368 total, 15913 used @45 pct, -25 free - zero
     => 35,20,45 is the UNIQUE fit at 250k; the per-layer-fastest
     device (3080, E120.2) cannot absorb a single layer more.
     README row "ts rebalance does not help (E105 + 08-15)" now
     has a measured mechanism. Segment-time rebalance is closed
     at the product context size.
  3. STALL-IMPROVEMENT FINAL ANSWER (250k): config space closed
     end-to-end (E120.1-E120.3). What remains, in cost order:
     a. -np 2+ serving: second slot fills idle device time with
        concurrent requests (throughput, not latency; needs VRAM
        re-fit for 2nd ctx or shares the 250k pool).
     b. speculative.cpp/sched restructure to overlap draft with
        target tail: TRUE data dependency on the sampled token -
        only partial overlap possible (prefix prep), invasive,
        parked.
     c. Trade ctx for TG: at lower n_ctx the 3080 can take more
        layers (ts toward fastest device) - against the product
        goal (250k is the point), listed for completeness.

- E120.4 PR SWEEP + #28190 NON-ADOPTION (2026-09-06, user asked
  for interesting PRs then "adopt 28190"):
  1. PR sweep since 09-03 sync (45 merged + open scan): watch
     list updated - #28457 vulkan small-M qwen (jeffbolznv),
     #28422 vulkan topk_moe prefill fusion, #28118 spec
     recurrent-state checkpoints on-device (stall-relevant),
     #28198 concurrent streams per split (merged, graphs-gated).
     Not for this rig: IQ4_XS vulkan wave, RDNA4/gfx1201 wave,
     HIP AllReduce, MMVQ nwarps.
  2. #28190 (vulkan FA dequant path engagement): ALREADY IN THE
     FORK - upstream commit c7bda030e rode in with the 09-03 sync
     (merged upstream 08:40 that morning); prod binary rebuilt
     09-05 contains it. Adoption attempt: patch did not apply
     because the fixed expression `(t->ne[3] == 1 || ...)` is at
     line 10969. CORRECTION to the E120.3-era claim: the 3080 FA
     dequant fast path HAS been engaging in all E120 baselines -
     the ~9 ms/round 3080 segment estimate is the post-fix
     number; no missed upside there. build-28190 hardlink clone
     removed, tree clean.

- E121 PR TRIALS, ONE BY ONE (2026-09-06 00:10+, user: "lets go
  with those and try them one by one", starting #28118):
  1. #28118 spec recurrent-state checkpoints on-device
     (vahpetr, +8/-8 server-context.cpp): applies clean to fork,
     builds clean. LANE A/B, standard 120k protocol (fill 120003
     + TG1024 @depth, n4, parity env, 250k ctx):
       e121b STOCK: pp1 405.1 fill 263.0 tg 12.9 acc .657
       e121a PATCH: pp1 398.4 fill 262.0 tg 12.4 acc .643
       both sha canonical e54019ff6b42, repro_ok
     VERDICT: REJECT - tg -3.9 pct @120k (approx -2 pct after
     acc-noise allowance), pp/fill neutral. Mechanism: on this
     3-device layer split the GDN recurrent state lives on ROCm1
     (-25 MB free); host-backed copies were already overlapped
     (E120.2: 89 pct HSA spin, copies free) while on-device
     restore adds scheduler/sync pressure. The author's +73 pct
     win case (single-device gfx1151) does not transfer to
     multi-GPU splits. Patch reverted, build-28118 removed.
     NOTE: ab-bench tg numbers (18.2-18.6) are SHALLOW-depth
     rows; first true 120k-depth stock baseline = e121b 12.9.
  2. #28457 vulkan small-M qwen (jeffbolznv, +29/-10, m=1 A/B
     swap + small-m tiles/split_k; targets qwen MoE router/GDN
     GEMVs on the vulkan device): applies+builds clean.
       e122a: pp1 403.2 fill 262.7 tg 12.8 acc .653 sha canonical
     VERDICT: PERF-NEUTRAL (tg -0.8 pct = noise; the 7x op-level
     win dilutes to nothing - those ops are a sliver of the
     3080's 13 pct segment). Reverted; upstream-bound anyway.
  3. #28422 vulkan topk_moe fusion via add_alloc_dep
     (jeffbolznv, +60/-37; API already in tree from sync):
       e123a: tg 13.1 acc .662 / e123b: tg 12.9 acc .657
     acc-matched pair vs stock 12.9 = DEAD NEUTRAL (the +1.6 pct
     single-lane was acc noise). PP/fill neutral both. Reverted;
     sync-gated later.
  4. #28198 concurrent-streams-per-split guard fix: ALREADY IN
     FORK via 09-03 sync (line 4714-4718 shape matches, no
     device_count guard + explicit set_device). BONUS DISCOVERY:
     the underlying pass is opt-in env GGML_CUDA_GRAPH_OPT=1,
     never enabled here. Trialed on stock binary:
       e125a: pp1 226.1 (-44 pct) fill 140.1 (-47 pct) tg 12.9
     (neutral, acc .657 identical) sha canonical
     VERDICT: HARD REJECT - the per-rebuild dependency analysis
     wrecks PP (graphs rebuild per batch), and TG neutral
     confirms E120.2: no intra-segment overlap to harvest; the
     serialization is inter-device. Env var stays off.
  5. SERIES SUMMARY: 0/4 adoptable. #28190+#28198 already in via
     sync; #28118/#28457/#28422 measured dead or regressive on
     this rig. All lanes sha canonical e54019ff6b42 throughout.
     True 120k-depth stock TG baseline on current binary: 12.9
     (e121b). GPU free after 01:33.
  6. WIDER SWEEP + #28432 (01:35-01:47): searched open PRs by
     cuda/moe/kv/server titles (previous sweep only
     speculative/vulkan/rocm). #28432 "top-k MoE should always
     fire" (am17an, +78/-12) applies+builds clean: e126a tg 12.9
     acc .657 EXACTLY baseline = DEAD NEUTRAL (fused path
     already fires on our shapes). Reverted.
     Correctness-class, no local repro -> SYNC WATCH LIST:
     #28383 (cuda MMQ MoE OOB, crash-class, vision repro not
     ours), #28302 (ckpt eviction on short prompts), #25592
     (hybrid ckpt restore invariant), plus #28391/#28333/#27692
     from earlier. Dismissed: expert-offload trio #25294/#27861/
     #28414 (fit lever, adds H2D to serialization-bound TG),
     FP8 KV #28392 (no gfx906 hw), lazy q8_0 KV #28267 (we
     quantize at load already).
     PR LANDSCAPE NOW EXHAUSTED for lane-triable candidates:
     6 trialed tonight, 0 adoptable, 0 sha movements.






