# JOURNAL 2026-09-11

## E142 - Qwen3.8-Flash-Next 200k serve config (recovered session, continued)

### Recovery record (post-reboot)
- Prior session `ses_f73b5ffe9ffegfIQF8041lxSRo` (project = this tree) died mid-step at
  00:47 local (reboot). Its /tmp evidence was wiped; findings below recovered from the
  session log. This session re-verifies rather than trusts.
- User constraints (restated): **f16/f16 KV mandatory**, **adaptive MTP in the serve
  config**, PP16384 as separate small-ctx lane (pipeline-parallel at 200k impossible:
  needs ~8 GiB free on rocm0).
- Working deterministic lane (E141.10): threads 8/8 + poll 0/0, build-qwen38-mtp
  build 11060 (b1a8d8b63 + PR #28243 port + q8_1-cache & GDN-chunk default-off).
  Clean reference hash 9d4badea.

### Recovered placement facts (pre-reboot, /tmp evidence lost)
- F64 FITS: `-sm layer -ts 64,10,26 -ncmoe 24` @ c=204800 f16/f16:
  rocm0 15.7/16.2, rocm1 15.4/16.2, RTX 6.3/8.2 GiB (~92% util).
- 200k KV needs 3.1 GiB contiguous on rocm0; at ts 70,13,17 rocm0 had only 2.77 free
  (that was the original blocker, fixed by 64,10,26 rebalance).
- q8_0-V arm (Q64) fits - DISCARDED per f16/f16 mandate.
- Adaptive MTP blocker: draft bundle = 2647.04 MiB (weights ~1.3 + KV ~0.35 + compute
  ~1.0 GiB) lands whole on rocm1; no --spec-draft-device flag in this build
  (verified). rocm1 has no room at F64; at a ts-23 variant rocm0 had ~3060 MiB free.
- -fit is an auto-tuner for unset args, NOT a buffer shrinker - not our lever.
- Interrupted step: `--fit-print` per-device memory estimates on the MTP-stacked fit.

### E142 plan
1. Re-verify F64@200k fit post-reboot + capture --fit-print table.
2. Same probe with adaptive-MTP sidecar stacked; solve draft-bundle placement
   (ts rebalance so one device has >=2647 MiB free; -ub shrink as fallback).
3. Determinism reps + smoke on the final 200k+MTP config.
4. PP16384 lane (bench/qwen38-pp16384.sh, c=20480, PP off per script).
5. Adaptive MTP depth tuning + production launcher; journal + commit.

### Log
- 01:0x Environment verified post-reboot: both Radeon VII idle 16 GiB free each,
  3080 86 MiB/8 GiB, model + sidecar + build-qwen38-mtp intact, RAM 48/62 GiB free.
  Git: 3 campaign commits present (f2f1a558e port, 4d41a52d6 default-off fixes,
  d95eaaf77 bench harnesses); bench/qwen38-fitprobe.sh has unstaged tweaks.
- 01:1x Re-ran interrupted step: --fit-print probes (below).
- 01:1x `--fit-print` DOES NOT EXIST in this build (pre-reboot session died holding an
  unverified assumption; arg-parse abort, no load). Real flags: --fit/-fitt/-fitc
  (auto-tuner, stays OFF for this arch per AtomicChat card) and -otd
  (--override-tensor-draft) which IS the lever for steering the 2647 MiB draft bundle.
- 01:2x F64@200k re-probe OOM'd instantly (10.3 GiB alloc fail on "idle" card).
  Root chase: kernel-truth sysfs showed card2 15.0/17.2 GiB + card3 9.9/17.2 GiB
  vram_used invisible to rocm-smi. CAUSE: the 01:00 baseline fitprobe was
  user-aborted mid-script; its backgrounded llama-cli (PID 62378) survived the abort
  and kept loading the model. Killed; VRAM released to ~11 MB. LESSON: fitprobe
  backgrounds llama-cli with no trap - aborts orphan it (fix pending).
- 01:3x MTP-bundle placement SOLVED at 200k:
  - -otd '.*=ROCm0' moves the 2647.04 MiB draft-weight range to ROCm0 (no
    --spec-draft-device needed - the flag the pre-reboot session missed).
  - Draft KV (400 MiB @ 200k) lands on the BORROWED target tail-layer device (ROCm1)
    regardless of -otd; freed via ts share shift 64,10,26 -> 66,10,24.
  - ncmoe ladder evidence: 27/29/31 all free ROCm0 only (CPU expert shift walks
    layers 24-30, all rocm0-resident; rocm1 expert sets start ~layer 37).
  - **FITTING CONFIG (mtp200k-e)**: `-ncmoe 31 -sm layer -ts 66,10,24 -c 204800
    -ctk/-ctv f16` + `-md MTP/mtp-Qwen3.8-Flash-Next-shared-Q8_0.gguf --spec-type
    draft-mtp-adaptive --spec-draft-n-max 10 --spec-draft-n-min-adaptive 3 -ngld all
    -ctkd/-ctvd f16 -otd '.*=ROCm0'` -> rocm0 14.65/17.16 GB, rocm1 16.24/17.16,
    RTX 6.40/8.19. PP-at-200k still impossible (as pre-reboot, separate lane).
- 01:4x Correctness gates on the MTP-stacked 200k lane:
  - E200K (ncmoe 31, no mmproj): 3/3 DETERMINISTIC, all == 9d4badea (clean
    reference) -> adaptive MTP greedy-equivalence PROVEN at 200k. Acceptance
    0.846, mean len 3.44, TG 14.5 t/s (vs 12.2 no-spec W8).
  - PP16384 lane (bench/qwen38-pp16384.sh, c20480 ncmoe 24): 24.99 t/s pp
    (17135 tokens). Prompt file was reboot-wiped; regenerated deterministically
    from README+docs (62k chars ~= 17.5k tok; 120k chars overflowed slot: 33930 tok).
- 01:4x mmproj (unsloth mmproj-F16.gguf, sha256 1f7b7f0b... VERIFIED vs LFS,
  904,004,000 bytes -> /home/srcds/ai/ai/mmproj-Qwen3.8-Flash-Next-F16.gguf):
  - -mmdev vulkan1: FITS (RTX 7.26/8.19) but B31M + E200KV arms NONDETERMINISTIC
    (3/3 unique, incl. new variants fe70e5ed/0cce7cf2). E200KR rerun (no mmproj,
    identical otherwise) clean 3/3 -> mmproj-on-GPU is an independent breaker.
  - ncmoe 29 (no mmproj, B29): ALSO nondeterministic 3/3 unique. Boundary
    geometry re-trips the E141 phase-3 cross-backend race; ncmoe 31 clean.
    GPU-fill ceiling is therefore ncmoe 29-fit but ncmoe 31-safe: fill sacrificed
    for determinism.
  - -mmdev none (CPU projector, BM31C): 3/3 DETERMINISTIC == 9d4badea. Matches
    the T4 production class (CPU mmproj). FINAL.
  - Vision functional smoke: bench/vision-test-1080p.png correctly described
    ("grid of colored squares ... diagonal split"), prompt 12.6 t/s gen 11.8 t/s.
- 01:5x PRODUCTION LAUNCHER run-qwen38f-200k.sh (llama-server, port 8010) with
  the full validated config + do-not-touch notes. VRAM at rest: rocm0 14.65,
  rocm1 16.24, RTX 6.4->(mmproj CPU) 6.4 GiB used. ncmoe 28 infeasible anywhere
  (no device with >=1.09 GiB contiguous).
- E142 verdict: config campaign COMPLETE. Deterministic 200k f16/f16 serve with
  adaptive MTP + CPU-mmproj vision. Residuals parked: (a) phase-3 root-cause hunt
  now has TWO reproducible triggers (ncmoe 29 boundary, GPU-mmproj) - next
  instrumentation lane; (b) adaptive n_max/target tuning left at defaults;
  (c) ctx-checkpoints 0 (raise only after a fit+det re-gate).

## E143 - Max-speed PP gate (NO-GO)

- User acceptance gate: improve PP by at least 10x over E142's 24.99 t/s
  (target >=250 t/s); otherwise stop before TG work.
- Corrected an invalid preliminary sweep: target-only fit probes omitted the MTP
  and mmproj sidecars. Full-stack 200k/F16/F16 probes with adaptive MTP, CPU
  mmproj, ncmoe 29, ts 66,10,24 and b16384:
  - ub256 FITS: ROCm0/ROCm1/RTX = 16.80/16.70/6.77 GB used.
  - ub384 load-only FITS but leaves only ~51 MB on ROCm1.
  - ub512 FAILS graph-reserve: ROCm1 cannot allocate 270.01 MiB.
- Measured 2684-token PP arms after one discarded warmup:
  - ub128: 20.84 t/s (128,796 ms).
  - ub256: 27.43 t/s (97,864 ms), +31.6% vs ub128.
  - ub384 PP-only clean retry: exit 134 after 86 s, ROCm OOM during real graph.
    The load-only fit was a false positive because it did not execute the graph.
- E143 verdict: **NO-GO**. Maximum runnable candidate is ub256 at 27.43 t/s,
  only 1.32x rather than the required 10x. The remaining ~9x cannot come from
  batch/ubatch/thread/spec tuning. Blocking physics: 94.5 GB model on 62 GB RAM
  + 40 GB VRAM, 38.4 GB SSD-backed lazy PLE table, and 29-31 CPU MoE expert sets.
- Unsloth alternatives do not solve capacity: UD-Q4_K_XL totals ~111.3 GB;
  UD-IQ4_XS totals ~93.7 GB (same class as current 94.5 GB file).
- Per user gate, no further TG/adaptive-depth tuning was performed. Temporary
  run-qwen38f-200k-fast.sh removed; deterministic E142 launcher remains the only
  production artifact. Reproducer retained as bench/qwen38-speed-arm.sh (PP-only).

## E144 - Context-subordinate PP search + upstream #28136 review

- User made context secondary to sensible PP. Best-case search started at c4096;
  F16/F16 target+draft KV, adaptive MTP and CPU mmproj remained mandatory.
- Pipeline-parallel is structurally unusable for qwen4exp in this fork:
  - c4096, b16384/ub128, ncmoe29: graph reserve requested 114,536.61 MiB on ROCm0.
  - b512/ub128, ncmoe31: graph reserve requested 122,236.62 MiB.
  - Target-only control requested 122,221.75 MiB -> MTP/mmproj exonerated.
  - b64/ub16 is the only fitting point; full stack at ncmoe31/ts70,13,17 uses
    ROCm0/ROCm1/RTX 8.76/12.98/6.75 GB but measures only 17.53 t/s PP.
  - b128/ub32 immediately returns to a 122,169.34 MiB reserve and OOM.
- Active-expert clarification: 512 expert weights are packed as whole per-layer
  3D tensors; llama.cpp cannot place only the 10 routed + shared expert slices.
  At ub128, 1,280 routed selections span an expected ~470/512 unique experts,
  so prompt batches are not sparse enough to make per-expert streaming a free win.
- Non-pipeline low-context full stack (ncmoe24, ts70,13,17, b16384) fits ub512,
  ub768 and ub1024. Highest point, c4096/ub1024, measures **51.23 t/s** over
  2,684 tokens (52,386.76 ms): 2.46x ub128 (20.84), but 4.88x below the
  user-required 250 t/s gate.
- Upstream PR #28136 reviewed at head c6a9e5c9 (open, not mergeable as fetched):
  adds `--lazy-mode on-direct`, sorted/deduplicated parallel pread() gathers for
  qwen4exp/Gemma4 PLE rows. It directly matches our real-input mmap/iowait
  pathology. Author reports 300 -> 750-800 t/s on GB10 (2-3x). Applying the same
  factor to our 51.23 t/s gives ~100-154 t/s, still below 250 but materially more
  usable. No port applied: user asked to inspect the PR, not authorize source changes.
- E144 verdict: existing configuration ceiling is 51.23 t/s at c4096/ub1024;
  pipeline parallel is worse. #28136 is the only evidence-backed next source lane.

## E145 - Port upstream PR #28136 (`--lazy-mode on-direct`)

- User authorized port after E144. PR fetched as `pr/28136`: base 67a17c17c,
  head c6a9e5c9a, 2 commits, 12 files, 346+/27-. Upstream status remains open;
  GitHub reported dirty/non-mergeable against current master.
- External evidence refresh:
  - Linux RX 9070 XT: PLE-local cold staging 18.01 s mmap -> 2.39 s direct
    with 32 workers; ~208k major faults -> near zero.
  - Low-RAM 8-GPU Linux/MTP lane: +37% cold, +25% warm PP; MTP lowers the
    absolute baseline to ~240 t/s but relative gain holds.
  - Strix Halo: +20-32%; cold low-memory Windows rerun: +91-133%.
  This 62 GiB RAM / 94.5 GB model / SATA SSD machine is the intended pressure regime.
- Port application: 10/12 files applied cleanly. Manual integrations:
  `src/models/qwen4exp.cpp` (local #28243 MTP + image-token PLE path) and
  `tools/llama-bench/llama-bench.cpp` (fork CLI drift). qwen4exp delta remains
  exactly 40+/10-, matching the PR. `git diff --check` passes.
- clangd diagnostics are non-authoritative in this checkout: compile database
  lacks project include roots and reports missing llama-model.h/ggml.h cascades,
  same known E141 condition. Isolated CMake build is the real gate.
- Isolated build gate PASSED: `llama-cli`, `llama-server`, `llama-bench`,
  `test-speculative-adaptive`, and `test-llama-archs` built. Focused ctest: 2/2
  passed in 52.50 s. `llama-cli --help` exposes `on-direct`.
- 16:47-16:56 correctness gate, full c4096 stack (ncmoe24, ts70,13,17,
  threads 8/8, poll 0/0, adaptive MTP on ROCm1, CPU mmproj): discarded mmap
  warmup produced reference SHA `9d4badea6aa191d73901adfa3d9414a2829e9c6e61815e0f80dc7706c3b612e9`.
  Kept A/B/A/B/A/B hashes were:
  mmap = `9d4badea`, `48ce5806`, `47f86323`; direct = `9d4badea`,
  `47f86323`, `9d4badea`. All processes exited 0. Correctness gate FAILED.
  Two variants occur in both modes, so this is not evidence of a direct-only
  reader race; PP performance claims remain blocked pending isolation.
- Nondeterminism hypotheses:
  H1 direct-read race: direct should produce unique corruption while mmap stays
  stable; current shared variants weaken H1.
  H2 ncmoe24/ts70,13,17 crosses the known qwen4exp backend-race boundary;
  target-only repetitions distinguish it.
  H3 adaptive MTP or draft placement on ROCm1 changes accepted-token paths;
  removing the sidecar distinguishes it.
  H4 HIP graphs race at this low-context graph shape; `HIP_GRAPH=0` distinguishes it.
  Debug artifacts: `/tmp/opencode/nondet/E145-eq-*` and
  `/tmp/opencode/E145-eq-*.driver.log`; remove after E145 is finalized.
- Pre-port control on `build-qwen38-mtp`, otherwise identical full-stack mmap
  command: exits 0/0/0; hashes `9d4badea`, `6adaca63`, `9d4badea`.
  This reproduces the flip without any PR #28136 code and refutes a port-introduced
  full-stack regression.
- No-spec isolation on the port build, with both known-dangerous kernels explicitly
  disabled (`GGML_CUDA_GDN_CHUNK=0`, `GGML_CUDA_Q8_1_CACHE=0`): direct was clean
  3/3 at `9d4badea`; mmap was clean twice then produced `48ce5806`. All exits 0.
  Direct mode has not produced a unique/wrong variant, but noisy mmap cannot serve
  as a strict equivalence baseline. Next gate uses the proven one-thread race kill
  switch while restoring adaptive MTP + CPU mmproj.
- One-thread full-stack gate (adaptive MTP + CPU mmproj, explicit GDN/q8_1
  disable) also remained intermittent: mmap = clean 3/3; direct = `1f3f7096`,
  `9d4badea`, `9d4badea`; all exits 0. `1f3f7096` is a known pre-port E141
  coherent variant. Threads therefore do not fully suppress the mixed HIP/Vulkan
  speculative flip in this geometry; this is not a direct-only corruption signature.
- Next equivalence control: target-only ROCm-only lane proven deterministic in E141
  (`--device rocm0,rocm1 -ts 70,30 -ncmoe 32`), adaptive MTP removed, CPU mmproj
  retained. Only `-lzm on` versus `-lzm on-direct` changes.
- ROCm-only/no-spec gate also remained flaky over four pairs. mmap hashes:
  `47f86323`, `9d4badea`, `9d4badea`, `0cce7cf2`; direct hashes:
  `9d4badea`, `9d4badea`, `9d4badea`, `47f86323`. These are all known
  pre-port coherent variants; neither reader produced a new signature. A strict
  generated-text equivalence verdict is still blocked by baseline instability.
- Next control runs the same ROCm-only/no-spec mmap lane on untouched
  `build-qwen38-mtp`; a flip there excludes PR-wide qwen4exp control-flow changes.
- Untouched pre-port ROCm-only/no-spec control (`build-qwen38-mtp`) confirms
  the environment/runtime baseline is currently flaky: six hashes were
  `9d4badea`, `9d4badea`, `0fed3225`, `6adaca63`, `0cce7cf2`, `9d4badea`.
  Thus PR #28136 is exonerated as the source of the generation variants.
- Correctness conclusion: strict text-hash A/B is BLOCKED by pre-existing target
  nondeterminism even after MTP removal, Vulkan removal, threads=1, poll=0, and
  explicit GDN/q8_1-cache disable. Direct produced no novel variant; every direct
  hash belongs to the pre-port variant family. Per the user priority, proceed only
  to an exploratory PP A/B against the >=250 t/s gate; do not claim a correctness-
  backed speedup or test TG unless a separate deterministic reader gate is built.
- Exploratory PP A/B used the exact E144 best lane on one port binary:
  c4096, ncmoe24, ts70,13,17, b16384/ub1024, pipeline off, F16/F16 KV,
  adaptive MTP on ROCm1, CPU mmproj, 2,684-token prompt, n=0. Both known-dangerous
  kernels were explicitly disabled. One warmup per mode was discarded.
  - warmup: mmap 34.32 t/s (78,204.64 ms), direct 63.86 t/s (42,029.50 ms).
  - kept mmap: 54.44, 64.98, 65.20 t/s; mean 61.54, median 64.98.
  - kept direct: 75.32, 76.13, 76.18 t/s; mean 75.88, median 76.13.
  - median uplift = 17.2%; mean uplift = 23.3%; direct maximum = 76.18 t/s.
  - all six exits 0; all n=0 output files hash `b3ba81c1...` (prompt transcript
    only, not a generated-output correctness result).
- User PP gate verdict: **NO-GO**. 76.18 t/s is only 3.05x E142's 24.99 t/s,
  not the required 10x/250 t/s, and remains 3.28x below 250. No TG, adaptive-depth,
  or production-launcher tuning will be performed. The current 200k launcher stays
  unchanged. The port itself shows a repeatable warm PP improvement but cannot be
  described as correctness-backed because the untouched pre-port runtime fails the
  generation-hash control.
- Adversarial review: all 12 source files inspected; source delta totals the
  upstream PR's 346+/27-, and `src/llama-lazy-reader.h` SHA-256
  `ec9048b5caf932923faf557f3f35980e180315f3d194636fb3fa561821c69b93`
  is byte-identical to `pr/28136`. No local merge defect found. Generic LSP is
  bound to the outer workspace; project Serena/clangd lacks include roots and
  reports known false missing `ggml.h`/`llama.h` cascades. tldr reports only
  unrelated pre-existing Python issues.
- Final authoritative gate: all five requested targets rebuilt successfully;
  focused ctest passed 2/2 in 49.07 s; `git diff --check` passed. UI asset fetch
  for build tag b11066 returned HTTP error, then the existing latest fallback
  downloaded, verified, extracted, and embedded 70 assets successfully.
- Debug/perf artifacts: `/tmp/opencode/nondet/E145-*`,
  `/tmp/opencode/qwen38-speed/E145-*`, and matching `/tmp/opencode/*.driver.log`;
  journal numbers are the durable record.

## E146 - Upstream PR scan: anything left for our PP lane?

- User asked whether upstream llama.cpp PRs could push beyond the 76 t/s ceiling.
  Live GitHub API search 2026-09-11 (open PRs, `moe`/`prefill`/`lazy`/`rocm` in
  title, ~70 candidates inspected). Verdicts:
  - #28414 `--prefetch-experts-slots` (H2D lookahead prefetch of host-resident
    experts): mechanism targets GPU-resident compute with host pages. Our ncmoe
    lane computes host experts ON CPU; at ub1024 ~470/512 experts/layer are used,
    so per-split upload would move ~90% of ~40 GB host experts over PCIe per
    ubatch. REJECT (wrong regime).
  - #27861 GPU-resident LRU expert cache: decode-only (`n_tokens == 1`).
    REJECT for PP (TG-only, and TG gate not met).
  - #28702 fused dense Q4_K Gate/Up+SwiGLU prefill: NVIDIA Ampere+; body says
    HIP keeps existing path. REJECT (gfx906 HIP).
  - #26001 GDN chunked prefill: we already carry a fork port; it is our bug #2
    nondeterminism source, default-off. REJECT (have it; unsafe here).
  - #28313 ROCm TOP_K kernel fixes: MoE routing topk is ~µs-scale x ~144 calls
    vs 35 s PP; immaterial. REJECT.
  - #27825 HIP AllReduce: `-sm tensor` only; we run `-sm layer`. REJECT.
  - #27637 OpenCL MoE batch-size kernel choice: OpenCL backend. REJECT.
  - #26704 SM120 CUTLASS W4A4: NVIDIA SM120. REJECT.
  - #28267 lazy q8_0 KV: violates F16/F16 mandate. REJECT.
- Conclusion: NO applicable upstream PR attacks our actual remaining bottlenecks
  (CPU-computed expert layers at 8 threads + 38.4 GB PLE on SATA SSD). The 2-3x
  seen on GB10 with #28136 did not transfer because our PP time is not dominated
  by PLE faults alone. Remaining levers are local: ncmoe raise at c4096/ub1024
  (VRAM permitting; determinism not required for the PP-only lane), or hardware
  (>=128 GB RAM to page-cache the PLE table, NVMe). No port scheduled.
- Merged-since-sync verification (2026-09-11, skeptical re-check of the scan's
  "open AND merged" scope): atom-feed review of all master merges
  2026-09-10T13:44 -> 09-11T13:50 (Metal kernels, RDNA4 FA gfx1201, cmake PCH,
  ci/tests — nothing for us) plus gap-window keyword searches
  (`moe`, `vulkan` in:title, merged 09-09..09-10):
  - #28422 vulkan topk_moe prefill fusion (merged 09-10): only lane-adjacent
    item; +2-3% pp512 on pure-Vulkan RTX 5090. Our vulkan1 holds ~13% of layers
    -> sub-1% expected. NOT ported.
  - #28457 vulkan small-M qwen mul_mat (m=1/m=32 decode buckets): our PP runs
    M=1024. REJECT.
  - #28618 vulkan idle CPU-writes for cpy_tensor_async: decode fence overhead
    (~50 us/sync); PP impact negligible. REJECT for PP.
  - #28552 RDNA3 MoE MMQ N-tiles: already in our fork (24f539c35) — confirms
    the 09-09 sync captured that day's merges.
  - #28476 SYCL, #28341 PowerVR, #28632 granite metadata: wrong backend/cosmetic.
  Verdict unchanged: firm ceiling at ~76 t/s PP from upstream's perspective;
  next lever is the local ncmoe sweep, not a port.

## E147 - Absolute-max PP hunt (user reopened the 76 t/s ceiling)

- User asked for the maximum possible PP speed and questioned whether
  lazy/PLE or expert placement blocks us; explicitly requested a
  `--lazy-mode off` control (never run before: all E144-E146 lanes used
  on/on-direct).
- Lane: build-qwen38-direct, c4096, F16/F16 KV, adaptive MTP, CPU mmproj,
  threads 16/16, GDN/q8_1-cache env-disabled unless stated, 2,684-token prompt.
- ncmoe sweep at ts70,13,17 ub1024 (cold 1-rep probes): ncmoe27 65.34,
  ncmoe29 58.88, ncmoe31 60.82 t/s. ncmoe27 warm+3-kept was UNSTABLE:
  16.21/65.82/49.55 t/s (165 s PP outlier) -> VRAM-pressure thrash, not a
  speed lever. ncmoe knee stays at 24 in this geometry.
- ts rebalance: ts44,12,44 crashed at load (ROCm1 contiguous tail request
  21,957.64 MiB > 16.2 GiB). ts65,13,22 + draft-on-CPU crashed on a Vulkan
  608 MB alloc (layer-range shift starved vulkan compute buffer). Heavy tails
  must stay off rocm1; structural.
- **ubatch is the real lever**: ub2048 (2 ubatches) kept 91.82/93.15/90.10
  t/s, median 91.82 (+20% over the 76.13 ub1024 ceiling). First-ever run
  91.18; flat across fresh processes -> NO cold-to-warm ramp: storage/PLE
  page-fault churn is NOT the wall under on-direct.
- ub1536 (2 ubatches, 1536+1148): 89.80 t/s. ub4096 (1 ubatch) FAILS:
  Vulkan1 cannot allocate the 1,389,674,512-byte compute buffer (8 GiB card).
  ub2048 is the VRAM-ceiling ubatch for the full stack.
- ub1024->ub2048 bought 6.4 s for one fewer ubatch (35.2 -> 28.8 s).
  Linear fit implies ~22.4 s single-ubatch floor (~120 t/s) IF the cost is
  fixed-per-ubatch (graph re-plan/launch) rather than per-token efficiency.
  ub512 (6 ubatches) probe pending to separate the two models.
- No-change knobs at ub2048: ncmoe25 = 91.96 t/s and GDN-chunk-ON
  (GGML_CUDA_GDN_CHUNK=1) = 91.96 t/s, both within noise of ncmoe24.
  (Suspiciously identical to <1 ms; treated as noise-level equal.)
- Next diagnostics (in order): (1) verify ffn_*_exps tensors for layers 0-23
  are truly in GPU buffers (loader log placement grep; pattern TBD - first
  grep 'model buffer size' found nothing), (2) user-requested `-lzm off`
  control at the ub2048 lane - expect load thrash since 56 GB weights +
  38.4 GB PLE > 62 GB RAM, which itself proves lazy is mandatory here,
  (3) ub512 point for the n_ub curve.
- Artifacts: /tmp/opencode/qwen38-speed/E146-* and /tmp/opencode/E146-*.driver.log.

### E147.1 - what was blocking us (user-challenged 91.8 ceiling)

- User challenged the ceiling and requested a `--lazy-mode off` control + placement
  proof. Ran both.
- `-lzm off` control at the ub2048 full-stack lane: **34.43 t/s**, 2.7x WORSE
  than on-direct (91.8). The 38.4 GB PLE cannot be resident in 62 GB RAM;
  the off-control thrashes. The 216->406 t/s lazy-regression reports come from
  big-RAM machines - here on-direct is mandatory. Lazy/PLE is EXONERATED.
- Placement proof: kernel-truth sysfs (/sys/class/drm/card{2,3}/device/) shows
  VRAM 15.2 G (card2/ROCm0) and 11.7 G (card3/ROCm1) - weights ARE on the
  GPUs. But gpu_busy_percent = 0 for ~90% of the PP window (two 1 s blips at
  100%), RTX 6-11%, CPU load 4/16. Nothing saturates -> the wall is
  serialization between tiny compute bursts (per-split host sync), not
  bandwidth or placement fraud.
- Ubatch curve (full stack, ncmoe24, ts70,13,17, on-direct): T = a + b*n_ub
  fits ALL points: ub512 (6 ub) 54.65 s, ub1024 (3 ub) 35.2 s, ub1536 (2 ub)
  29.9 s, ub2048 (2 ub) 28.8-29.2 s -> a ~= 15.8 s token-work, b ~= 6.5 s
  FIXED per-ubatch cost (graph rebuild; 'graphs reused = 0' every run).
  Single-ubatch floor ~= 22.3 s -> ~120 t/s.
- MTP dropped for PP work (user decision, E147): harness gained FULL_STACK=0
  (drops draft+mmproj sidecars; draft bundle wastes 2647 MiB and never runs
  at -n 0) and PROMPT_BYTES knobs. Committed 75327a710.
- No-MTP results (c4096, 2684-tok prompt, on-direct):
  - ub2048 3dev: 91.38 t/s warm (first-ever 54.85 = known cold-Vulkan-pipeline
    artifact).
  - ub2688 3dev SINGLE ubatch: 117.05 / 117.17 t/s (22.9 s; model predicted
    22.3 s). Clean baseline.
  - ub2688 2dev (rocm0,rocm1 ts75,25): **125.73 t/s** (21.35 s).
  - A-resample of the 3dev lane hit the cold-thrash outlier (22.94 t/s,
    116.9 s) - same class as ncmoe27 instability; discarded as invalid.
- 17k-token lane (c20480, ub4096, PROMPT_BYTES=62000 -> 17,135 tokens, MTP
  off, on-direct):
  - 3dev ts76,6,18: 153.00 t/s (ub4096/ub8192 at ts70,13,17 fail on Vulkan
    ~1 GiB compute allocs).
  - **2dev ts75,25: 180.87 t/s.**
- SETTLED (user framing): Vulkan boundary costs ~8% shallow / ~18% at 17k.
  Real but secondary; it amplifies the per-ubatch/rebuild problem without
  explaining the gap to 300+. 2xVII lane is the continuation surface; RTX
  returns only after the 2-dev lane is healthy.
- Dashboard: bench/ppwatch.sh (tmux session 'ppwatch') shows live GPU busy,
  VRAM, load, arm history. Committed ff5969ff3.
- Next: 2-dev ncmoe sweep (24/27/29/31) at the 17k lane with per-card VRAM
  capture; discriminator: PP scaling => CPU experts dominant; flat PP with
  bursty GPU => per-ubatch rebuild/scheduler serialization is the target.

### E147.2 - 2-dev ncmoe sweep + ubatch decomposition (17k lane)

- Lane: rocm0,rocm1 ts75,25, c20480, ub4096, MTP off, on-direct, 17,135 tok.
- Single probes: ncmoe24 168.90, ncmoe27 158.23, ncmoe29 44.16 (thrash;
  card3 pinned 15.97/16 GiB), ncmoe31 181.31 t/s. ncmoe31 packs card2 DOWN
  to 12.3 GiB (vs 15.2 at ncmoe24) - placement bin-packing quirk.
- Interleaved 3x reps: ncmoe24 = 193.61/193.50/186.63 (median 193.5),
  ncmoe31 = 188.12/187.84/186.05 (median 187.8). NEUTRAL-to-negative.
  **CPU experts are NOT the limiter.** Lane also warms with repetition
  (169 -> 193 across passes; keep interleaved reps for any ranking).
- **New lane best: 193.6 t/s** (2dev, no MTP, ncmoe24, ub4096, 17k tok).
- Ubatch decomposition on this lane: ub2048 (9 ub) = 121.14 s vs ub4096
  (5 ub) = 88.5 s -> **b ~= 8.2 s FIXED per ubatch**, a ~= 47.5 s token work
  -> asymptotic ceiling ~360 t/s if the per-ubatch cost is eliminated.
  At 5 ubatches the overhead is 41 s = 46% of the 88.5 s runtime.
- GPU duty during sweep (kernel-truth sysfs, 2 s samples): card2 avg 33%,
  card3 avg 15%, both max 100%, card2 >=90% in 115/356 samples, card3 52/356
  -> bursty compute, idle gaps between bursts: scheduler serialization
  confirmed as the primary target (user's discriminator).
- ub8192 (3 ub, predicted ~237 t/s) OOMs: 3712.83 MiB alloc on ROCm1
  (15.97/16 GiB full). No ts shift frees both cards; ubatch lever exhausted
  at ub4096 on this VRAM.
- Next work items (in order):
  1. Hunt why `graphs reused = 0` on every PP run - 4x4096+783 ubatches
     contain 4 equal-size graphs that should be reusable; a fork-level
     invalidation is a concrete bug candidate with a direct counter metric.
  2. perf-record the host during PP to split graph rebuild/alloc time from
     execution (user's instrumentation step).
  3. If reuse is fixed: projected 5-ub 88.5 s -> ~50-60 s (~290-340 t/s).
- Artifacts: /tmp/opencode/qwen38-speed/E147-2dev-*, /tmp/opencode/psi/E147-2dev-*
  (vram/busy traces), /tmp/opencode/E147-2dev-sweep2.txt.

### E147.3 - per-ubatch phase instrumentation (user-prescribed)

- src/llama-context.cpp `process_ubatch` now times phases under
  LLAMA_PP_PHASE_TIMING=1 (raw fprintf, bypasses the fork's log filter that
  eats src/ LLAMA_LOG_INFO even at -lv 3): mctx-apply, reuse check,
  reset/build/alloc (rebuild path), set_inputs, compute (now INCLUDING an
  explicit ggml_backend_sched_synchronize so async tails are billed to
  compute, per user's ownership rule), plus n_tokens/nodes/splits/copies.
  Zero-cost when off (PP unchanged: 181-185 t/s with timing on).
- 17k lane tables (2dev ts75,25 ub4096, MTP off, on-direct):
  - ncmoe24 steady 4096-tok ubatches: build 1.1-1.4 ms, alloc ~2.5 ms
    (FLAT after the sync fix; the earlier 2.4 ms -> 2.4 s growth was
    deferred async work billed to alloc), setin 0.58-0.64 s (PLE pread
    gather, ~0.15 ms/tok), compute 18.6-19.8 s, total ~19.4 s.
  - ncmoe31 (7 fewer CPU-expert layers): compute 19.5 s median (WORSE),
    splits 96 (up from 90), PP 178.9 vs 185.0. **CPU experts formally
    exonerated as the compute wall** (user's matrix cell 4).
  - 2-token preamble ubatch costs 6.9-8.6 s (cold first-launch; --no-warmup
    artifact, excluded from steady analysis).
  - Tail ubatch (751 tok): 13.5 ms/tok vs 4.6 at 4096 tok. Two-point solve:
    compute ~= n_tok x 2.56 ms + 90 splits x ~92 ms -> **~92 ms fixed cost
    PER SPLIT** (upper bound; deepest-KV attention inflates the tail c).
    90 splits x 92 ms ~= 8.3 s/ubatch of pure handshake overhead.
- Remaining suspects inside compute: per-split launch/copy/event handshake
  (gfx906 HIP + SDMA), or per-op CPU-pool sync in the CPU-expert splits.
- Next: per-split timing inside ggml_backend_sched_compute_splits
  (ggml/src/ggml-backend.cpp): split id, backend, input-copy time,
  subgraph compute time. Commit instrumentation before proceeding.

### E147.4 - ROOT CAUSE FOUND: MoE expert H2D staging dominates PP

- Per-split instrumentation (GGML_SCHED_SPLIT_TIMING=1, committed 929627282)
  in ggml_backend_sched_compute_splits. 17k lane, ncmoe24, default op-offload:
  - ROCm0: 470 splits, inputs=68.1 s, graph=1.4 s (avg 145 ms/split)
  - ROCm1:   6 splits, inputs=10.1 s, graph=1.3 s
  - CPU:    35 splits, graph=5.3 s (real CPU compute)
  **~78 s of the ~90 s PP is the split INPUT loop, not kernels.**
- Mechanism: with op-offload, host-resident expert weights stay GPU-executed;
  compute_splits' copy_experts branch H2D-stages every USED expert slice per
  ubatch. At ub4096 ~all 512 experts are hot -> ~full expert tensors streamed
  per layer per ubatch (~150+ GB over PCIe per 17k prompt) at only ~2-3 GB/s
  effective (pageable host memory; ggml-hip has NO pinned path - grep for
  HostRegister/pinned in ggml-cuda/hip: only cudaMallocHost via
  ggml_backend_cuda_host_buffer_type, which mmap-backed -ot CPU tensors
  cannot use). GPU kernels are NOT the wall (2.7 s total).
- `--no-op-offload` control: 103.55 t/s - experts execute on CPU instead:
  CPU graph=111.7 s. The lane is stuck between PCIe staging (78 s) and CPU
  execution (112 s); both beat the other only marginally.
- `-lm none` (fork's own loader advice for CPU tensor overrides): 188.49 t/s,
  inputs 64.2 s - NO pinned transformation; override-placed CPU weights do
  not take the CUDA_Host buffer type.
- `-mg 1` role flip: 190.07 t/s, asymmetry unchanged (ROCm0 67.7 s vs
  ROCm1 9.8 s) - the planner funnels nearly all 24 layers' staging through
  ROCm0's link; ts25,75 flip OOMs on the 23.2 GiB contiguous ROCm1 tail
  (structural). SDMA=0 probe was INVALID (script hardcodes HSA_ENABLE_SDMA=1
  in its env; honor-override fix pending).
- Lane best stands at ~190-193.6 t/s. Fix directions (source level):
  1. Pin host-resident expert weights (route -ot/ncmoe CPU WEIGHTS buffers
     through the CUDA/HIP host buffer type so H2D runs ~8-12 GB/s).
  2. Pipeline expert staging with compute (overlap H2D of layer N+1 with
     compute of N) instead of serialize-before-use.
  3. Revisit the INPUT-flag event syncs that chop the GPU pipeline per split.

### E147.5 - pinning thesis tested and killed; link physics established

- Implemented + committed-safe (see E147.6 commit): LLAMA_EXPS_PINNED=1 keeps
  the host buft for op-offloaded expert weights past the mmap demote;
  LLAMA_EXPS_BUFT_DEBUG=1 prints per-tensor buft (PPBUFT lines) and a LOUD
  pinned-alloc-failure line in ggml_cuda_host_malloc.
- Diagnostic: -lm none already lands all 144 expert tensors in ROCm_Host
  (pinned, zero failures) - and staging was STILL 64.2 s. Pinned ~= pageable.
- bench/hipbw.cu microbenchmark (both cards, 2 GiB H2D):
  - ROCm0 (x4 slot): pageable 3.23 GB/s, pinned 3.40, serialized 3 MiB
    storm 3.25 -> the LINK is the cap, memory type irrelevant.
  - ROCm1 (x1 slot): 0.77 / 0.81 / 0.79 GB/s.
  User confirmed wiring: one card x4, one x1. Total usable H2D pipe ~= 3.3.
- NCMOE SEMANTICS CORRECTION (prior analysis inverted): `-ncmoe N` keeps the
  FIRST N layers' experts host-resident. ncmoe24 stages layers 0-23; higher
  ncmoe = MORE staging (nc31 splits: ROCm0 inputs 74.8 s, PP 175.9 - worse).
  All lane numbers now consistent.
- Physics of the lane: 24 host layers x ~1.29 GB x ~0.92 hot x 5-6 ubatches
  ~= 180 GB per 17k prompt over the 3.3 GB/s x4 pipe ~= 56 s minimum staging
  (measured 64-68 incl. waits); GPU kernels ~2.7 s. The lane already sits
  within ~15% of its physical ceiling (~285 t/s) for this VRAM/link geometry.
- --warmup probe: warmup passes are BILLED to slot prompt-eval in this server
  lane (two 2-token preambles, 9.85 s + 0.55 s with reused=1 - graph reuse
  DOES engage on identical repeats); measured PP got worse (179.6). Not a
  benchmark lever; only helps real serving.
- x1/x4 accommodation verdict: the scheduler already does the right thing -
  all expert staging funnels through the x4 card, ROCm1 stays a passive
  weight-holder (light tail, 1 boundary/ubatch, GPU busy ~15%). Explicit
  rules: never route expert compute/staging to the x1 card (0.8 GB/s trap:
  staging its 12 tail layers would cost ~19 s/ubatch); keep the heavy head on
  the x4 card; accept the one-time slow load. Dual-pipe staging would add
  only +24% pipe (0.8/3.3) and requires host-expert layers on ROCm1 - the
  heavy head cannot fit it (contiguous-tail OOM at ts<=55, 21.9 GiB fail).
  VRAM re-placement is zero-sum: both cards are full; any expert moved into
  VRAM displaces another to host (+staging).
- Revised realistic envelope on this rig: ~200-290 t/s depending on protocol;
  500+ requires either 2x VRAM (all experts resident) or a faster link.

### E147.6 - bounded scheduler-overhead cleanup pass (FAILED, lane frozen)

- Hypothesis: the ~10-12 s gap between pure-bytes staging time (180 GB /
  3.3 GB/s ~= 56 s) and measured inputs (64-68 s) was sync overhead
  (per-layer ids round-trip device sync + bitset scan + ~40 grouped small
  copies per staged layer in compute_splits).
- Implemented GGML_MOE_FULLCOPY_TOKENS: above a token threshold, one
  contiguous full-tensor expert copy replaces the ids fetch + grouped copies.
- A/B (3 reps, 17k lane, ub4096): full-copy 172.4/163.8/169.3 (median 169.3)
  vs baseline 193.5 - WORSE. ROCm0 inputs rose 68.1 -> 75.4 s: the +8% extra
  bytes (~15 GB, +4.5 s) cost more than the ~144 eliminated syncs saved,
  proving the ids syncs were already fully pipelined behind finished routing
  kernels. **The recoverable-sync bucket does not exist**; the 56-vs-68 s gap
  is in-situ link throughput (contention with activation/KV traffic) vs the
  isolated microbench, not latency.
- Per the pre-agreed stop condition (no >=210-220 reproducibly): STOP.
  Full-copy kept as opt-in only, default disabled (threshold 2^30).
- Post-pass drift note: late-session runs (incl. a default-path verify at
  173.4 t/s, inputs 73.5 s) sit below the earlier clean cluster (183-193.6).
  Cause unverified (thermal / sustained-load state suspected); the validated
  lane best remains 193.6 t/s from the interleaved series. Flash-Next PP is
  FROZEN at: 2dev ts75,25, ncmoe24, ub4096, c20480, MTP off, on-direct,
  threads 16/16, GDN/q8_1-cache disabled.

## E148 - ubatch/placement rebalance + RTX third-pipe experiments

- Insight: staging volume = L_host x ~1.19 GB x n_ubatches -> cutting
  ubatch count via VRAM rebalance is a real lever.
- P1 (tail-offload to host, ts75,25, ub5712): 65.8 t/s CATASTROPHE - ROCm0
  inputs 235.6 s. Cross-card expert compute (tail layers on ROCm1, expert
  op on ROCm0) hits a pathological copy path. Trap confirmed.
- **P2 (offload ROCm0's own resident experts 24-35 via -ot, ts80,20,
  ub5712 = 3 ubatches): 199.50 t/s - NEW LANE BEST.** Clean geometry: every
  host-expert layer computes on its walk-owner. Reps 195.1/194.7/163.1
  (third = known late-session drift outlier).
- RTX third-pipe probes (user: RTX sits on Gen3 x16-capable, ~9.4 GB/s
  measured via vulkan staging in P3 - 3x the x4 pipe):
  - P3 (ts50,28,22, RTX owns 24-35 host-expert): 192.4 t/s. RTX staged its
    43 GB in 4.6 s BUT ROCm0's effective rate FELL 1.9 -> 1.3 GB/s.
  - P4 (ts40,38,22): OOM - Vulkan1 3.4 GiB compute reserve, 8 GB card.
  - P5 (ts47,31,22): 172.5 t/s. ROCm0 rate -> 1.04 GB/s. **Every GB/s the
    RTX pulls concurrently, ROCm0 loses more** - pageable H2D copies are
    CPU-bounce copies; two GPU streams fight for the host feed / root
    complex on this board.
  - P6 (P3 + -lm none + LLAMA_EXPS_PINNED=1): 184.2 t/s. Pinned helps ROCm0
    (60.3 s) but the Vulkan H2D path degrades with pinned weights
    (RTX 14.0 s vs 4.6 pageable).
- Verdict: the RTX cannot beat the 2-dev lane on this board despite its
  faster link - concurrency cost exceeds the pipe it adds. Single clean
  x4 stream wins.
- **FINAL FROZEN LANE (E148): 2dev (rocm0,rocm1) ts80,20, ncmoe24 +
  -ot 'blk\.(2[4-9]|3[0-5])\.ffn_(up|gate|down)_exps\.\=CPU', b20480/ub5712,
  c20480, MTP off, on-direct, threads 16/16, GDN/q8_1-cache disabled.
  Best 199.5, reproducible ~195.** (51.23 -> 199.5 = 3.9x campaign total.)
- Artifacts: /tmp/opencode/qwen38-speed/E148-*, /tmp/opencode/E148-*.driver.log.

## E149 - pp16384 definitive 3-GPU vs 2-GPU (same-session interleave)

- 1 discarded warmup, then interleaved A/B x3, cool machine, no drift.
- 3-GPU best (ts50,28,22, RTX owns 24-35 host experts, ub5712):
  197.06 / 199.36 / 198.33 -> median 198.3 t/s.
- 2-GPU frozen lane (ts80,20, same -ot, ub5712):
  201.38 / 209.83 / 208.01 -> median 208.0 t/s. NEW PEAK 209.83.
- Verdict: 2-GPU beats 3-GPU by ~5% head-to-head. RTX third-pipe no-go
  CONFIRMED under clean conditions. Prior 163-195 spread = thermal drift
  (cool session reproduces 198-210 without any config change).
- Campaign: 51.23 -> 208 t/s median (4.06x), peak 209.83.
- Artifacts: /tmp/opencode/E149-pp16384.txt, /tmp/opencode/qwen38-speed/E149-*.

## E151 - ubatch ladder sweep (user Q: max PP via ub tuning @ c20480)

- Found project skill .opencode/skills/dual-gpu-context-balancing/SKILL.md
  (context-maximizing procedure: per-device min-fit, -ts coarse + -ot fine
  moves off limiting device, -lv 4 breakdowns, one variable per trial).
  Also read run-qwen38f-200k.sh: production c204800 fits at ts66,10,24
  ncmoe31 FULL stack incl draft+mmproj -> PP lane (no sidecars) has ~2.6 GiB
  more room; max-c estimate 220-260k, ceiling ~400k (RAM/staging-capped).
- ub ladder, 2gpu ts80,20 lane, bench/logs/ub-ladder.log:
  4284 (4ub, L36): 161.91 | 5712 (3ub, L36): 199.5-209.8 BEST | 7100
  (3ub, L37): 178.30 | 8568 (2ub, L39): 194.31 | 17135 (1ub, L45):
  ~110 DEGENERATE (ROCm0 inputs 144 s = 0.37 GB/s, cold mmap pages of
  newly-hosted blk.36-44 + single-pass gather).
- ROCm0 in-situ staging RATE degrades monotonically with ub: 67.6 s (3ub
  5712) -> 72.3 (2ub) -> 78.0 (3ub-fat 7100) -> 144 (1ub). Small ub loses
  on total staging count instead.
- VERDICT: V-curve, minimum at ub5712 = exactly-balanced 3ub with smallest
  L_host VRAM allows. ub tuning is EXHAUSTED: 199.5/208 stands as the
  c20480 PP optimum for this rig.
- Logging now in bench/logs/ per user directive.

## E151b - fine ub sweep, 3gpu lane, corrections

- USER CORRECTION: 'there is no thermal drift' - thermal theory RETIRED.
  Real variance source: mmap PAGE-CACHE residency (90 GB file in 62 GB RAM)
  + swap thrash. Different -ot ranges each experiment evict the previous
  arm's expert pages -> cold SATA reads at 0.4-0.5 GB/s instead of RAM speed.
  'Healthy' fast clusters (E149 197-210) = pages hot from identical prior
  runs.
- Fine sweep on 2gpu: 5700 (4ub) 166.8 | 5712 199.5-209.8 | 5776 169.0 |
  5840 183.6 | 5968 183.5 | 6208 192.4 | 6608 57.2 CATASTROPHE. Non-
  monotonic = cache state chaos, not structure.
- 3gpu interleave: anchors 183.9/178.3/174.3 sliding; 5744 190.7; 5904
  47.9 CLIFF. Post-4min-'cooldown' reps: 179.1/185.1 (5712), 179.7/153.9
  (5744) - no convergence.
- SYSTEM STATE at end: 6.1 Gi in swap (no sudo to flush), 50 Gi buff/cache,
  405 Mi free. Catastrophe floor 18-21 t/s = swap-in + page-cache-miss both
  on the SATA bus during staging. Needs swapoff -a && swapon -a (root) or
  reboot for clean numbers.
- Findings that SURVIVE the noise: (1) ub cliff at ~5850-5900 on 3gpu =
  Vulkan1 VRAM edge (~0.5 GiB free at c20480/ub5712); ub growth tips it
  into fallback. (2) ub<5712 = 4th ubatch cliff (proven 166.8 vs 199.5).
  (3) At healthy cache state 5712 and 5744 indistinguishable; 5712 stands
  (E149 median 198.3 3gpu / 208 2gpu).
- Open: re-verify 5712 vs 5744 after swap flush; max-ctx task still
  pending (skill procedure ready).

## E153 - expert-staging phase decomposition + pinned ring (code work)

- Code archaeology: every expert H2D goes through
  ggml_backend_tensor_set_async -> ggml_backend_cuda_set_tensor_async
  (async, no sync) from PAGEABLE mmap source; per-layer ids D2H +
  ggml_backend_synchronize(split_backend) drains the pipe ~108x/run;
  buffer_set_tensor (sync variant) NOT used in this path.
- GGML_SCHED_COPY_PHASES=1 instrumentation (new): decomposes expert
  copies into ids_sync vs issue. FINDINGS (3gpu lane):
  cold ub1: issue 25.2s @ 1.36 GB/s; warm ub2/3: 10.9/10.3s @
  3.25/3.44 GB/s. Pageable issue ALREADY saturates the x4 pipe on
  warm pages - the old '1.9-2.4 in-situ' was cold-blend + misattribution.
- GGML_EXPS_RING=1 (new): 3-slot pinned host ring (430-563 MiB/slot,
  ~1.4 GiB pinned), memcpy mmap->slot + async H2D + event-guarded reuse.
  Same bytes/transactions. Issue rate 3.7/7.65/8.61 GB/s (issue = CPU
  only; DMA queues behind). Cold ub1 issue 25.2->10.0 s.
- A/B interleaved x3, 3gpu ts50,28,22 (bench/logs/ring-ab.log):
  off median 188.1 vs on median 191.1 (+1.6%, within variance).
  sha 9c566ecdb48b 6/6 IDENTICAL - output equivalence holds.
- VERDICT: ring is a cold-start optimization (first-ubatch 2.5x),
  steady-state no-op. Kept env-gated default OFF. Production cold
  first-pass should set GGML_EXPS_RING=1.
- Remaining wall structure (warm ~86-90 s): staging issue ~20-25 s,
  ids_sync ~4.4 s, graph+other backends+PLE ~55 s. Next levers if
  pursued: event-scoped ids wait (~5%), decompose the ~55 s non-staging
  wall via SPLIT lines.
- Artifacts: bench/logs/ring-ab.log, /tmp/opencode/qwen38-speed/E153-*.

## E153b - event-scoped ids wait: REJECTED (correctness)

- Implemented GGML_EXPS_IDS_EVENT=1 (event record+wait instead of
  full ggml_backend_synchronize after ids D2H). A/B on 3gpu lane:
  BOTH 'on' runs failed to print PP timing AND produced different
  output sha (130d15d7ccfd vs 9c566ecd 6/6 baseline) = correctness
  failure per skill equivalence gate. 'off' arm second rep also
  degraded (93.5 t/s, cache chaos from broken runs).
- Code REVERTED to HEAD 6f5194cde (git checkout ggml-backend.cpp).
  Binary needs rebuild before next measurement (bin still has the
  ids-event build at 00:56).
- Wall model refined from ub-ladder data: fixed ~= 27 s (load+PLE+
  cold ub1) + 19.8 s per 5712-tok ubatch -> asymptote ~288 t/s;
  predicted pp32768 ~225, pp65536 ~250. Amortization is the only
  remaining scaling lever (user insight: rising issue rate = cache
  warmth, but bigger fills DO raise avg t/s by amortizing fixed cost).
- Next: rebuild clean binary, probe pp32768 on 3gpu ts54,24,22
  (vulkan KV headroom), PROMPT_BYTES~124000.

## E153c - ids round-trip, strike 2: REJECTED, direction CLOSED

- Root-cause hunt for strike 1: HIP graphs launch on cuda_ctx->stream()
  (not a rogue stream); real strike-1 bug = static event not keyed to
   backend (CUDA event_record asserts device match -> mid-run abort).
- Strike 2 (device-keyed event, GGML_EXPS_IDS_EVENT=1): TWO distinct
  failure modes - on1 correct sha but 42.8 t/s (4x slower), on2 fast
  but WRONG sha (abbf93600f78 = corruption). The 'wait for exactly
  this D2H' semantic is not expressible with backend-level events:
  ids-producing work sits in GPU splits OR CPU routing depending on
  layer/ubatch, and scoping wrong = race.
- FULLY REVERTED (no knob shipped). Binary rebuilt clean from 6f5194cde.
- ids round-trip drain bubbles (~30 s) remain, reachable ONLY via a
  GPU-side used-expert gather (kernel builds copy ranges on device,
  zero CPU round-trips) - real redesign, parked.
- Wall decomposition (E153-ring SPLIT lines, 191 t/s run): ROCm0
  inputs 65.0 s (DMA-pure 33.6 s + ~30 s bubbles), graph 4.7 s all
  backends, rocm1 6.6 s, vulkan 5.3 s, CPU 1.9 s, ~9 s unattributed
  gaps + PLE 2.6 s.
- Artifacts: bench/logs/ids-event2-ab.log.

## E154 - 3gpu + MTP campaign (user: 3 gpu mandatory, rebalance manually)

- -lm none: NO effect on vulkan decode OOM (VRAM wall, not page
  mode). Kept in stack per user until measured: PP neutral-to-negative,
  VRAM neutral.
- Q4 shared draft found: /home/srcds/ai/ai/mtp-Qwen3.8-Flash-Next-
  Next... mtp-Qwen3.8-Flash-Next-shared-Q4_K_M.gguf (1819 MiB vs Q8
  2657) - frees 840 MB on ROCm0 (-otd '.*=ROCm0').
- 3gpu+MTP ts ladder ALL FAIL: ts54/24/22 vulkan decode OOM 373 MB
  (CONSTANT across ts - verification graph buffer, not ts-scaled);
  ts56/22/22 ROCm0 core dump; ts55/23/22 vulkan again. Q4 draft +
  lm none do not move it. Window empty: vulkan needs what ROCm0
  can't yield.
- MANUAL REBALANCE (skill fine-grained -ot): host vulkan's 2 resident
  expert layers (36-37): -ot 'blk\.(2[4-9]|3[0-7])' -> loads, PP 128.3
  t/s, TG 4.50 t/s, BUT then ROCm OOM at draft load fixed by ALSO
  hosting 38-39 (-ot 24-39, L40): FULL SUCCESS - PP 128.34, TG 4.50,
  acceptance 0.50 len 2.50.
- TG killers: ids round-trip x40 host layers PER TOKEN (100+ ms)
  + ~50 split syncs. Deeper draft + threads 8: --spec-draft-n-max 16
  --threads 8 -> TG 9.26 t/s, acceptance 0.546 len 2.65 (2x win).
- WORKING 3gpu+MTP lane (c20480): ts50,28,22, ncmoe24, -ot 24-39,
  Q4 draft, threads 8/8, draft-n-max 16. PP cost of TG-compat: 199 ->
  128 t/s (L40 staging + MTP stack + threads 8).

## E155 - c32768 attempt: ROCm OOM at load (2.23 s) - unresolved

## E156 - 200k ctx + MTP probe: ROCm0 compute 15.8 GiB reserve OOM

- CAUSE: carried PP lane ub5712 into serve geometry - compute scales
  with ubatch; production uses b512/ub128. Retry with b/ub ~2048.
- Harness now supports PROMPT_SOURCE env (pp32k source built:
  /tmp/opencode/pp32k-prompt.txt, 124000 B ~= 34k tok).

## E156/E157 - high-ctx 3gpu+Q4-MTP fit war (WALLS FULLY MAPPED)

- ctx x ub blob on Vulkan1: ~16.5-17 B per (tok x ub) - vulkan caps
  (ctx*ub) <= ~360M. Attested: 10.06 GB @100k/ub5712, 6.88 @200k/ub2048,
  10.97 @100k as first device. ts-insensitive, b-insensitive, output-pin
  insensitive, device-order insensitive.
- Decomposition (E156-48k -lv4, ub5712): vulkan compute 5462 MiB =
  full-vocab logits intermediate [5712 x 150936 x f32] = 3328 MiB
  (build_lora_mm(model.output) -> t_logits FULL, no get_rows in
  qwen3moe.cpp builder; qwen35.cpp DOES get_rows) + ~2100 MiB other.
  Plus ~25 KB/tok ctx-scaled scratch on vulkan (unattributed).
- ROCm0 ctx-scaled blob: 16.9 GB @200k (82 KiB/tok) - includes KV share
  + logits + spec bookkeeping.
- Device-order law: 2nd device gets ~10 GB draft-context blob, LAST
  device gets resident expert tail -> both must be 16 GB cards -> vulkan
  must be first (head walk, non-expert only).
- RAM wall: L44 (52.4 GB expert working set) = page-cache livelock
  (0 progress in 8 min, D-state proc). L42 = 50 GB borderline thrash.
- **WORKING HIGH-CTX LANE (E157): c40960, 3gpu, ts70,10,20, ncmoe31,
  -ot 'blk\.(3[1-9]|4[01])' (L42), b=ub=5712, Q4 shared draft,
  --spec-draft-n-max 16, threads 8/8, -lm mmap. FITS AND RUNS:
  TG 8.45 t/s (acc 0.48, len 2.44), PP 18.8 t/s COLD-THRASH (would be
  ~120-130 warm; mlock would fix permanently).**
- n_outputs_max exists as ctx param (defaults n_batch, no CLI flag) but
  only sizes post-get_rows output, NOT the matmul intermediate.
- NEXT CODE TARGETS: (a) get_rows-before-output-matmul per-arch fix
  (shrinks logits blob 3.3 GB -> ~0.15 GB at ub5712, unlocks 60-100k);
  (b) GPU-side expert gather (kills ids round-trip, TG 9 -> 20-30 est);
  (c) identify 25 KB/tok vulkan ctx-scratch.
- Artifacts: /tmp/opencode/qwen38-speed/E156-*, E157-40k.

## E158 - --n-outputs-max flag + blob attribution CORRECTED

- Added --n-outputs-max CLI flag (common/arg.cpp; plumbing existed
  end-to-end: common.h:463 -> common.cpp:1727 -> llama-context:249).
  Flag parses and reaches cparams. Harmless, upstream-intended knob;
  useful for non-spec lanes with big b.
- CRITICAL CORRECTION: the speculative wrapper OVERRIDES n_outputs_max
  to draft-n-max+1 (=17) for its contexts (speculative.cpp:2680+).
  The MTP lane's logits intermediate was ALREADY capped at ~10 MB.
  The 3.3 GB logits attribution was WRONG.
- Real vulkan reserve at c40960/ub5712/MTP: 4436.82 MiB, ~110 KB/token
  ctx-scaled, UNATTRIBUTED. Flag changed it by exactly 0 bytes
  (matched the no-flag 'expectation' line byte-for-byte). Next hunt
  step if pursued: gallocr tensor-name instrumentation (name each
  buffer in the reserve).
- Warm-cache lane numbers (c40960, 3gpu, Q4 MTP, n16, threads 8):
  PP 165.56 t/s, TG 9.31 t/s, acceptance 0.523 len 2.57.
  (E157's 18.8 PP was pure page-cache thrash - confirmed.)
- DETERMINISM: this lane is NONDETERMINISTIC - 3/3 unique hashes
  (f0004056 / cf17b2a8 / 71935da0) at identical config. Same class as
  E141/E145 mixed-backend races. Production lane achieved 3/3 only
  with its exact calibrated geometry (ts66,10,24, ncmoe31, Q8 draft,
  n10). BLOCKER for #2: the GPU-gather hash gate needs a deterministic
  baseline lane first.
- #2 GPU-side expert gather: BLOCKED on lane determinism. Design
  pinned: (a) tiny CUDA kernel post-routing builds used-expert range
  list + D2H of ranges only (~few hundred bytes, pinned dest);
  (b) scheduler copy_experts consumes ranges without ids round-trip;
  (c) expected win ~30 s PP bubbles + per-token TG latency (9 ->
  20-30 est). Execute only after a deterministic A/B lane exists.
- Artifacts: /tmp/opencode/qwen38-speed/E158-*.

## E159 - GPU-side expert gather: SHIPPED, hash-gated WIN

- Implemented (env GGML_EXPS_GPUGATHER=1, default off):
  ggml_backend_i.moe_build_ranges iface (ggml-backend-impl.h), CUDA
  impl (bitmap + range-scan kernels, per-device scratch, compact
  pinned D2H, stream-scoped cudaEvent - correct by construction
  since kernel+D2H+event all enqueue on the backend's own stream),
  scheduler call site fills used_ids from device-built ranges;
  fallback to ids round-trip when iface missing or CPU ids_backend.
- Positional-initializer fixes required: meta.cpp, cpu.cpp,
  vulkan.cpp (comment-labeled positional lists shifted one slot).
- RESULTS (2gpu deterministic lane, interleaved A/B x3):
  off 212.5/211.6/213.2 vs on 219.4/216.3/213.3 -> median 216.3,
  +1.8%. sha 9c566ecdb48b 6/6 IDENTICAL - correctness gate PASS.
  ids_sync per ubatch: ~1450 ms -> ~6 ms (250x). NEW CAMPAIGN
  BEST: 216.3 median / 219.4 peak (prev 209.8). Note: off-arm also
  at 212+ (warm clean page cache tonight) - both arms benefit from
  system state; the pairing is same-session so the +3.8 delta is
  real.
- Pending: TG probe on the 3gpu MTP lane (gather kills ~40
  round-trips PER TOKEN - expect the big win there, est 9 -> 15+).
- Artifacts: bench/logs/gpu-gather-ab.log (gitignored, on disk),
  /tmp/opencode/qwen38-speed/E159-*.
  Commit: a2bf395db.

## E159b - gather on TG lane: NEGATIVE, verdict recorded

- 3gpu MTP lane (E154-tg-i config + gather): TG 7.82 t/s vs 9.26
  baseline; acceptance 0.42 vs 0.546 (lane nondeterminism range).
- WHY: at TG, batches <= 17 tokens -> old ids D2H tiny + shallow
  queue -> the full sync it paid was cheap. Gather adds 40x
  (memset + 2 kernel launches + event sync) PER TOKEN - fixed
  overhead that does not pay at decode scale.
- VERDICT: GGML_EXPS_GPUGATHER is a PP-lane optimization only.
  Do NOT set on TG/serve lanes. TG floor on this rig = ~50 split
  syncs per token (architectural). Possible polish: n_tokens
  threshold to auto-disable at small batches (not implemented).
- PP 164.49 on the 3gpu MTP fill (same run) - consistent with
  the 165.6 warm baseline.

## E161/E162 - the 200k ctx hunt: blob chain named, three beasts found

- E160 (fits, warm): c49152 3gpu+MTP ub5712: PP 162.90 / TG 8.06 /
  acc 0.468. Journaled late.
- E161: `-fa on` kills the vulkan KQ blob: vulkan compute reserve
  5065 -> 2777 MiB at c49152 (KQ = materialized scores; FA was
  auto-OFF in all prior lanes! llama-cli `-fa` now takes on/off/
  auto, default auto -> off for this model). PP unchanged (160.5
  vs 162.9, noise).
- GGML_SCHED_DEBUG=2 + `-v` + bench/tools/top-tensors.sh (new
  tool, permanent): per-tensor reserve attribution.
- BEAST 1: `indexer_score_tokens` chain (qwen4exp.cpp build_qsa_top_k
  ~860-912): n_kv*n_tps-scaled f32 family, ~85 KiB/tok slope on
  ROCm0 (13.27 GB @160k). FIXED: token-axis chunking, env
  LLAMA_QSA_CHUNK (0=stock).
- BEAST 2: `attn_inp_kq_` dense QSA mask (fill->set_rows->add,
  build_attn_qsa ~986-1010): 1.87 GB/layer @160k on the attn
  device. FIXED: same chunk env, mask+FA chunked, per-chunk concat.
- HASH GATES PASS: both chunk paths, sha 9c566ecdb48b identical
  (E162-gate2, chunk 1024 = 3 chunks). PP cost on speed lane ~7%
  (196.7) - speed lane keeps env OFF, chunking only for ctx lanes.
- BEAST 3 (THE remaining wall): `attn_inp_kq_mask (copy)` CPY
  nodes: scheduler input-copies of the 3.74 GB F32 [n_kv,n_tps]
  mask input, ONE PER SPLIT READING IT, 30+ copies to ROCm0 in
  the failing reserve (= 9824.63 MiB demand, byte-identical
  across ts/pin attempts). Chunking MULTIPLIES splits (more
  copies) - explains chunk 256 vs 64 delta of only 100 MB.
  -ot 'kqv_fa=Vulkan1' pin FAILED to move it (FA node anon ->
  named it 'kqv_fa' in llama-graph.cpp:2636, still no effect on
  the failing draft-region reserve).
- New tooling: GGML_GALLOC_DUMP / GGML_GALLOC_DUMP_MB (ggml-alloc.c
  reserve composition dump, >=64 MB default); top-tensors.sh.
- NEXT (terminal fix): generate the causal mask IN-GRAPH from
  tiny inputs - n_visible [n_tps] I32 + arange [n_kv] I32 ->
  where(less(col,vis),0,-inf) per chunk, in both build_qsa_top_k
  and build_attn_qsa; kills the dense mask input + the entire CPY
  storm. Draft replay (17 spec steps + dense build_attn path)
  included. Values identical -> hash-gateable.
- NOT yet journaled as done: mask-generation surgery. Rig state:
  probes clean, no stray processes.
- Artifacts: /tmp/opencode/qwen38-speed/E161-*, E162-*.
