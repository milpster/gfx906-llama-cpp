# JOURNAL 2026-09-20

## E174 - upstream scan + sync prep: 120 commits (b6b003d2c..a894dae93, v0.4.1)

User directives:
- "check upstream llama cpp for everything that is new and prepare to
  integrate it all into our fork. list what could benefit our prod
  configs. read and write journals."
- "dont break existing builds and prod configs" (binding constraint on
  everything below).

### Starting state
- HEAD 0d95fa0f0 (merge through b6b003d2c, sync 2026-09-13). Upstream
  origin/master now a894dae93 (2026-09-20), 120 new commits, version
  bump to 0.4.1 (ggml 0.24.0). Fork carries 312 commits.
- E169 carry set CONFIRMED already in tree: #28587 (fa6769818), #28715
  (b0dcb8192), #28830 (f1e44dcc1). Nothing to re-carry.
- E170 (#28873, #28896, #28874): #28896 rides this sync as 41abbfd59;
  #28873/#28874 are still OPEN upstream PRs (not in the 120) - stay on
  the E170 patch track, not the sync.
- Prod untouched this session: port 8009 server, all launchers, all
  build dirs. Read-only git + journal writes only.

### Direct prod-relevant (mainline: Qwen3.8 i1-Q6_K + mmproj-F16 CPU +
DFlash2 Q4_K_M, gfx906 x2 + 3080 Vulkan, -sm layer)
1. #28149 + #26070: ggml alloc-failure checks + graph buffer
   reservation failure handling. Same class as the E173 fattn-pool OOM
   aborts (AMD ~200 MiB floor). Will not add memory, but can convert
   hard GGML aborts into handled failures. CARRY + verify abort path.
2. #28389 CUB argsort in-place keys corruption fix (CUDA/HIP):
   sampling/top-k run on ROCm0 in our topology (E128/E146 note) -> this
   is a prod-path CORRECTNESS fix, not cosmetic. CARRY + sha lane.
3. #28882 CPU CACHE_LINE_SIZE/PCH heap-corruption fix (+ #28892 PCH
   removal): mmproj vision runs on CPU -> stability there. CARRY.
4. #27779 CPU FWHT F16 input: vision-on-CPU perf path. CARRY.
5. #28975 vulkan NV argsort_large driver workaround + #28996 vulkan
   im2col buffer_reference alignment: vulkan1 stability/correctness,
   cheap. CARRY.
6. Robustness for prod traffic: #28868 get_key_or_arr, #29127
   json-schema escaped hyphen (opencode structured output), #29161 PEG
   invalid UTF-8, #28993 GGUF data-section alignment, #29036 gguf-py
   Q8_1 block size, #28869 qwen3-coder think-close, #29125 server
   startup logs (cosmetic; our E168 patch lives in server-context.cpp
   which upstream did NOT touch -> no conflict).
7. Hygiene: 0.4.1 + ggml 0.24.0 bumps, #28771 cmake source-dir fix,
   #29026 cmake CPU=OFF fix.

### Side-lane relevant
- #28549 MTP draft CUDA-graph thrash fix: E169 flagged the same thrash
   on our qwen4exp HIP-graph lane (VEGA_TUNE_GRAPHS). Now merged.
   PORTED VIA SYNC; touches llama-context.{cpp,h} = top-2 conflict zone.
- #28896 qwen4exp rms_norm+mul fusion (= E170 candidate 2) + #28901
   qwen4exp hc ops + #28988 vulkan hc ops: qwen4exp/qwen38f lane perf
   on both gfx906 and the 3080.
- #28770 sparse FA for qwen4exp: mma-kernel gated (WMMA/MFMA) - gfx906
   has NEITHER, so no gfx906 kernel path; benefit only possible on the
   3080 Vulkan slice via #28105 vulkan sparse FA, and only if the lane
   model emits sparse masks at DKQ/DV=256. INVESTIGATE at lane time,
   do not promise.
- #28849 auto-fit unified-KV n_ctx sizing: we always set explicit -c,
   fitter only validates (E171: "kept user params"). Small fit.cpp
   conflict, low prod impact. CARRY.
- #28013 CUDA/HIP im2col access patterns: vision is CPU-mounted in
   prod -> dormant unless vision moves to GPU. RIDE.
- #28846 BF16->F32 fallback (no-BF16 devices): dormant robustness,
   gfx906 has no BF16. RIDE.
- #27825 HIP AllReduce: we run -sm layer PP, not TP. Dormant. RIDE.
- #28822 vulkan IQ3_S MMQ: no IQ3_S in any lane. SKIP (watch if a
   future drafter goes IQ3_S).
- MoE vulkan batch (#28501, #25483, #28923): dense prod. SKIP.

### Rejected again (same verdicts as E169/E146/E167)
- RDNA3.5 MoE tile heuristic #28935, MFMA fp32 accum #28576 (CDNA;
   conflicts with our fattn files but no gfx906 effect - resolve, do
   not chase), metal/hexagon/opencl/sycl/webgpu/openvino/spacemit/MUSA
   batch (~50 of 120 commits), Maple ternary MoE, HrmText, Ling 3.0,
   DeepSeek V3.2/V4 delimiters, Nemotron MTP/H, mimo2 SWA, mamba
   contiguity, RPC pair, TP qkv-split state, Model-Saver, CI/docs.

### Conflict surface (ranked; fork-side scale measured)
| zone | fork lines | upstream driver | resolution rule |
|---|---|---|---|
| ggml-cuda fattn.cu/-common/-mma-f16 (+mmq.cu) | +309 total | #28770 sparse rework, #28576 | keep vega tunes/tables; take upstream hunks around them; gfx906 never compiles mma path |
| llama-context.{cpp,h} | +276 | #28549 MTP graph fix | keep pipeline-parallel + draft-ctx exclusion; re-apply #28549 logic onto fork's draft graph handling |
| ggml-vulkan.cpp | +27 (+43 shaders) | #28732 file split | shaders unaffected; re-map our 27-line fattn-path selector into the split layout (ggml-vulkan.cpp/-buffers.cpp/-common.h) |
| common/fit.cpp | +27 | #28849 | keep both (fork fit additions + upstream n_seq_max sizing) |
| remaining 33 overlap files | small | additive (llama.h enums, loader, bench --version, tests) | standard keep-both, per 2026-08-25 playbook |

### E174 sync procedure (NOT executed this session)
0. Protected surfaces: main worktree tracked state (journal-only
   writes), build-sync0909/0913/dflash-novega/vega20-prod-verified and
   all build-*, all launchers (byte-stable, incl. E173 floors: i1
   q8/q8 353200 ot3, swift q8/q8 344000 ot3, swift q8/q4 393500 no-ot,
   f16_f16 port 8009), prod server 8009 (no GPU lane work without an
   explicit stop/start decision), E170 worktrees.
1. Detached worktree at 0d95fa0f0, `git merge a894dae93`; resolve per
   table above; document resolutions like UPSTREAM-SYNC-2026-08-25.md.
2. Fresh build dir build-sync0920, exact build-dflash-novega.sh flags;
   migrate GGML_CUDA_FA_ALL_QUANTS -> GGML_FA_QUANTS (#28079 friction,
   E169 item 4) and confirm FA coverage did not silently change.
3. Gates before any master move: temp-0 sha lanes on ALL THREE shipped
   configs; pp16384 + tg1024 quick lane (E171/E173 method); fattn-pool
   survival double-run at shipped -c (AMD free >= 200 rule); abort
   check that #28149/#26070 turn the pool OOM into a handled failure.
4. Gates pass -> merge to master in main tree; launchers unchanged;
   prod binary swap only on explicit user stop/start decision; rollback
   = build-sync0909 pointer.
5. Fold-ins: drop E170 pr28896 worktree if 41abbfd59 passes gates;
   E170 #28873/#28874 stay on their own track (still open upstream).

### Verdict
No new upstream perf lever for the prod mainline (consistent with
E169/E146). The sync's real value: robustness cluster (#28149, #26070,
#28389, #28882, vulkan NV fixes), lane upgrades (qwen4exp fusion/hc,
MTP graph fix), and staying mergeable - the vulkan file split only
gets more painful to re-map the longer we wait. Recommend executing
E174 sync next session with the gate set above.

### E174.1 - Qwen3.8-Flash-Next (qwen38f/qwen4exp) sparse FA, explicit
Question: does the upstream sparse-FA work help the Flash-Next lane?

Facts (diffs read, not guessed):
- The enable is exactly our open TODO: qwen4exp.cpp:761 flips
  build_attn_mha(..., 0, ...) -> (..., top_k->ne[0], ...) (PR #27970
  matured into #28770). Machinery (QSA mask, hybrid-idx indexer cache,
  hc ops) is already in our tree; the one-liner rides the sync.
- gfx906/ROCm (58-60 of 66 layers): NO sparse FA, by upstream design.
  fattn.cu: shall_use_sparse returns false under GGML_USE_HIP, and
  compact_mask GGML_ABORTs on HIP ("only supported on NVIDIA CUDA").
  Not a gfx906 limitation - ALL AMD (incl. CDNA/MFMA) is excluded.
  Dense QSA as today; zero change, zero risk on the VIIs.
- Vulkan1 3080 (~7-10 of 66 layers, ts 66,10,24): the ONLY possible
  win. #28105 adds sparse FA to ALL vulkan fattn variants (generic +
  cm1 + cm2) plus a new sparse-compact shader (subgroup or plain),
  ratio-gated (engages when KV/sparse-window >= 2, or 4 on coopmat2),
  env kill switch GGML_VK_FA_SPARSE_DISABLE. 3080 has coopmat2
  (E169 #28528 note). Open items for lane time: D=256 (qwen4exp head
  dim) shader specialization actually picked on the 3080; whether the
  attn layers landing on the vulkan slice are QSA layers at all.
- Ceiling: SMALL. Only vulkan1's slice benefits, only at depth; the
  model is mostly GDN/linear + MoE FFN that never touch fattn. Do not
  promise lane-level t/s; verify per-layer before claiming anything.
- Numerics: sparse compaction changes accumulation order on the
  vulkan slice -> lane sha WILL move; run-qwen38f-200k.sh header
  mandates the determinism gate (bench/qwen38-nondet-arm.sh x3) for
  ANY change. Fallback switch: GGML_VK_FA_SPARSE_DISABLE=1.
- Bigger Flash-Next wins in the same 120: #28549 MTP draft graph
  thrash fix (this lane runs draft-mtp-adaptive + HIP graphs, the
  E169-flagged class), #28896 rms_norm+mul fusion (= E170 candidate 2,
  needs our NextN HC adaptation), #28901 hc ops (CUDA/HIP = VIIs),
  #28988 vulkan hc ops. Sparse FA is the least of the lane's sync
  value; do not gate the sync on it.

Verdict: take the enable (free), expect nothing on the VIIs, treat
3080 sparse as a lane-time experiment behind the determinism gate.

### E174.2 - rest of the 120, Flash-Next lane sweep (qwen38f/qwen4exp)
Headline: this lane, not prod mainline, is the biggest sync beneficiary.

1. #28901 qwen4exp hc ops: NEW fused ggml op ggml_dsv4_hc_pre_gated -
   the ggml.h comment names "(Qwen3.8-Flash-Next)" explicitly. Fuses
   the hyper-connection modules (2 per layer: pre token-mixer, pre MoE;
   hc_dim = hc*n_embd, plus low-rank up/down) that our graph builds
   from primitives today. Implementations land for CPU, CUDA/HIP
   (dsv4-hc.cu), Vulkan, metal, sycl. Runs on EVERY layer on EVERY
   device incl. the CPU expert zone -> per-layer perf everywhere +
   fewer ops = fewer cross-backend scheduling boundaries (the E141
   race lived exactly there). #29169 (metal) and the 4-day cadence
   show upstream is productizing this arch - expect more each sync.
   MERGE COST: qwen4exp.cpp graph switches to the fused ops; our fork
   carries fork-only NextN HC tensor handling (E170 candidate 2 was
   adapted for exactly these) -> reconciliation is the lane's main
   sync conflict, bigger than sparse FA.
2. #28988 vulkan hc ops: 3080 side of the same (dsv4_hc_pre/post.comp).
3. #28896 rms_norm+mul fusion: E170 candidate 2; VIIs, lane-relevant.
4. #28549 MTP draft graph fix: lane runs draft-mtp-adaptive n10/amin3
   with HIP graphs - the E169-flagged thrash class. Direct fix.
5. #26070 graph-reservation failure handling: the lane's own evidence
   (E142: reserve requests 114,536-122,236 MiB on ROCm0 at b512/b16384)
   is exactly this failure mode; graceful handling beats abort during
   fit experiments. #28149 alloc checks: shared robustness.
6. #28882 CPU heap-corruption fix: UPGRADED relevance here - the lane
   runs 29-31 CPU MoE expert sets + CPU mmproj; heavy CPU path use.
7. Vulkan MoE trio: #25483 coopmat1 unneeded-work skip (TG sparsity is
   real: ~10 routed selections of 512 experts at ub<=128; but cm1-path
   only - 3080 may dispatch cm2 for mul_mm, verify), #28501 mul_mat_id
   hoist 256->512 experts (EXACTLY this model's 512-expert count;
   dormant if lane runs packed 3D tensors via plain mul_mm - E142
   suggests packed), #28923 MUL_MAT_ID BN/2 tail. All narrow (vulkan
   slice only), check at merge, do not promise.
8. Shared: #28389 CUB argsort fix (sampling on ROCm0), #28749 n_rs_seq
   early-out (no probe decode; ctx-checkpoints 0 here, minor),
   loader/json/PEG robustness rides.
9. N/A again: mamba dt contiguity (GDN has no dt proj), BF16 fallback,
   RPC/TP, im2col (mmproj CPU), FWHT.

Constraint reminder: ANY lane change re-runs bench/qwen38-nondet-arm.sh
x3 per run-qwen38f-200k.sh header. hc-op fusion + #28549 + vulkan MoE
changes all qualify -> one gated lane pass after sync covers them all.

### E174.3 - sync EXECUTED: merge, build-sync0920, CPU-side test gates

User: "adopt all, make new build in a new dir, (are there tests?) report
back. dont touch running llama cpp."

Merge: 7d6d1a1b9 "Merge upstream master through a894dae93" + fixup
cfd7c3071. 6 conflicted files, all resolved per E174 table:
- fattn-mma-f16.cuh: kept fork nvis line, took upstream sparse
  KV_max/sparse_indices semantics (downstream hunks auto-merged).
- llama-context.{cpp,h}: kept fork seq-layout drain + graph_seq_ids,
  took upstream gf_res_prev_active tracking (#28549) - independent
  features, both live.
- qwen4exp.cpp: fork trunk_flags/flags MTP loading scheme kept;
  upstream [n_embd,hc] gamma reshape + TENSOR_ALLOW_RESHAPE applied ON
  TOP (combined flags); upstream fused-hc graph switch auto-merged
  (cparams.fused_dsv4_hc_pre); sparse enable applied INSIDE the fork's
  chunked-QSA loop (LLAMA_QSA_CHUNK preserved, n_kv_max=top_k->ne[0]).
- test-backend-ops.cpp: kept both test blocks (fork SYCL m=12/20 FWHT
  + upstream F16 FWHT).
- ggml-vulkan.cpp: kept fork nvis param/buf + upstream sparse; NV
  buffer renumbered binding 7 -> 8 (upstream SP took 7); FA pipelines
  9 descriptors; debug blocks 5/6 dropped (moved upstream to
  ggml-vulkan-debug.cpp by the #28732 split).
- LESSON (repeat offender class): running `sed` on a file in the SAME
  parallel batch as edit-tool calls on that file = last-write-wins
  race; the 9-descriptor bump was silently reverted and shipped in the
  merge commit. Caught by test-recurrent-state-rollback aborting at
  ggml-vulkan-common.h:263 (parameter_count 8 != 9 buffer infos).
  Fixup cfd7c3071; sequential file mutations from now on.

Build: build-sync0920 (FRESH, full targets, vega tunes on, FA_QUANTS=all
already in the script - E169 item 4 was pre-fixed). 100%, no errors.
Version: 0.4.1-dev (build 11377, commit cfd7c3071 after fixup).

Tests (CPU-side only; prod owns the GPUs):
- ctest -E download: 60/61 pass. Failures triaged:
  a) 7 aborts/fails (rollback x3, save-load-state, state-restore-
     fragmented, eval-callback, thread-safety) = ALL the lost
     descriptor bump; green after cfd7c3071.
  b) test-tokenizers-ggml-vocabs: models/ggml-vocabs/ is UNTRACKED
     stale LFS-pointer cruft (not in git, git-lfs absent, upstream
     media URL 404s). Environment, pre-existing, NOT merge. Remedy:
     drop the dir or install git-lfs (user call).
- test-backend-ops -b CPU: runs Vulkan0-3 + CPU = 5/5 PASS, 0 fails.
  This hardware-exercised the merged FA shaders + 9-descriptor
  pipelines on the 3080 (NV_coopmat2 confirmed in device list) and
  both VIIs (RADV), plus new hc ops / F16 FWHT on CPU.
- chat-auto-parser + chat-analysis: PASS from repo root (need CWD for
  models/templates/*.jinja). gbnf-validator: arg-needing tool, not a
  self-running test. HIP backend itself not covered by any test here -
  prod lanes are its gate.
- Evidence prod untouched: server pid 18457 constant, /health
  {"status":"ok"} before+after all GPU-adjacent tests, port 8009
  listening.

Pending (needs explicit prod stop/start decision, E174 step 3-4):
temp-0 sha lanes x3 shipped configs, pp16384+tg1024 quick lanes,
fattn-pool survival double-run, qwen38f determinism gate x3
(bench/qwen38-nondet-arm.sh), abort-path check for #28149/#26070.
Launchers and all pre-existing build dirs untouched; nothing pushed.

date: 2026-09-20
session: merge + build + CPU tests; GPUs untouched except transient
test-backend-ops allocations (Vulkan devices, small tensors, all
passed); prod server never stopped/restarted.
