# JOURNAL 2026-09-10

## Push unblock: GitHub 100MB limit (ops, not an experiment)

- Symptom: `git push gfx906 master` rejected by pre-receive hook.
  `bench/bin-baseline-mmq/libggml-hip.so.0.16.0` at 115.70 MB exceeds
  GitHub's 100 MB hard limit; blob entered history in the E139
  sync-completion commit (c84835a69) and was only tree-removed by the
  later "removed unneeded bloat" commit, which does not help a push.
  A 44 MB `libggml-vulkan.so.0.16.0` twin rode along (under limit, bloat).

- Root cause: `.gitignore` has `*.so` but not `*.so.*`, so versioned
  library names (`libggml-hip.so.0.16.0`) slip through.

- Fix:
  1. `pip install git-filter-repo`, then strip both blobs:
     `git filter-repo --force --invert-paths --path <hip>.so.0.16.0
     --path <vulkan>.so.0.16.0 --refs gfx906/master..master`
  2. `.gitignore`: added `*.so.*` after `*.so` (Extensions section).

- Verification:
  - `git diff backup/pre-blob-strip master` is empty: HEAD tree
    byte-identical to pre-rewrite.
  - Blob scan of gfx906/master..master: nothing over 20 MB remains.
  - 103 commits in range, merge-base == gfx906/master tip (f4a3841fa):
    push is a clean fast-forward.

- Known side effect: filter-repo's `--refs` range mode rehashed 102/103
  commits (incl. the 93 upstream commits of the 2026-09-09 sync, now
  content-identical twins). Next `git merge origin/master` resolves
  trivially since trees match. Rollback point: branch
  `backup/pre-blob-strip` holds the full pre-rewrite chain; delete it
  once the push is confirmed good.

## E141 - Qwen3.8-Flash-Next AD-4.27 placement setup

- Requested target: `/home/srcds/ai/ai/Qwen3.8-Flash-Next-AD-4.27bpw-Q4_K_M-M64.gguf`, 94,525,391,200 file bytes, 1,224 tensors, `qwen4exp`, 48 layers, native context 262,144.
- Fixed hardware order from the previous production launcher: `rocm0,vulkan1,rocm1` = Radeon VII 16 GiB, RTX 3080 Laptop 8 GiB, Radeon VII 16 GiB. `Vulkan0` is the Renoir iGPU and is excluded. Clean free VRAM reported by this build: 16,348 / 7,754 / 16,348 MiB.
- Binary baseline: `build-sync0909/bin/llama-server`, version `0.4.0-dev`, build 11053, commit `c389ecc99`; matching libraries come from the same `bin` directory plus the existing gfx906 ROCm userspace.
- Exact GGUF storage split from `GGUFReader`: 94,514,368,000 tensor bytes total; `per_layer_token_embd.weight` is Q5_1 `[160, 320001536]` and 38,400,184,320 bytes; all other tensors total 56,114,183,680 bytes. The PLE table is a random row lookup, not a compute-bearing layer.
- Architecture: 12 groups of three Gated DeltaNet layers then one Qwen Sparse Attention layer; every layer has 512 MoE experts with 10 routed plus one shared active. Local layer tensor sizes are about 1.090-1.095 GB for blocks 4-39, with the imatrix high-bit bands at blocks 0-3 and 40-47 about 1.274-1.314 GB each.
- Storage policy: keep mmap enabled and force `--lazy-mode on`. Local `qwen4exp.cpp` marks the PLE table `TENSOR_READ_LAZY`; this leaves the 38.4 GB table SSD-backed and gathers only selected rows. Do not use `--load-mode none`, mlock, or `USE_MLOCK=true`. The AtomicChat model card also requires `-fit off` because automatic fitting mis-sizes this architecture.
- KV policy: explicit F16 target K/V (`-ctk f16 -ctv f16`), one slot, native 262,144 context as the first manual fit target. No iGPU, no vision projector for this text-only calibration.
- MTP gate: the target GGUF contains zero `nextn` or `mtp` tensors. Unsloth publishes a compatible sidecar and recommends `MTP/mtp-Qwen3.8-Flash-Next-shared-Q8_0.gguf`, but this fork has no qwen4exp MTP graph or shared-tensor borrowing from pending upstream PR #28243. The generic adaptive controller alone is insufficient, so MTP is disabled rather than silently misconfigured. No sidecar was downloaded.
- Sources: Qwen model card `https://huggingface.co/Qwen/Qwen3.8-Flash-Next`; exact quant card `https://huggingface.co/AtomicChat/Qwen3.8-Flash-Next-GGUF`; Unsloth MTP guide `https://huggingface.co/unsloth/Qwen3.8-Flash-Next-GGUF/blob/main/MTP/README.md`; qwen4exp MTP work `https://github.com/ggml-org/llama.cpp/pull/28243`.
- User selected adapting PR #28243 before startup trials. Fetched `refs/pull/28243/head` as `pr/28243`; head `d1a92352cbd417fd840b4e765c0b82f5fe3d1d89`, merge-base with local HEAD `95ef7fc16054e63b427a3ef00188e055ef7586d8`. The final PR delta is 19 files, 401 insertions, 73 deletions; `git diff --check` passes.
- Conflict simulation: `git merge-tree --write-tree HEAD pr/28243` completed without conflicts and produced tree `c90682d7526bfa66c809142c48cc464fd2c6ea12`. Port the final tree instead of replaying its superseded intermediate commits; omit only the unrelated blank line in `src/llama-model-loader.h`.
- Fork interaction review: local adaptive MTP commit `d095dc2fd` changes the generic draft-depth controller around the PR's `is_mem_shared` assignment but does not conflict. The PR's final qwen4exp rule must remain: borrowing `ctx_other` does not imply shared KV memory; only `gemma4-assistant` is memory-shared. A qwen4exp sidecar has one MTP layer, so `chain_heads` remains false while plain or adaptive repeated drafting can still use the configured cap.
- Pipeline interaction review: local commit `165fc79cb` excludes `LLAMA_CONTEXT_TYPE_MTP` from pipeline parallelism. Reserve fallback `bed69188e`, sequence-layout drain `42fc61855`, and single-device drafter wrapper `bfe829c95` are orthogonal to the qwen4exp graph and loader changes. Automatic fitting still cannot instantiate a shared-head draft before the target context exists, so runtime remains manual with `-fit off`.
- Upstream risk gate: PR #28243 is still a draft with no completed status checks or approval. Build and focused tests must pass first. Then compare no-spec and plain `draft-mtp` at low context before enabling `draft-mtp-adaptive`; stop and report, without a source workaround, if this fork crashes, corrupts output, emits invalid positions, or leaves the scheduler unable to recover.
- Next: apply `HEAD..c90682d7` uncommitted, run diagnostics and focused tests, build in a separate directory, then obtain and verify the Unsloth shared-Q8_0 sidecar.
- Applied-port state: applied `HEAD..c90682d7` to the 18 intended source/converter files without staging or committing. The resulting files compare byte-for-byte equal to merge tree `c90682d7`; `src/llama-model-loader.h` remains unchanged and `git diff --check` passes.
- Static validation: all 10 changed Python files pass `uv run python -m py_compile`; `convert_hf_to_gguf.py --help` imports successfully and advertises `--mtp-shared-embd`. C++ clangd diagnostics are not usable in this checkout because its compile database lacks project include paths (`llama.h` and `ggml.h` not found); the real CMake build is the authoritative C++ gate. Existing basedpyright debt remains in `conversion/qwen4exp.py`, `convert_hf_to_gguf.py`, and `gguf-py`, while the other changed converter modules report no diagnostics.
- Build gate in progress: fresh isolated directory `build-qwen38-mtp`, ROCm 6.1 clang, `AMDGPU_TARGETS=gfx906`, HIP plus Vulkan, production VEGA tune flags. Requested targets are `llama-server`, `test-speculative-adaptive`, and `test-llama-archs`; no production build directory is modified.
- Build result: success. `build-qwen38-mtp/bin/llama-server` is a valid ELF, version 0.4.0-dev, build 11060, commit `b1a8d8b63`; HIP gfx906 and Vulkan libraries linked with the production VEGA FATTN/MMQ/TOPK/GRAPHS flags. `ctest -R 'test-speculative-adaptive|test-llama-archs'` passed 2/2 in 72.12 seconds.
- Adversarial converter review: `Qwen4ExpTextModel` reaches `_QwenMtpMixin` through `_LinearAttentionVReorderBase -> Qwen3NextModel`, so MTP block-count extension, `nextn` metadata, and `mtp_shared_embd` filtering are active despite the indirect inheritance. No missing integration edge was found.
- Live Hugging Face API metadata for `MTP/mtp-Qwen3.8-Flash-Next-shared-Q8_0.gguf`: exact size 2,786,568,256 bytes; LFS SHA-256 `5ff54097406a905cf3a724c709124ceb0e3e10235ee862298969e91c96fa96e6`. Download destination is `/home/srcds/ai/ai/MTP/mtp-Qwen3.8-Flash-Next-shared-Q8_0.gguf`; verify both fields before runtime use.
- Sidecar result: download completed and both size and SHA-256 match the Hugging Face LFS metadata. `llama-gguf` reads all 52 metadata entries and 32 tensors; the payload is a qwen4exp block 48 shared head with no token embedding or output tensor.
- Placement smoke: `-ngl all -ncmoe 24 -sm layer -ts 40,19,41` failed before graph execution because the contiguous heavy tail requested 21,875.36 MiB on ROCm1. Rebalancing to `-ts 70,13,17` loaded successfully at 4,096 context; observed allocation was about 13.4 GiB ROCm0, 6.7 GiB Vulkan1, and 9.2 GiB ROCm1. This was a clean configuration miss, not a source failure.
- Runtime command core for all equivalence arms: target GGUF, `-lm mmap -lzm on -fit off -fa on -ngl all -ncmoe 24 -b 512 -ub 128 -cram 0 --ctx-checkpoints 0 --device rocm0,vulkan1,rocm1 --pipeline-parallel off -ts 70,13,17 -sm layer -c 4096 -ctk f16 -ctv f16 --temp 0 --seed 42 -n 32 --no-warmup`; prompt `The capital of France is`.
- No-spec A1 completed normally but continued `[Start thinking] The user is asking for the capital of France...`; prompt 3.33 tok/s, generation 9.01 tok/s. Plain MTP B1 added only the verified sidecar, `draft-mtp`, `n_max=2`, `-ngld all`, and F16/F16 draft KV; it completed normally but instead closed thinking and began the answer, with acceptance `1.00000 (20 accepted / 20 generated)`, prompt 5.74 tok/s, generation 6.93 tok/s.
- Correctness confirmation blocker: an identical warmed no-spec A2 did not reproduce A1. A2 instead continued `[Start thinking] Thinking Process: 1. **Identify the core entity and question:**`; prompt 5.94 tok/s, generation 12.57 tok/s. Therefore the no-spec temp-0/seed-42 lane itself is not output-reproducible, and B1 cannot be used to attribute divergence to MTP.
- Abort: this violates the fork's deterministic output gate. Per user instruction, stop without a source workaround. Do not test MTP `n_max=1`, adaptive MTP, claim performance, or continue high-context tuning until the baseline nondeterminism is investigated separately.
- User authorized the baseline-nondeterminism investigation. Reusable harness: `bench/qwen38-nondet-arm.sh <arm> <reps> [extra args]`; fixed lane from E141 line 60, outputs to `/tmp/opencode/nondet/<arm>/`, verdict = unique sha256 count.
- Arm R (baseline x3): NONDETERMINISTIC, 3 unique hashes (`9d4badea`, `47f86323`, `1f3f7096`). Flips at varying token positions (token ~3 or ~13); after a flip the continuation stays coherent. Same hash `47f86323` equals original A1 text, `1f3f7096` equals A2 - the bug reproduces the originally observed variants exactly.
- Arm H (`HIP_GRAPH=0`): NONDETERMINISTIC - invalid toggle, `HIP_GRAPH` env is read nowhere in ggml (production-launcher leftover); duplicated baseline by accident.
- Arm H2 (`GGML_CUDA_DISABLE_GRAPHS=1`, the real switch in `common.cuh:1315`): NONDETERMINISTIC. Log counter `graphs reused = 30` is `llama_perf_context().n_reused` (compute-buffer reuse), unrelated to HIP cudaGraph. HIP graphs exonerated.
- Arm F2 (`-fa off`, effective: TG dropped 12.6 -> 5.9 tok/s): NONDETERMINISTIC, same variant pool. Attention path exonerated (both fattn and non-fattn).
- Upstream context (refresh): llama-cli fixed-shape runs are expected run-to-run deterministic; known upstream nondeterminism requires batch-shape variation (server slots/prompt cache) which this lane does not use. Divergence is therefore anomalous, consistent with a fork defect.
- Suspect ranking after R/H2/F2: (1) CPU-MoE expert path with 9/10 threads, (2) GPU MoE MMQ lane incl. commit `469de3c3d` OOB padding (uninitialized pool pad reads), (3) Vulkan lane on the 3080, (4) lazy-PLE mmap gather. Arms queued: V (`--device rocm0,rocm1 -ts 60,40`), M (`-ncmoe 48`), T (`--threads 1 --threads-batch 1`).
- Arm V (rocm-only): did not run - with 2 devices the layer allocator still wants a ~21.8 GB contiguous tail on the last device (`failed to allocate ROCm1 buffer of size 21842448128`), same failure class as the original 40,19,41 OOM. rocm-only needs a different -ncmoe, so this arm stays confounded and parked.
- Arm M (`-ncmoe 48`, all experts CPU, threads 9/10, TG 0.26 t/s): DETERMINISTIC, both reps `9d4badea`.
- Arm T (`--threads 1 --threads-batch 1`, 24 expert sets still on GPU, TG 3.3 t/s): DETERMINISTIC, both reps `9d4badea`. `9d4badea` is the clean-numerics reference variant.
- Arms TB1 (9 gen/1 batch), TG1 (1 gen/10 batch), W8 (8/8, full speed 6.9 PP / 12.2 TG): all DETERMINISTIC.
- Arm R2 = exact baseline rerun (9 gen/10 batch) at 21:12: NONDETERMINISTIC again (2 unique of 3). W10 (10/10): NONDETERMINISTIC. So no time-window artifact; current trigger map: both phases threaded at >8 threads (9/10, 10/10) nondeterministic; 8/8 or either phase at 1 or ≤8 deterministic.
- CPU topology: AMD Ryzen 9 5900HX, 8 physical cores / 16 SMT threads. The determinism boundary sits exactly at physical-core count: CPU work split across >8 workers enters SMT-sibling territory and flips near-tie logits run-to-run.
- CPU backend provenance: `git log ggml/src/ggml-cpu` shows only upstream commits, no fork modifications.
- Interim synthesis: single-thread (T) and all-CPU-experts (M) arms agree bit-exact on `9d4badea`, so logits math is stable when the racing condition is absent; at >8 threads in both phases the lane produces the other variants. Mechanism arms running: P0 (9/10 + `--poll 0 --poll-batch 0`, threadpool spin-off), W9 (9/9), NCM30 (`-ncmoe 30` at 9/10, fewer GPU-expert layers).
- Mechanism arms: P0 (9/10 with `--poll 0 --poll-batch 0`) DETERMINISTIC; W9 (9/9) DETERMINISTIC; NCM30 (`-ncmoe 30`, 18 GPU expert sets, 9/10) NONDETERMINISTIC.
- ROOT CAUSE (localized, evidence-complete): the CPU threadpool spin-polling wait (`--poll`, fork default 50; loop in `ggml/src/ggml-cpu/ggml-cpu.c` `ggml_graph_compute_poll_for_work`, ~line 3208, sync helpers at 3178-3197). With worker counts above the 8 physical cores of the Ryzen 9 5900HX (8C/16T), spin-polling workers oversubscribe SMT siblings while GPU-resident MoE expert layers interleave with CPU-expert layers in the same graph; near-tie greedy logits then flip run-to-run. Kill switches, proven independently: (a) `--poll 0 --poll-batch 0` at 9/10 -> deterministic; (b) `--threads 8 --threads-batch 8` -> deterministic at FULL speed (PP 6.9 t/s vs 5.4 at 9/10, TG 12.2 vs 12.8); (c) any phase at 1 thread; (d) removing GPU-resident experts (`-ncmoe 48`, 0.26 t/s). HIP graphs off: still nondeterministic. FA off: still nondeterministic. GPU expert count (24 vs 18 sets): irrelevant.
- Determinism also requires GPU experts in the graph: M (all experts CPU) is deterministic even at 9/10 - so the defect is in the CPU-polling x CPU/GPU-interleaved-MoE interaction, not in either backend's kernels alone. Exact racing pair (threadpool sync state machine vs scheduler async handoff) not yet isolated; needs phase-2 instrumentation.
- Three independent deterministic configurations (T 1/1, M -ncmoe 48, W8 8/8) converge bit-exact on output hash `9d4badea...` - the clean-numerics reference for future equivalence gates.
- Full arm matrix (2-3 reps each, all logged under `/tmp/opencode/nondet/<arm>/verdict.txt`): NONDET: R, R2 (9/10), H (invalid toggle), H2, F2, W10 (10/10), NCM30. DET: T (1/1), TB1 (9/1), TG1 (1/10), W8 (8/8), W9 (9/9), M (`-ncmoe 48`), P0 (9/10 + poll 0).
- Practical unblock, no source change: run the Qwen3.8 lane with `--threads 8 --threads-batch 8` (faster than 9/10 anyway) or `--poll 0 --poll-batch 0`. Production launchers using >8 threads with default poll on SMT boxes carry this risk on interleaved CPU/GPU MoE graphs.
- Harness kept: `bench/qwen38-nondet-arm.sh`. Not committed; awaiting user direction on (a) phase-2 root-cause instrumentation, (b) source fix in threadpool/scheduler, (c) resuming the MTP equivalence work on the W8 lane.
- MTP correctness resumed on the deterministic lane: arm MTP2 (`draft-mtp`, `n_max=2`, sidecar, threads 8/8, poll 0) NONDETERMINISTIC, 3 unique hashes - BUT r3 equals the clean reference `9d4badea` exactly. The qwen4exp MTP graph and acceptance logic are therefore correct; spec-decode draft/target ping-pong reopens the same race even at 8/8 + poll 0 (r1/r2 diverge at token ~3/~13, same near-tie signature; acceptance 0.86, mean len 2.73; single shared threadpool n=8).
- Arm RO32 (`--device rocm0,rocm1 -ts 70,30 -ncmoe 32`, 16 GPU expert sets on ROCm only, threads 9/10, default poll 50): DETERMINISTIC x3. Narrowing: the race REQUIRES the Vulkan device in the layer pipeline - ROCm-only with CPU/GPU interleaved MoE at oversubscribed thread counts is clean. Fork carries Vulkan tuning commits (`1945e0920`, `9063d7970`: f16 B-type matmul pipelines + warp tile tuning) in the expert-matmul lane; prime suspects for the racing code.
- Next: differential run against pristine upstream `~/dev/llama.cpp` (build-0909) on the identical 3-device no-spec lane. Upstream deterministic -> fork-introduced -> bisect fork Vulkan/ROCm commits. Upstream nondeterministic -> upstream/driver bug -> workaround config only.
- Arm UP3 (upstream b10884 = 434ddbbc0, same lane minus fork-only `--pipeline-parallel`): DETERMINISTIC x3, hash `9d4badea` - bit-identical to the fork's clean reference. Race is fork-introduced; fork numerics match upstream exactly when clean. UP3B rerun later: still deterministic (environment stable).
- Fork delta since sync (50 commits): ggml-vulkan and ggml-cpu untouched; all ggml changes in ggml-cuda (vega tunes, fattn/mmz/mmvq/topk, HIP graph guards, q8_1 activation cache port from mx-llama.cpp 775a8051f6); src/ changes are pipeline-parallel machinery (inactive in these arms, `--pipeline-parallel off`).
- Arm Q0 (broken baseline 9/10 + `GGML_CUDA_Q8_1_CACHE=0`): DETERMINISTIC x3 clean. The q8_1 activation cache (ON by default, `ggml-cuda.cu:1808`) is bug #1; its eviction-safety argument assumes single-stream pool reuse, which does not hold under the fork's concurrent streams / split schedules.
- Fix #1 applied: `ggml_cuda_q8_1_cache_enabled()` default flipped to off (env opt-in `=1`), comment documents the stream-safety constraint. Rationale: fork-measured perf-neutral on this rig. Rebuilt llama-cli + llama-server (build 11060 tree + port + fix).
- Arm MTP2Q0 (MTP2 + cache off via env, old binary): r1/r2 clean, r3 flipped (`2d1f4ed5`) - a second, MTP-present, intermittent race exists (bug #2).
- Arm FIXB (baseline, new binary, no env): NONDETERMINISTIC 3/3 unique - burst window. Arm FIXB2 (identical, 30 min later): DETERMINISTIC x3 clean. Bug #2 is intermittent at config level, unlike the always-on cache bug. Arm FIXMG (MTP + `GGML_CUDA_DISABLE_GRAPHS=1` + fix): still nondeterministic - HIP graph replay exonerated for bug #2.
- Bug #2 suspect (source-read): concurrent-streams join/fusion hole. The planner (ggml-cuda.cu ~4744-4896) forks Q/K/V (`attn_norm` fan-out 3, `nrows<=1` i.e. decode-time) onto parallel streams; at the join, main stream waits join_events. If kernel fusion consumes the join node (the `i - prev_i > 1` fused path at ~4448), join events are never recorded/waited and the NDEBUG build silently maps unknown nodes to stream 0 (`stream_mapping[node]` default-inserts) - branch streams then race main-stream kernels. Fusion toggle: `GGML_CUDA_DISABLE_FUSION=1`. Matched 4-rep arms FUSEM (fusion off) vs FIXM2 (fusion on) queued to discriminate.
- PDL (`GGML_CUDA_PDL`, common.cuh:1740s) exonerated: Hopper-gated, inactive on gfx906.
- Arms FUSEM (MTP2 + `GGML_CUDA_DISABLE_FUSION=1`, 4 reps): NONDET (2 unique). FIXM2 (MTP2, no env, 4 reps): NONDET (3 unique). Fusion exonerated. Concurrent-streams planner exonerated entirely: it is opt-in via `GGML_CUDA_GRAPH_OPT=1` (default off, also requires graphs) - never active in any arm.
- Arm MTP1 (`n_max=1`, minimal 2-token draft/verify loop, 4 reps): NONDET (3 unique, two clean `9d4badea`). Bug #2 sits in the basic draft/target interleave, not the deeper draft chain.
- Bug #2 synthesis: intermittent (roughly half of MTP runs flip; one no-spec burst FIXB 3/3 unique immediately after rebuild - cold Vulkan pipeline cache window suspected as amplifier; FIXB2 warm rerun clean). Values correct when clean (MTP clean reps bit-match no-spec reference). Excluded so far: q8_1 cache, HIP graphs, kernel fusion, concurrent streams, PDL, CPU threadpool (8/8+poll0 in all MTP arms). Port wiring uses the EAGLE3/DFLASH-proven ctx_other borrowing pattern; port's speculative.cpp delta is one line. Remaining suspects: cross-backend async handoff in the mixed HIP/Vulkan/CPU pipeline under spec-decode scheduling (draft context GPU traffic raises the race rate), upstream MTP loop interaction, or fork's HIP copy paths. Needs phase-3 instrumentation (per-op tensor hashing or API logit capture) to pin the racing pair.
- Bug #1 status: FIXED (default-off) and verified - no-spec lane deterministic across all post-fix arms except the one cold-window burst; upstream bit-parity holds (`9d4badea`).

### E141.9 BUG #2 ROOT-CAUSED: GDN chunked prefill kernel
- GDN0 arm (GGML_CUDA_GDN_CHUNK=0, MTP2 lane 8/8+poll0, 4 reps): DETERMINISTIC, all == 9d4badea
- Same lane with chunk ON (default): MTP1/MTP2/FIXM2/FIXMG/FUSEM flipped ~half of ~20 runs; FIXB burst (3/3) also chunk ON
- Conclusion: bug #2 lives in fork-only ggml/src/ggml-cuda/gated_delta_net_chunk.cu (+330 lines, ON by default, engages prefill n_tokens >= 2*CS)
- Next: read kernel, find defect (suspect tail/OOB/uninit read at n_tokens % CS != 0), fix, verify MTP2 x6
- PDL exonerated definitively: GGML_CUDA_USE_PDL never defined in build config (grep CMakeLists + cache: absent) -> all PDL code compiles out
- hip.h:177 pins __CUDA_ARCH__=1300 for all AMD archs (would pass the >=900 Hopper gate); irrelevant while PDL not compiled in
- Chunk vs recurrent kernel diff: indexing/state offsets identical (K=1 in chunk path); recurrent kernel upstream-clean
- In-kernel suspects (deterministic-wrong at worst, cannot explain intermittence alone): fork-added GCN DPP reductions (s_nop hazard counts, ds_swizzle SWAP,16, __shfl_xor_sync(0xffffffff,r,32,64) = 32-bit mask on wave64); true intermittence likely cross-device input timing, DPP asm exposed by chunk burst-read pattern
- Decision: default GDN chunk OFF (env opt-in), mirrors q8_1 cache fix; perf-neutral per E138 (GDN 7.6% of fill, 2% TG)
- Fix applied: gated_delta_net.cu chunk_enabled default flip; rebuild + MTP2 x6 verify next

### E141.10 BUG #2 FIXED + VERIFIED
- Fix: GDN chunk default OFF (gated_delta_net.cu chunk_enabled, env GGML_CUDA_GDN_CHUNK=1 opts in)
- FIX2 arm (MTP2 lane, 8/8+poll0, NO env, new binary): DETERMINISTIC x6, all == 9d4badea
- Lane that flipped ~50% across ~20 arms (chunk on) now stable at stock defaults
- Both nondet sources now default-off with env opt-in: GGML_CUDA_Q8_1_CACHE, GGML_CUDA_GDN_CHUNK
- Next: placement tuning (fill GPUs), PP16384 only after that, then adaptive MTP + launcher
