# JOURNAL 2026-09-24

## E178 - upstream sync through a02c7f58c and validation preparation

User directives:
- integrate all upstream changes into the fork on master
- make a new build, use bench tools, and keep evidence in the repository logging folder
- validation regime: temp-0, PP16384, and TG1024 with `swift_llama_start-q6_q8_q4.sh`
- do not delegate
- notify before changing any running server
- prepare validation, but do not start it

### Integration
- Starting fork HEAD: `0d5326147`.
- Fetched upstream `origin/master` through `a02c7f58c` (106 upstream-only commits at fetch time).
- Existing uncommitted fork work was checkpointed before the sync.
- Merged `origin/master` on `master` without conflicts.
- Merge result: `7887b8887`, version `0.5.0-dev`, build 11497.

### Build
- Fresh build directory: `build-sync0924`.
- Command: `BUILD_DIR=$PWD/build-sync0924 FRESH=1 JOBS=16 ./build-dflash-novega.sh`.
- Result: PASS. Full build completed, `llama-server` is a valid ELF, and the expected gfx906 FATTN/MMQ/TOPK/GRAPHS tuning flags are enabled.
- Transcript: `bench/logs/build-sync0924.log`.
- Environment pins: `bench/logs/{git,rocm,system}-sync0924.txt`.

### Validation prepared, not run
- Wrapper: `bench/run-sync0924-validation.sh`.
- The wrapper refuses to run while any `llama-server` process exists. It does not stop or replace production.
- It launches `build-sync0924` through `swift_llama_start-q6_q8_q4.sh` using only `BIN`, `LD_LIB`, and `LOG_DIR` overrides, so model, split, KV, context, speculative decoding, and sampler settings remain launcher-exact.
- It runs `bench/cmp-client.py` with `FILL1=16384` and `TG_N=1024`. This covers server smoke/listen, temp-0 reproduction before and after the lane, PP16384, and TG1024.
- Planned evidence directory: `bench/logs/sync0924-swift-q6q8q4/`.
- Validation status: PENDING by user request. The production Swift server on port 8009 and the CPU embedding server on port 8010 were not changed.
