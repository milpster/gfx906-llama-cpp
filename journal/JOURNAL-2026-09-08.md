# Journal 2026-09-08

## E131 - PR + journal audit: untried opportunities (user request)

Sources: GitHub API (73 PRs merged upstream since 09-03 sync + open-PR
scan), journals E1-E130, README survey table, fork source greps.
gh CLI unauthenticated on this host; used REST API + patch-diff
fetches instead.

### Verified in fork source (the top find)

Upstream #28068 "models: fix GDN normalization from max to rsqrt"
(merged 09-06, AFTER our 09-03 sync) replaces `ggml_l2_norm` with
`build_gdn_l2_norm` (= rms_norm(x, eps/n) scaled 1/sqrt(n)) in the
GDN q/k conv norm of qwen35/qwen35moe/qwen3next/kimi-linear/kimi-k3/
bailingmoe3. CORRECTION (same day, E132 prep): the PR diff fetch was
head-truncated - upstream master DOES fix qwen4exp.cpp (lines 942-943
build_gdn_l2_norm, raw-file verified). Our fork at 941-942 still has
`ggml_l2_norm` (pre-09-03-sync state). Our production model
(Qwen3.8-27B) IS qwen4exp. => port = mirror upstream master text
exactly (helper + 2 call sites); applied in worktree exp/gdn-norm,
diff blob-identical to upstream (models.h 9b87a40d5..29e56d881).
Quality/correctness class: sha WILL move, PPL delta unknown. Trial =
PPL lane (vs i1's 1.3043, E122/E130 protocol) + temp-0 sha lane.
Both fork AND mainline-cmp measured pre-fix, so both PPL rows shift
if adopted. NOT TRIED yet - user go needed (GPU window).

### Other untried candidates found (not yet trialed)

1. #28302 server checkpoint eviction fix (merged 09-08): old code
   evicts "too close" checkpoints even when list not full -> short
   chat prompts degrade checkpoint density; also dedupes same-n_tokens
   checkpoints. We run --checkpoints 30 in prod. Server-side only,
   no numerics. Cherry-pick candidate.
2. #27044 (open) mmq.cu: src1_q8_1 pool sized from ne11 instead of
   ne12*n_expant_used on the MoE ids path - our verify hot path runs
   MMQ-for-ids every round. Correctness class (pool undersize).
   Related: #28383 (open, "mmq moe oob fix", on E121 watch list).
3. #28587 (open) DFlash + mtmd: skip pos-pinned embedding windows
   (M-RoPE images) when seeding draft cache - fixes "failed to decode
   mtmd chunk with DFlash". Our exact config class (DFlash2 + CPU
   mmproj). Trial needs an image-input repro.
4. #28387 (merged) ggml sched: removes capacity-forced extra splits
   -> fewer cross-device handoffs possible in our 3-way pipeline.
   Rides at next sync; lane-check then.
5. #28390 (merged 09-08, was triaged pre-merge as VRAM-only): now
   ALSO changes defaults - mmproj device follows --device,
   --spec-draft-device default follows --device. Our launchers pass
   explicit flags (--no-mmproj-offload, --spec-draft-device ROCm0),
   but re-verify additive semantics at sync.

### Watch list adds (correctness class, no local repro)

#27530 (K/V + recurrent state cleanup after failed restores - GDN
state hygiene, we use checkpoint restores), #28510 (tensor split
drift fix - we pin -ts 35,20,45), #28523 (gguf-py sign-loss on wide
tensor quantization - our requant/splice toolchain), #28208 (convert
writes explicit recurrent_layers for Qwen3-Next/3.5 - GGUF metadata,
check at next model re-convert), #27332 (vulkan density gate for
MUL_MAT_VEC_ID - 3080 decode MoE; marginal, 3080 not the bottleneck).

### Confirmed dead / no-op for this rig (do not re-lane)

- #28475 mmid/mmf race fixes: ggml_cuda_syncwarp is a NO-OP under
  GGML_USE_HIP; 3080 is Vulkan. Zero local effect, rides at sync.
- #27870 f16 fattn divergent barrier: fattn-mma path (tensor cores),
  VIIs run fattn-tile/vec. Not our kernel family.
- #24546 MoE MMQ N-tiles: merged 09-07 then REVERTED 09-07 (#28551);
  recreated as open #28552, still RDNA3-targeted. Still dead here
  (96.9pc MMQ cycles already J=64 fb=0).
- #26705 branchless Q4_K/Q5_K MMVQ: drafter-MMVQ closed by E88/E120
  correction.
- #28576 MFMA fp32 accumulation: gfx906 is GCN, not MFMA.
- #25557 Q8_0 full byte range: format divergence + full requant;
  revisit only if upstream adopts.
- #28569 -sm tensor for qwen4exp (open): we run -sm layer.
- #28532 server throughput-priority: single-user latency rig.

### Fork-internal untried items resurfaced from journals

- E128 mirror-OOM crash: known fork bug (mirror alloc fail +
  --spec-draft-device = hard crash at sched_reserve). Fix candidate
  already designed (fall back to pre-mirror cross-device draft
  graph). Not implemented. Blocks mirror on tight-fit lanes (v2/L).
- E120.3 option (a): -np 2+ throughput serving - listed, never tried
  (needs VRAM re-fit or 250k pool share).
- E123 optional VEGA_TUNE_MMQ=0 KLD ablation (doc value only).
- NEXT-LANES verify lanes (fa1s-auto250b, native250) still pending
  user GPU window. Launcher commit (carries -c 257000) pending go.
- E80 Q8_0-drafter@250k crash root cause still parked.

### Sync hygiene note

73 upstream merges since 09-03 (95ef7fc16). Notables riding next
sync: #28386 version 0.4.0, #28364 version.h.in refactor, #28437
--log-jsonl (ops QoL), #28177 peer-batch-size env removal, #28604
revert of HIP prop.integrated (#24233) - host has an APU iGPU
(card4); we pin devices explicitly, expect no effect, verify at sync.

No lanes run this session (investigation only). No commits (user go
required per AGENTS.md).

## E132 - T1 GDN-norm port trial (#28068 -> qwen4exp), 2026-09-08 evening

User-approved campaign (TRIALS-2026-09-08.md), regime: pp16384 +
fill 120k + temp-0 sha + PPL @120k ctx.

Port: upstream #28068 build_gdn_l2_norm (models.h helper + qwen4exp
call sites 941-942), worktree exp/gdn-norm, blob-identical to
upstream master (models.h 9b87a40d5..29e56d881). Build build-gdnnorm
@JOBS=11 (user cap), BUILD-OK, tune flags verified in cache.

Window attempt 1 CRASHED both lanes identically at PP16384:
ggml_cuda_pool_leg::alloc -> ggml_cuda_error -> ggml_abort, client
"no progress in PP stream"; load-time warning had shown a failed
248 MiB ROCm0 reserve. Environmental (identical on stock binary),
class = E120.1 (ROCm0 slack exhausted, mmproj on GPU). Fix:
EXTRA += --no-mmproj-offload (CPU mmproj; vision tensor not in the
text path -> sha-neutral). Attempt 2: both lanes clean, window DONE.

Lane A/B (i1-Q6_K @250k, ts 35,20,45, q8_0-V, dflash n4, mirror
parity env, CPU mmproj):

| side | pp1 | fill1 120k | tg @depth | acc | sha | repro |
|---|---|---|---|---|---|---|
| e132b stock | 402.1 | 261.3 | 13.4 | 0.653 | e54019ff6b42 | ok |
| e132a patch | 402.1 | 261.8 | 13.6 | 0.657 | e54019ff6b42 | ok |

PERF + ACC PARITY, temp-0 sha UNCHANGED (canonical). Patch is
numerics-neutral at greedy resolution. Binary identity proven
(qwen4exp.cpp edited 16:54:53 < object compiled 17:05:02; server
build 10955 f4a3841fa = worktree).

PPL @120k ctx (perplexity-gdnnorm.sh: -c 120000 -b 1024 -ub 384,
f16 KV, ts 35,20,45 -sm layer, full corpus -> 2 chunks):

| side | chunk1 | chunk2 | final +/- sigma | wall |
|---|---|---|---|---|
| stock | 11305.8863 | 126.9515 | 126.9515 +/- 2.63684 | 36:05 |
| gdn-norm | 11309.7784 | 126.9744 | 126.9744 +/- 2.63737 | 29:32 |

Delta 0.018% << sigma: A/B tie. T1 VERDICT: numerics-neutral on this
model end-to-end (greedy sha + PPL). No local adoption action -
upstream master already carries the change, rides free at next sync;
worktree exp/gdn-norm kept as reference.

OPEN ANOMALY (new finding, user flagged): PPL @120k ctx ~127 vs
1.3033 @4096 (E122, same corpus + same binary class). Chunk 1 ~11.3k,
chunk 2 ~127. Yet the server generates canonical-sha text with
acc 0.65 at the same 120k depth -> model math fine in serving mode;
perplexity-mode deep-ctx eval diverges. Mainline control launched
(bench/ppl-mainline-120k.sh, build-stock c5a5535e6, pre-#28068,
identical protocol): discriminates upstream-wide vs fork-side.
Result appended below when done.

## E133 - T2 checkpoint eviction trial (#28302), 2026-09-08 night

Port: exact upstream #28302 patch in worktree `/home/srcds/dev/uf3-wt-ckpt`, branch `exp/checkpoint-eviction`, base `f4a3841fa`. The patch gates checkpoint min-step eviction on a full list and supersedes an existing checkpoint at the same `n_tokens`. Diff is 17 additions and 1 deletion in `tools/server/server-context.cpp`.

Verification: `build-ckpt/bin/llama-server` rebuilt successfully with `-j11`; `git diff --check` passed; focused `unit/test_metrics.py::test_metrics_prompt_processed_and_cached` passed. Diagnostics have no errors and only unrelated pre-existing clang-tidy warnings outside `create_checkpoint`.

Short-chat rewind probe (`--ctx-checkpoints 30 --checkpoint-min-step 8192`, Qwen3.8 production target model):

| side | restored checkpoint | edit prompt_n | edit cache_n | edit prompt_ms | output SHA |
|---|---:|---:|---:|---:|---|
| stock | 348 | 543 | 348 | 3179.667 | 9c5ab258db76 |
| patch | 868 | 23 | 868 | 790.869 | 9c5ab258db76 |

The patch removed 520 prompt tokens of redundant rewind work (95.8%) and cut this edit's prompt time by 75.1% (4.02x). Stock emitted 17 `erasing context checkpoint too close` traces; patch emitted zero and logged `superseding context checkpoint at n_tokens = ...`. Identical regeneration matched on both sides and processed 4 prompt tokens.

Production deterministic A/B (i1-Q6_K, 250k context, ts 35,20,45, f16K/q8_0V, DFlash2 n4, mirror output, force-convert FATTN, CPU mmproj, PP16384, fill120k, greedy TG1024):

| side | pp1 | fill1 120k | tg @depth | acc | TG SHA | repro SHA |
|---|---:|---:|---:|---:|---|---|
| e133g-stock | 403.8 | 263.3 | 14.3 | 0.679 | bc1f64f15ce8 | e54019ff6b42 |
| e133g-patch | 407.7 | 264.7 | 14.1 | 0.679 | bc1f64f15ce8 | e54019ff6b42 |

Both sides cached 119999 tokens at TG depth and passed the before/after repro gate. Deltas are +1.0% PP, +0.5% fill, and -1.4% TG, all neutral at lane resolution. An earlier temperature-1 pair measured stock 13.4/0.657 and patch 12.3/0.631 TG/acceptance; it did not prove equal token paths and was superseded by the deterministic hashed lane. User confirmed thermals were not an issue. An interrupted `e133-patch2` attempt produced no result and its orphaned server was stopped.

PPL: not run. `llama-perplexity` does not execute `server_context_impl::create_checkpoint`, so PPL cannot exercise this server-only patch.

T2 VERDICT: KEEP. #28302 fixes a reproduced checkpoint-density regression with no numerical or production performance regression. No commit made; retain the worktree for explicit user approval or take the merged change at the next upstream sync.

## E134 - T3 mmq MoE OOB host-padding trial (#27044 + #28383 hybrid), 2026-09-09

Worktree `/home/srcds/dev/uf3-wt-mmq`, branch `exp/mmq-oob`, base `f4a3841fa`. Full #28383 port was measured first and REJECTED: production PP 403.8->346.1, fill 263.3->237.5, TG 14.3->13.9; microbench MUL_MAT_ID q6_K n=8 lost 10-30% - the five device hot-loop clamps (2 y-staging + 3 ids) cost too much on gfx906.

Adopted hybrid: (a) #28383 small-J floor in `ggml_cuda_mmq_get_J_max` (mmq.cuh) so tiny flattened-row counts still get a valid J; (b) #27044 flattened-row q8_1 pool sizing: allocate `J_max*sizeof(block_q8_1_mmq)` where `J_max = ggml_cuda_mmq_get_J_max(..., ne_get_rows)`, `ne_get_rows = ne12*n_expert_used` (mmq.cu); (c) NEW matching `ids_dst` allocation `ne_get_rows + J_max`; (d) remove all five device clamps -> branch-free loads; (e) NVFP4 scale tail padding retained. Padded tails are read-but-unwritten junk; both writeback paths already guard `j > j_max`, so no memset needed. Diff 16+/6- over `ggml/src/ggml-cuda/mmq.{cu,cuh}`; `git diff --check` clean; HIP build OK at -j11 (MMQ_VEGA_TUNE on, DUALACC/Q8LDR off).

Correctness: focused `test-backend-ops test MUL_MAT_ID type_a=q6_K` 3/3 OK incl. adversarial n=1. Perf microbench n=8: stock 808.11 vs patch 821.25 GFLOPS (128 experts), 965.01 vs 1.04 TFLOPS (32 experts) - regression eliminated.

METHODOLOGY CATCH: first bracket (e134h) compared the patch against a STALE stock binary (build 10949 / 29bece8d2 vs 10955 / f4a3841fa) and falsely showed a TG hash divergence (42e3d9e7a8fd) plus TG 13.6. Lesson recorded: verify `llama-server --version` build banners on BOTH sides before trusting an A/B. Fresh stock rebuilt at same commit (build-mmq-stock); e134h lanes invalidated.

Production bracket e134i (all build 10955 f4a3841fa, deterministic lanes, 250k ctx, f16K/q8_0V, DFlash2 n4, mirror, force-convert FATTN, CPU mmproj):

| side | pp16384 | fill 120k | tg @depth | acc | TG SHA | repro SHA |
|---|---:|---:|---:|---:|---|---|
| e134i-stock1 | 402.8 | 262.4 | 14.4 | 0.679 | bc1f64f15ce8 | e54019ff6b42 |
| e134i-pad | 406.6 | 265.1 | 14.2 | 0.679 | bc1f64f15ce8 | e54019ff6b42 |
| e134i-stock2 | 404.8 | 262.8 | 14.5 | 0.679 | bc1f64f15ce8 | e54019ff6b42 |

Neutral band: pp +0.7%, fill +1.0%, tg -1.7% vs stock mean; exact deterministic output identity.

PPL gate (user correction applied: WikiText-2 raw test split, NOT the debug-log corpus; `-c 10000` always): first attempt on /home/srcds/ai/ai/log.txt was aborted at 7/26 chunks and its log discarded. Official corpus `/home/srcds/ai/ai/wikitext-2-raw/wiki.test.raw` (1,290,590 bytes, archive sha prefix ef7edb...35a11, validated in prior session): stock 6.2847 +/- 0.04085, patch 6.2848 +/- 0.04085 over 29/29 chunks. Delta 0.0001 - tie at ~400x below sigma.

T3 VERDICT: KEEP. Uncommitted pending user approval. If upstream merges #27044/#28383 first, diff-check at next sync - upstream versions should subsume this hybrid.


## E135 - T5 forced-splits trial (#28387), 2026-09-09

Cherry-pick of merged upstream #28387 onto f4a3841fa (worktree /home/srcds/dev/uf3-wt-splits, branch exp/forced-splits): removes the split->n_inputs >= capacity forced-new-split heuristic, 2+/13- in ggml/src/ggml-backend.cpp, applies clean. Build -j11 OK, VEGA_TUNE config identical, both sides verified build 10955 f4a3841fa before the bracket.

Bracket: e135-stock1 397.3/260.7/14.1 acc 0.68 sha bc1f64f15ce8; e135-patch 404.2/264.3/14.5 acc 0.689 sha fa3432ce6ef6. SHA GATE TRIPPED -> user stopped bracket expansion, ordered log-only diagnosis:

- Split counts identical both sides (main 4, draft 2, mmproj 1); per-backend compute buffers byte-identical (722.71/282.21/722.72/198.22; deep 737.71/282.33/737.72/198.22); node counts identical (4327, 711/611). No split-inputs-capacity warnings anywhere -> removed heuristic is DORMANT in this workload; patch cannot change numerics here.
- Acceptance metric itself wobbles in stock: e134i-stock1 task104 acc 0.53135 (0.814,0.573,0.424,0.308) vs e135-stock1 0.53374 (0.810,0.578,0.428,0.312), both canonical sha. Draft acceptance is timing-nondeterministic even with stable greedy target stream.

Discriminator (user-approved single rerun): e135-patch2 = 400.3/263.8/14.1 acc 0.679 sha bc1f64f15ce8 -> first divergence was a nondeterminism fluke, NOT patch-caused. Orphaned server from the user-aborted e135-stock2 lane (PID 3398867) was handled by the lane script's port hygiene; GPUs verified idle after.

T5 VERDICT: DROP locally - provably dormant, perf neutral (all lanes inside the stock noise band), no sha effect. Rides at next upstream sync regardless (merged). Protocol amendment recorded: single-lane deep-TG sha divergence ~= 1/7 false-alarm rate; rerun once before diagnosing. Worktree uf3-wt-splits retained uncommitted pending user decision.

## E136 - T9 slot-save checkpoint preservation (#26004), 2026-09-09

2026-09-09 fresh PR sweep (100 open, cross-referenced vs all journals/TRIALS/dead-list: no duplicates; #27210/#27694/#28391 already covered; #26004/#26001/#28616 genuinely new). #26001 GDN chunked prefill found HW-gated (BF16 TC, gfx906 FP16-only WMMA) -> spawned T10 plan (FP16+rescale port) instead of a lane.

T9 trial: #26004 onto exp/checkpoint-eviction (stack with T2/#28302). 4/5 hunks clean, hunk #1 (save/load_slot_checkpoints helpers, SCKP magic appendix format) hand-applied at line 2358; diff 213+/3- (server-context.cpp + test_slot_save.py). Build -j11 OK, banner 10955 f4a3841fa.

Evidence: (1) focused pytest test_slot_save.py 9/9 PASSED incl. new test_slot_restore_preserves_context_checkpoints (tinygemma3 SWA preset, asserts post-restore rewind reuses n_live tokens). (2) T2 regression probe e136-stack: edit prompt_n 23, cache_n 868, 797.666 ms, output SHA 9c5ab258db76, regenerate matches - #28302 behavior fully intact under the stack. (3) Lane e136-stack2: canonical bc1f64f15ce8/e54019ff6b42, acc 0.679, 398.7/260.7/14.8 - neutral perf. First lane e136-stack was a FLAKE (tg_n 691 early stop, acc 0.657, sha c8b366fc47f0; patch only touches the slot-save FILE path which the lane never invokes - dormant code cannot cause it; canonical on rerun). Nondeterminism flake tally: ~2/14 deep lanes now; rerun-once protocol reaffirmed.

T9 VERDICT: KEEP. Turns save->restore->divergent-prompt from a ~455 s full re-prefill into a checkpoint rewind on our hybrid/recurrent model. Uncommitted in uf3-wt-ckpt pending user approval.

## E137 - T10 Phase A' GDN WY-chunked port for gfx906, 2026-09-09 (in progress)

SETUP: worktree uf3-wt-gdnfp16 branch exp/gdn-chunked-fp16 = f4a3841fa + PR #26001 series
(git am --3way, 6 commits 9d37d5504..81615425f, one conflict in gated_delta_net.cu resolved:
kept BOTH includes - fork E107 mx-chunk dispatch restored verbatim + upstream's own routing).
PR head pinned 5eb430657ad (updated 2026-09-09T14:55Z, still open).

KEY DISCOVERIES (premise corrections):
1. Fork already has E107 mx chunk port (gated_delta_net_chunk.cu, GGML_CUDA_GDN_CHUNK env,
   Sep 3): bit-identical, perf-NEUTRAL - it tiles the SAME sequential recurrence. #26001 is a
   DIFFERENT algorithm (WY-representation, reassociates recurrence into per-chunk matmuls).
   E107's "compute-dominated S_v^2=16384 MAC/token/head" finding = exactly the T10 thesis.
2. PR final state (commit 4c883b538 "converted to FP16 most places"): all TC GEMMs already
   fp16-in/fp32-acc; H-state AUTHORITATIVE in fp32 registers h_regs (fp32 accum preserved,
   fp16 only as per-chunk WMMA operand snapshot s_hfp16). = Our planned A' precision profile
   already upstream. No rescale work needed for A'.
3. gfx906 has NO matrix units (no HMMA/MFMA/WMMA hw; rocWMMA needs gfx908+; ROCm 6.1 ships
   no nvcuda::wmma shim - verified /opt/rocm-6.1.0 has none; lightning-indexer.cu guards all
   wmma under !GGML_USE_HIP with "TODO rocWMMA"). The 26.8TF FP16 = packed half2 vector FMA
   (2x FP32 rate). => A' GEMMs = SOFTWARE warp tiles (fp16 inputs, fp32 accum v1).

IMPLEMENTED (uncommitted on exp/gdn-chunked-fp16):
- chunk_gated_delta_net.cu: GDN_SW_GEMM path for HIP gfx906 - 3 software helpers replacing
  the WMMA/mma tiles: cgdr_gemm_abt_16 (A@B^T 16x16xBK), cgdr_gemm_ktv (Vnew^T@Kch into
  v-major s_hdelta), cgdr_gemm_qkv (qk@Vnew into row-major s_hdelta). 32-lane mapping 2rows
  x4cols, no cross-lane ops; wave64 lanes 32-63 recompute identical tile (benign). fp32 accum
  (hfma2 packed variant deferred - measure first). gfx906 exempted from GDN_USE_GGML_MMA;
  GDN_TC_AVAILABLE via GDN_SW_GEMM.
- gated_delta_net.cu: host gate ggml_cuda_should_use_chunked_gdn now allows
  cc == GGML_CUDA_CC_VEGA20 (0x1000906) + runtime A/B env GGML_CUDA_DISABLE_GDN_CHUNK
  (upstream removed their env in final commits; re-added, "0"/unset = on).
- Launcher config: BV=32 NT=256 OCC=4, ~30KB smem (fits 48KB default; no opt-in on Vega20).
  preqk kernel: 64-thread wave64 block calls abt_16 (redundant half benign).
Next: build build-gdnfp16 -j11, gates: test-backend-ops GDN on/off, microbench, PPL, lane.

## E137 continued - T10 A' verdict: NO-GO for Phase C, 2026-09-09

Gate 1 PASSED: test-backend-ops GATED_DELTA_NET on ROCm0 (both VII, cc 0x1000906 via
HSA_OVERRIDE): 53 OK / 0 FAIL; 30 chunked engagements (GGML_CUDA_GDN_WY_TRACE).
Bug found+fixed on the way: host pass defines GDN_USE_GGML_MMA (cannot see __gfx906__)
=> 2D (64,4) block, device flat branch read threadIdx.x only => 3/4 threads invisible
=> NaN. Fix: linear tid = threadIdx.x + threadIdx.y*blockDim.x. Also note: test-backend-
ops needs LD_LIBRARY_PATH=<build>/bin, op name is GATED_DELTA_NET not GDN; -b ROCm0.
--list-devices aborts free():invalid-pointer on ALL builds incl stock (fork quirk, avoid).

Gate 2 microbench (perf, head_size=128, chunked ON vs OFF):
- v1 tiles (fp32 accum, per-load converts): 105 GFLOPS vs recurrent 350 = x0.30
- v2 tiles (__hfma2 packed, fp32 flush every 16 chains, half2 vec loads): 174-182 GFLOPS
  (H=32) / 49-50 (H=4, occupancy collapse 16 blocks/60 CU) = x0.50-0.56. All correct.

DIAGNOSIS (structural): recurrent kernel 350 GFLOPS = 2.6% of FP32 peak -> the op is
latency/dependency-bound, NOT throughput-bound. #26001 monetizes hardware mma (1 instr
per m16n16k16); without HMMA the smem-staged chunk pipeline (3 GEMM stages x T/16 chunks
x ~6 syncthreads each + global->smem->reg staging) cannot amortize. Fork recurrent kernel
is register-resident w/ DPP butterfly - exceptionally strong baseline (E107 bit-identical).
Packed-fp16 2x rate is irrelevant when neither kernel is compute-bound.

T10 A' VERDICT: NO-GO for Phase C (rescale). Lane skipped by diagnosis: 0.5x op-level on
GDN layers can only regress fill120k; no non-GDN upside exists in the patch. Worktree
uf3-wt-gdnfp16 kept uncommitted (branch exp/gdn-chunked-fp16) as reference: correct,
engaging, documented software-tile implementation for any future CDNA move (MI300x has
the AMD_MFMA_AVAILABLE path in ggml_cuda_mma ready). #26001 stays watch-listed: rides at
sync only if we change hardware.
Durable learnings: gfx906 has NO wmma/HMMA (rocminfo shows gfx900 VII x2, override
9.0.6); lightning-indexer TODO comment confirms; ROCm 6.1 ships no nvcuda shim.

## E138 - post-T10 opt-idea triage (profile = synthesis, not new tooling), 2026-09-09

User asked to derive opts from E137, approved "1 then 2" (profile -> micro-chunk).

PROFILER DEAD-END REDISCOVERED (should have hit journals first): rocprof attach/launch
segfaults were already burned in E91 + E119.1.4; rd-trace exists BECAUSE of that. This
session re-burned it 4x (rocprof-attach inject fail; rocprofv3 launch SIGSEGV in
common_params_fit_impl with custom xnack libs, stock libs, AND single-GPU). bench/
prof-kernels.sh kept (reusable rocprofv3 driver) but the profiler route on this runtime
is CLOSED for good. rd-trace duration+name extension sketched, NOT built - unnecessary:
JOURNAL-2026-09-05 E120.1-E120.3 already holds the decomposition (TG fingerprints, busy%,
65%-host-in-HSA-syncs, ts rebalance VRAM-walled, 35/20/45 = unique fit at 250k).

IDEA-1 ANSWER VIA ARITHMETIC (existing E137 microbench recurrent-column + E120 lane data):
- GDN prefill share of fill120k: 1745us/layer-ubatch (T=384,H=48 interp) x64 x313 ubatches
  = 111.7ms of 1463ms/ubatch = 7.6% of fill wall. Perfect 2x GDN prefill = +7.6% fill.
- GDN verify share of TG round: ~48us/layer x64 = 3.05ms of ~150ms round = 2.0%.
  Micro-chunk CS=5 ceiling = +2% of round (< 0.3 t/s). DEAD.
- Idea 3 (fill pair-pipelining) ceiling = fraction of the 7.6%, realistic +3-5% fill.
  Below campaign threshold. DEAD.
- Idea 4 (TS rebalance): already closed E120.3 (VRAM-walled). DEAD.

CONCLUSION: all four derived ideas are sub-threshold. The E137 "latency-bound" finding is
real but the GDN ops occupy too small a wall share for chain-shortening to matter. The
REAL unmined levers remain E120.2's parked list: (a) -np 2+ multi-request throughput
serving, (b) speculative.cpp draft/target overlap restructure (invasive), (c) ctx-vs-TG
trade (contradicts product goal). Nothing kernel-side left at prod shapes (E120.1/E120.2
closed config, kernel-family, dispatch-table, host-machinery spaces).

## E139 - mainline sync integration (f4a3841fa + upstream 93 + T3 + T9 -> master), 2026-09-09

User: "take our newest and best branch and combine it with the newest mainline llama cpp and
put that all on master once tested" + "copy the latest working build into our project folder
and adapt the start scripts" + later: mainline build in ~/dev/llama.cpp for comprehensive
fork-vs-mainline A/B (speeds, PPL, KLD; PPL/KLD ctx = 10k).

Integration branch sync/upstream-2026-09-09 @ c389ecc99:
- merge origin/master (434ddbbc0, 93 commits, 0.3.0-dev -> 0.4.0-dev): ONE conflict
  ggml/src/ggml-cuda/fattn.cu (fork VEGA_TUNE tile-q8 natives vs upstream const-signature
  of ggml_cuda_fattn_kv_type_supported) - resolved keep-both. GGML_CUDA_FA_ALL_QUANTS is
  DEAD upstream (#28079 -> GGML_CUDA_FA_QUANTS list); build carries
  q4_0;q8_0;f16;bf16 pairs, covers prod shapes (f16-f16, q8_0-q8_0; mixed f16K/q8V goes
  convert path as before).
- merge exp/mmq-oob (T3, committed 469de3c3d "cuda: MoE MMQ OOB fix") clean.
- merge pr-26004 tip 06d9d0ff5 clean (server-context.cpp 120+ / test_slot_save.py 77+).
  Upstream already merged #28302 (5d806aa25) so T9 = just #26004 now; supersede-at-same-
  n_tokens + list-full min-step eviction both present post-merge. T9 worktree snapshot
  superseded (was older PR rev + #28302 dupes).
- #28387 (T5 DROP) came in via upstream merge - fine, dormant.
- #26001 (T10 NO-GO) still open upstream - not merged, not wanted.

Build build-sync0909: 0.4.0-dev build 11053 commit c389ecc99, VEGA_TUNE flags all preserved.

BREAKING FLAG: --no-mmap removed upstream (#26934 -> --load-mode none). Fixed
bench/run-checkpoint-eviction-probe.sh; lane-dflash.sh now version-detects per binary
( stock f4a3841fa rejects --load-mode ); start scripts pending (todo).

GATES:
- test-backend-ops GATED_DELTA_NET ROCm0: 7/7 OK.
- MUL_MAT / MUL_MAT_ID via tbo: CRASH rocblas Tensile PlaceholderLibrary loadPlaceholderLibrary
  SIGSEGV - PRE-EXISTING (pre-merge prod 29bece8d2 crashes identically; no historical green
  run of these ops in journals; gfx906+rocBLAS 6.1 Tensile family issue, tuned-lib dir does
  not fix for tbo shapes). Not a regression - real MoE/MMQ coverage comes via PPL+lane.
- test_slot_save.py: 10/10 passed (T9-era was 9/9; PR tip added 1 test).
- T2 probe e139-sync0909: edit 23/868/793.1ms SHA 9c5ab258db76, regenerate matches,
  370.985ms - BIT-EXACT vs T9 baseline (797.7ms). Rewind intact through upstream merge.
- PPL wikitext2 10k: RUNNING (log bench/logs/ppl-e139-sync0909-wikitext2-10k.log;
  e134 control 6.2847+/-0.04085).

NEXT: PPL -> lane A/B vs build-mmq-stock stock -> master -> copy build + adapt start
scripts -> ~/dev/llama.cpp mainline + comprehensive A/B.
