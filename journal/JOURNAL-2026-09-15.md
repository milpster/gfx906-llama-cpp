# JOURNAL 2026-09-15

## E170 - isolated upstream candidate A/B: #28873, #28896, #28874

User directives:
- "test all three one by one"
- "use our previous testing scheme and existing infrastructure"
- "keep a journal"
- "dont delegte"
- Production server is running on port 8009. Notify the user before testing. Do not touch it without an explicit stop/start decision.

### Starting state
- Source: `/home/srcds/dev/uf3_rocm6.1_llama.cpp`, `HEAD 0d95fa0f0` (`Merge upstream master through b6b003d2c`).
- Main worktree is dirty and must remain untouched except for this journal and any explicitly approved permanent harness update. Existing tracked change: `M bench/cmp-client.py`.
- Production binary/launcher remain `build-sync0909/bin/llama-server` and `2llama-start-iq6v_f16_f16.sh`.
- No candidate build or runtime test has run yet in E170.

### Candidates and isolation order
1. #28873: honor `LLAMA_STATE_SEQ_FLAGS_PARTIAL_ONLY` in KV-cache state write/read.
2. #28896: qwen4exp/qwen35 `RMS_NORM + MUL` fusion, manually adapted for fork-only NextN HC tensors and local tensor flags.
3. #28874: copy user inputs before cross-device inputs in `ggml_backend_sched_compute_splits`.

Each candidate gets a detached worktree at the same base commit and its own build directory. No stacking until all three isolated verdicts exist.

### Recovered testing scheme
- Same-commit controls are mandatory. E134 proved stale-baseline comparisons are invalid.
- Verify source commit and both `llama-server --version` banners before accepting an A/B.
- Production-shaped lane: `bench/cmp-client.py`, which records temp-0 repro before/after, PP16384 first-batch speed, one fill to 120k, greedy TG1024 at depth, DFlash2 acceptance, TG SHA, fitted context, and load time.
- Candidate order within a bracket: discarded warmup where needed, then interleaved baseline/candidate runs. Use a fresh baseline around each candidate and rerun one isolated SHA failure once before attribution.
- #28873 also uses `bench/run-checkpoint-eviction-probe.sh` plus `bench/checkpoint-eviction-probe.py` to measure rewind `prompt_n`, `cache_n`, `prompt_ms`, and identical-regeneration SHA.
- `bench/ab-mainline-0913.sh` is the closest current production-shaped launcher reference: Qwen3.8 i1-Q6_K, F16 mmproj, Q4_K_M DFlash2, ngram-mod + draft-dflash n4, mirror output, `force_convert`, `-b 16384 -ub 384`, F16 K/V, `-ts 35,20,45 -sm layer`, and `-c 130000` for a fitting benchmark lane.
- Full production server holds the GPU memory needed by any probe. Runtime lanes cannot coexist with port 8009; obtain an explicit stop/start decision before runtime work.

### Gates
- Build: exact release flags from `build-dflash-novega.sh`; candidate diff contains only intended source changes; `git diff --check` clean.
- Correctness: temp-0 before/after repro, cross-arm TG SHA, complete TG token count, checkpoint regeneration match for #28873.
- Performance: PP16384, fill120k, TG1024, DFlash2 acceptance, fitted context, load time; checkpoint payload/rewind timing for #28873.
- Keep only a reproduced benefit with no correctness, fit, or VRAM regression. Otherwise drop/watch.
- Final adversarial pass checks provenance, confounds, run ordering, cold/page-cache effects, and whether the changed path was actually exercised.

### Log
- 2026-09-15: Recovered the existing infrastructure and prior methodology. No GPU/server action performed.
- Created four detached worktrees, all clean at `0d95fa0f0feea088015a11532c64efd38940b986`: `/home/srcds/dev/uf3-wt-e170-base`, `/home/srcds/dev/uf3-wt-pr28873`, `/home/srcds/dev/uf3-wt-pr28896`, `/home/srcds/dev/uf3-wt-pr28874`.
- Applied upstream #28873 exactly in `/home/srcds/dev/uf3-wt-pr28873`: `src/llama-kv-cache.cpp`, 8 additions and 2 deletions. `git diff --check` passed.
- Serena/clangd diagnostics are non-authoritative in the detached tree because no compile database exists; reported the known `llama.h file not found` cascade. The isolated compile is authoritative.
- Clean baseline build command: `BUILD_DIR=/home/srcds/dev/uf3-wt-e170-base/build-e170-base FRESH=1 JOBS=11 TARGETS=llama-server ./build-dflash-novega.sh`. PASS; valid ELF linked.
- #28873 build command: `BUILD_DIR=/home/srcds/dev/uf3-wt-pr28873/build-e170-pr28873 FRESH=1 JOBS=11 TARGETS=llama-server ./build-dflash-novega.sh`. PASS; valid ELF linked.
- Both builds used identical cache flags: ROCm 6.1, gfx906, Vulkan ON, VEGA_TUNE FATTN/MMQ/TOPK/GRAPHS ON, fast math OFF. Both emitted the same pre-existing FATTN template division-by-zero warnings.
- Production server was not queried, stopped, restarted, or sent requests during source and build work.
