# Session journal - 2026-09-04 (TG improvement research: upstream survey + local levers)

Continues JOURNAL-2026-09-03.md. Research session, no code changes, no GPU
lanes (analysis only). Trigger: user request - improve TG output of
2llama-start-iq6v-dflash2.sh without meaningful PP loss; some ctx loss is
acceptable.

- E119 TG research: upstream deltas + PR sweep + local lever audit,
  2026-09-04:

  1. UPSTREAM SINCE SYNC (95ef7fc16..origin/master 8b4b3558f, 21 commits):
     no TG relevance. CI/UI/docs/SYCL/Metal only; ggml sync #28379 = symbol
     rename; #28177 removes dead GGML_CUDA_PEER_MAX_BATCH_SIZE defines;
     version bump 0.4.0. Nothing to cherry-pick; next sync rides free.

  2. OPEN-PR SWEEP (fresh, 2026-09-04, unauthenticated API):
     - #28391 (ggerganov, opened today, draft): default speculative config
       on (ngram-mod); --spec-type becomes additive/dedup; --no-spec-type
       new; --spec-default deprecated. Perf-neutral. WATCH for sync: our
       launcher passes explicit --spec-type ngram-mod,draft-dflash, so
       additive semantics are safe, but re-verify flag parsing on merge.
     - #28390 (am17an, today): single-device drafter meta-backend wrapper;
       saves VRAM only under -sm tensor. N/A (we are -sm layer).
     - #28333: zero MTP carrier at sequence start (correctness, MTP
       family). On next sync, check whether dflash carrier/mask init is
       affected (likely separate mechanism; low risk).
     - #27694: probabilistic drafter + rejection-sampling verify (simple +
       MTP). Acceptance-side semantics change = sha-breaking class; not in
       PR for dflash anyway. WATCH, do not port.
     - #28305: keep backend-sampling subgraph static across ubatches. We do
       not backend-sample for dflash2 (CPU lattice walk). N/A.
     - Merged-since-08-25 re-check: #27310/#27991/#28011/#27762/#28040 all
       already in via the 09-03 sync. Nothing new for TG.
     - #27173 RE-AFFIRMED REJECTED as a whole (D7, 08-26: multi-GPU
       thinking-heavy 3.4x regression report; our dflash already drafts in
       one decode). BUT two pieces are NOT covered by the D7 rejection and
       remain portable:
       a. LLAMA_SCHED_POOL (llama-context.cpp, self-contained, env-gated):
          per-shape scheduler + cached graph pool so alternating spec
          verify shapes (5/4/3/2/1) stop evicting each other's plans.
          Claimed ~+5 t/s at 146 t/s on 2x5090 (launch-bound there);
          expected smaller here. HIP-graph interplay must respect our
          carried d98c53b9c0 shape-mixed graph cache. Lane-gate sha+perf.
       b. Deferred catch-up (draft_mtp impl in the PR): merges the
          per-round catch-up decode into the first draft decode - one
          drafter eval per round instead of two. For us this needs a port
          into draft_dflash: at TG, process() injects the accepted-token
          rows (target-layer embeddings) as its own llama_decode, then
          draft() runs the noise-block decode. Merging halves drafter
          evals/round. Depth stays fixed, so D7's depth-behavior
          regression class does not apply. Gate: sha (batch-composition
          changes are in the documented nondeterminism class), acc, t/s.
       c. Its token-rollback state-overwrite bugfix: worth cherry-picking
          with (b) - same file family.

  3. ROUND-STRUCTURE FACTS (from journals, current build):
     - TG is machinery-bound, not kernel-bound: ts redistribution dead in
       every era (E105; 08-15 sweeps); decode sits ~2.5x above the
       weight-streaming floor (FINDINGS); MMQ/TOPK/GRAPHS together worth
       <= 0.4 t/s in TG (E53 bound).
     - Spec buys +67% TG (12.2 vs 7.3, E110 probe) and costs 8.6% PP +
       6.2% fill; the residual tax is ROCm0 SM contention drafter-vs-target
       (E110), i.e. "drafter placement/sizing" is the named open lever.
     - E46-era decomposition (9.0-era, STALE): draft-gen ~43 ms of ~217 ms
       round. The in-tree timer dur(b,g,a) still exists
       (common/speculative.cpp:3132, gen_perf gate) - a TG512 lane with -v
       refreshes this on the current build for ~zero cost. ALL further
       lever ranking should gate on that measurement (step 0).
     - D15 caveat still open: --spec-draft-override-tensor '.*=ROCm0' never
       fully pinned the draft graph (layers 3,4,5 landed on ROCm1/Vulkan1;
       E44 measured a 23-split draft graph with ~75 ms/round meta-sched
       delta at the 9.0 era). Whether the current build still splits the
       draft graph is unmeasured; the timer + a -lv 4 fit table would show
       it.

  4. LOCAL LEVERS, RANKED (ctx loss acceptable):
     0. INSTRUMENT FIRST: one TG512@120k lane with the dur(b,g,a) timers
        on the prod build; splits round time into catch-up/draft/accept.
        Decides the expected value of 2/3/4 below. No risk.
     1. Drafter execution placement (config + code, untested lane):
        variants = (a) finish the D15 pin so ALL draft compute sits on
        ROCm0 (removes cross-device splits from the draft graph), or
        (b) move the drafter to ROCm1/VK1 with reduced -c. Fit facts:
        drafter = 1090 MB Q4_K_M + 2048-token draft KV window + >=415 MiB
        draft compute (E50); ROCm1 real free ~840 MiB at 250k (E105 R0);
        -10k ctx frees only ~90 MiB on ROCm1 (20.7 KiB/token split, E89) -
        so variant (b) needs either a big ctx cut or a layer shift toward
        the freed ROCm0. PROBE (dual-gpu-context-balancing skill) before
        any lane. Mechanism cuts both ways (moves contention partner onto
        the 45% stage); the E119.0 timer decides if drafter latency is
        even on the critical path.
     2. SCHED_POOL port (small, env-gated; see 2a).
     3. Defer-catch-up port to dflash (medium; see 2b). If the timer shows
        catch-up inject >= ~15% of round time, expected +0.5-1.5 t/s.
     4. Depth rebracket: n_max 5/6 lanes AFTER any draft-cost reduction
        (Y-bracket 3/4/5/7 = 8.5/9.0/8.0/8.6 predates all current work).
     5. checkpoint sparsify: --checkpoint-min-step 16384 (or 24576) A/B.
        AB6 measured +0.65 t/s for checkpoints=0 (unshippable: breaks
        cross-request restore); density knob is min-step, not the count
        cap (K2). Shippable variant, expected small; D5's per-round
        spec_ckpt stash cost scales with draft depth.
     6. WATCH (no action): #27694 (sha-breaking), #28391 flag semantics
        on next sync, #28333 carrier init on next sync.

  5. RECOMMENDED AGAINST (measured dead or wrong trade; do not re-lane):
     -ctk q8_0 (E54-class: native tile -2.6 t/s TG, convert cost, quality
     philosophy, hybrid arch limits the KV-scan win); any -ts change at
     equal ctx (E105, 08-15 falsifier); pipeline on (E30/D5, np=1);
     CPU drafter (E47: wins shallow fresh TG, loses at depth, and the
     prefill replay tax is serial on CPU); checkpoints off entirely
     (AB6/AB8); GDN layers to VK1 (F6 RS-buffer stranding); drafter quant
     change (X1: Q4_K_M == Q8_0 acceptance-identical; Q8_0 crashes at
     250k, E80 open); full #27173 SPEC_CHAIN port (D7).

  6. Cross-check of TG reference points (do not mix protocols):
     TG1024@120k prod shape 12.0-12.3 (E112/E115 tables); TG512-t0 at the
     c=200192 A/B shape = 13.6 both builds (E118) - the delta is shape
     (ctx length, pipeline off, protocol), not build. Lane comparisons
     must hold cmp-lane.sh's protocol fixed.

  Deliverable: this entry. No code, no build consumed. Next concrete
  actions for a GPU window, in order: E119.0 timer lane (30 min), then
  whichever of steps 1-5 the split justifies. Commit of this journal entry
  pending user approval (AGENTS.md).

- E119.0 timer lane + draft-split evidence, 2026-09-04 (GPU window, agent
  run, standard lane-dflash.sh harness):

  LANE e119a0 (prod shape: ts 35,20,45, c 250000, f16K/q8_0V, ngram-mod +
  dflash n4, PP on, --no-mmproj-offload, force_convert): pp 371.2 client /
  fill120k 251.7 / tg1024@120k 11.7 / acc .658 / sha e54019ff6b42
  (canonical; tg vs E112's 12.2 = day/ngram variance, sha intact).
  dur(b,g,a) decomposition (in-tree timers, 512 draft rounds):
    - draft-dflash: 39.5 ms/round = 23% of the 171 ms round
    - catch-up inject (b): ~0 at TG - DFlash2 keeps its own KV aligned by
      construction; there is NO per-round catch-up eval. The #27173
      defer-catch-up port idea is MOOT for dflash (E119 step 3/2b dead).
    - ngram-mod: ~0 (12.4 ms total; fired 2x on replay spans, mean len
      24.5, all-accept) - free insurance, confirmed.
    - accept (a): ~0.
  Reconciliation vs E110's no-spec 7.3 t/s (137 ms/token @120k): round =
  target pass (~130-137 ms, batch-size-insensitive: 1 vs 5 verify rows
  same pass) + draft (~40 ms). NO hidden machinery tax; the remaining TG
  campaign targets are (1) the 130 ms target pass vs the ~42 ms
  weight-stream floor (KV scan + pipeline stage serialization + launches)
  and (2) the 39.5 ms draft round.

  DRAFT SPLIT EVIDENCE (load tables, -lv 4): draft weights pin works
  (ROCm0 1079.61 MiB, single buffer) but the draft graph still spans 3
  devices: draft compute 415 (ROCm0) + 371 (ROCm1) + 34 (Vulkan1) MiB,
  draft KV 30/10/10 MiB. Mechanism (code read): dev_layer map (ts split)
  assigns draft LAYERS across devices; KV follows dev_layer
  (llama-kv-cache.cpp:217), the -otd override only re-homes weight
  buffers. Plus llama_context::graph_reserve asserts n_outputs >= 1
  (llama-context.cpp:2493), so the draft reserve graph includes the
  borrowed output head (target output.weight lives on ROCm1), forcing an
  all-backends draft sched (the D15/E43 abort class). DFlash2 never uses
  the vocab head for drafting (lattice embeddings only) - an
  embeddings-nextn-only reserve + draft dev_layer pin would consolidate
  the entire draft graph on ROCm0. Ceiling if draft overhead halves:
  ~+1 t/s.

  FOLLOW-UP RUNS this window: e119g0 (GGML_CUDA_DISABLE_GRAPHS=1 lane,
  same shape) to size HIP-graph sensitivity of the 40 ms draft + 130 ms
  pass; rocprof TG-phase timeline (bench/rocprof-server-tg.sh, new twin:
  prod parity PP-on + ngram + no-mmproj-offload, port 8017) for kernel /
  gap / copy structure of a round.

- E119.1 window results: graphs neutral, KV-depth curve, rocprof TG-zone
  crash repeat, 2026-09-04 (agent run):

  1. e119g0 (graphs disabled, otherwise = e119a0): pp 389.4 / fill 253.5 /
     tg 11.8 / acc .658 / sha canonical; draft = 20212/512 = 39.5 ms/round
     - IDENTICAL to graphs-on (39.5). HIP graphs are TG-neutral on this
     rig; the draft round and target pass are not launch-bound.
     => #27173 SCHED_POOL port DEMOTED to ~zero expected gain. Its only
     remaining value would be a cheap confirm lane after any port.
  2. e119k1 (FILL1=16000, same shape): pp 376.3 / fill 361.4 @16k /
     tg 12.3 @16k depth (tg_n 388, acc .608 - prompt-class variance) /
     sha canonical. KV-depth curve: 12.3 @16k -> 11.7 @120k = ~5% TG cost
     over 104k of fill. The target pass is depth-INSENSITIVE.
  3. Combined pass characterization (batch-insensitive: 1 vs 5 verify
     rows same 130-137 ms; depth-insensitive: ~5% over 104k; graphs-
     insensitive: identical with graphs off): the ~130 ms target pass is
     FIXED-OVERHEAD-bound (kernel-chain latency + 3-stage pipeline
     handoffs), not bandwidth/KV/launch-count bound. Decomposing it needs
     a working TG-phase kernel timeline - blocked (see 4).
  4. rocprof TG attempt (rocprof-server-tg.sh, pmc-free file plugin,
     16k-fill protocol): server SEGFAULTED ~2 min in, mid first client
     task (E91 class, second occurrence of the TG-zone interposer
     fragility; E91's crash was also ~2 min in at the TG phase). The CSV
     captured the startup warmup (decode-phase dispatch mix: matvec /
     bcast / cpy / quantize_q8_1 / rms_norm dominate; no MMQ = no PP
     reached) but per-kernel DURATIONS are corrupted (avg "192 ms"
     matvecs, 126 ks timestamp window) - unusable for shares. The
     verify-regime PMU pass (planned since E56) stays blocked on a stack
     repair (rocprofiler 6.1 rebuild or an in-process round tracer);
     keep PP-only protocols for rocprof on this stack.
  5. Lane rows appended to bench/logs/lane-results.jsonl (e119a0/g0/k1).
     Rig left clean (no servers). rocprof-server-tg.sh kept as the
     protocol twin for the next attempt.

  REVISED TG LEVER RANKING (measured):
  1. Draft-graph consolidation (code, ceiling ~+1 t/s / +9-11% TG):
     draft = 39.5 ms/round = 23% of round, provably spread over 3
     devices (KV 30/10/10 by dev_layer, compute 415/371/34). Two-part
     fix: (a) pin draft dev_layer to one device (the -otd override only
     re-homes weight buffers; llama-kv-cache.cpp:217 follows dev_layer),
     (b) embeddings-nextn-only reserve for the dflash ctx (graph_reserve
     asserts n_outputs >= 1 at llama-context.cpp:2493; DFlash2 never
     uses the borrowed vocab head) so the draft sched can be
     single-backend without the D15/E43 output.weight abort. Gate: lane
     sha + dur(b,g,a) timers (expect draft ms/round to drop; if it drops
     to ~20 ms the depth rebracket n_max 5/6 becomes worthwhile again).
  2. Target-pass fixed overhead (77% of round): biggest pool, but needs
     a working TG-phase profiler first (E119.1.4 blocker). Candidate
     attacks once kernel/gap shares exist: kernel-chain fusion in the
     decode graph, stage-handoff batching, MoE MUL_MAT_ID batch<=5 path.
  3. Checkpoint sparsify (--checkpoint-min-step 16384): untested cheap
     lane, small expected.
  4. SCHED_POOL / defer-catch-up / #27173 chain: dead or ~zero on this
     rig (D7 + E119.0 b=0 + E119.1.1).
  5. KV quant beyond current f16K/q8_0V: no (5% depth effect only; E54
     native-tile penalty class).

  Deliverables this session: journal E119/E119.1, bench/rocprof-server-tg.sh
  (new), 3 lane rows, draft-split root-cause code read. No source changes,
  no build consumed. Commit pending user approval (AGENTS.md).

- E119.2 draft-head mirror implementation + rig-bisect trail, 2026-09-04
  (agent run, GPU window 2):

  CODE (in tree, uncommitted, env-gated LLAMA_DFLASH_MIRROR_OUTPUT=1,
  default off): give a single-device DFlash draft ctx its own device-local
  copy of the borrowed target output head (995 MiB on the draft device) so
  the draft graph never crosses devices. Files: llama-context.{h,cpp}
  (mirror alloc via meta-ctx + tallocr + host-staged copy + head/tail
  byte verify), llama-graph.{h,cpp} + llm_graph_params plumbing,
  models/dflash.cpp (two borrow sites prefer the mirror). Build:
  build-mirror (fresh, D11 recipe, flags verified = prod).

  EVIDENCE CHAIN (all lanes @prod shape, short protocol FILL1=16000):
  1. e119d1 probe: -devd ROCm0 aborts at draft ctx creation (E43 class,
     output.weight on ROCm1 not runnable by ROCm0-only sched).
  2. e119d2: -ot escaping bug (double backslash; note unanchored
     "output.weight" would also match attn_output.weight - anchor
     required). e119d3 PROBE (fixed regex): LISTENS - output.weight
     moved ROCm1->ROCm0 (995 MiB), draft KV single 50 MiB buffer.
  3. e119d4 full lane (-ot variant): warmup draft 29.2 ms/round vs 50.1
     baseline (-42%), warmup TG 18.1 vs 13.3 (+36%) - the consolidation
     mechanism is real. PP16384 231.7 (vs 401.8) = the moved head wrecks
     prefill (head GEMM cross-device after the ROCm1 stage) => config
     variant DOA under the no-PP-loss constraint; the mirror code is the
     fix (target untouched). Lane died at 82% fill: GPU Hang node-1 -
     LATER ATTRIBUTED TO THE USER-SIDE GPU ISSUE, not this config.
  4. m-series (mirror build, env on): warmup dflash acc 0/120 (prod
     build: ~.48). m4 instrumentation: mirror bytes verified OK at head
     AND tail; src type q6_K ne 5120x248320 nb/nbytes identical to
     mirror. Debug raw-prompt completions produce degenerate loops
     ("!!!!"/CJK repeats) - ON PROD TOO: raw-prompt quirk of this model,
     all such evidence VOID.
  5. Bisection of the 0-acc: e119c0/c1 (mirror build env off, then
     STASHED clean-source fresh build) BOTH 0-acc => patch and mirror
     exonerated. e119p0 (PROD binary, post-GPU-fix): ALSO 0-acc (0/263).
  6. VERDICT: the dflash drafter broke at the user's GPU-fix boundary,
     on every binary. Post-fix signature: target healthy (pp 381/fill
     367 @16k), draft rounds SLOWER (53.3 vs 39.5 ms), dflash acc 0,
     TG carried by ngram replay (lane tg 47 t/s acc .94 = replay
     artifact, sha off-canonical a73e540c7841, deterministic).
     Pre-fix same protocol: prod acc .608 (e119k1). Root cause of the
     rig state: outside the tree - user action item (what changed:
     driver reload / GPU reset / E80-class residue?). E80 history
     (drafter + fattn invalid-device-function family) is the closest
     precedent; crash-test-setdevice branch da5aa8f77 is the parked
     runbook if IMA-class symptoms appear.

  METHOD LESSONS (recorded): (a) raw /completion prompts on this model
  degenerate greedily regardless of build - never use them as numerics
  evidence; the lane protocol (corpus prompts + sha) is the only valid
  instrument. (b) two variables changed across the m-series (fresh
  builds AND the GPU fix) - the prod-binary post-fix control (e119p0)
  was the missing discriminator until it ran. (c) pkill -f patterns
  must never share a command line with launch strings containing the
  same text (self-match suicide, 3 occurrences this session).

  STATE: rig drafting broken pending user investigation; mirror code
  ready to lane-test the moment drafting works (expected: draft ~29
  ms/round, PP restored ~400 via untouched target, TG upside per d4's
  +36% warmup signal). Stash popped? NO - clean tree currently stashed
  as dflash-mirror-wip; pop before testing.

- E119.3 rig self-healed + MIRROR VALIDATED, 2026-09-04 (evening):
  1. RIG HEAL: e119r0 canonical short lane (prod binary) = sha
     e54019ff6b42, acc .608, tg 12.3 - IDENTICAL to pre-fix k1. The
     corruption was a transient GPU/driver state (wedged VRAM or
     post-fix residue), cleared by the evening's server resets + the
     vram-test exercise between 19:15 and 19:51. All garbage-output
     evidence (raw-prompt CJK/! degeneracy) was this state. The sanity
     ladder (CPU ref sane; 1xROCm0 35L sane; 1xROCm1 35L sane; late
     band 41L on ROCm1 sane; 1xVulkan 20L sane; 2xVII full sane;
     3-device VKMIN/10L/20L sane; ub384 sane; q8V+force_convert sane;
     c8192 sane; b4096 sane) all ran POST-heal - it mapped a ghost.
     Keep for the record: the ladder configs are valid health probes,
     and bench/vram-test.cu (+binary) is now permanent instrumentation
     (both VIIs PASS 14 GiB x 2 patterns). If corruption recurs: vram-
     test first, canonical short lane second (it caught this instantly).
  2. MIRROR VALIDATED (stash popped, build-mirror rebuilt):
     lane    config                    pp      fill16k draft ms/r  tg16k  sha
     e119r0  prod binary (baseline)    373.5   360.5   42.0        12.3   e54019ff (canonical)
     e119m6  mirror only               386.0   367.0   37.6        13.0   e54019ff
     e119m5  mirror+-devd (GAME ON)   344.3   331.9   32.4        14.8   e54019ff
     e119m7  mirror+-devd (GAME OFF)  404.7   388.2   28.8        15.8   e54019ff
     Acceptance profile identical across all (0.70/0.48/0.30/0.21):
     the mirror is numerically transparent. The m5 PP drop was the
     user's iGPU game (system-RAM bandwidth), NOT -devd: game closed,
     mirror+-devd dominates every metric (pp top-of-day-band, draft
     -31%, tg +28% @16k depth).
     User finding: iGPU game was also possibly the earlier "GPU edge"
     contributor and today's PP noise floor.
  3. e119m8 (mirror+-devd @120k full protocol) STARTED then stopped at
     user request - 120k validation deferred to a user-called mainline
     A/B session. rig left free.
  4. PENDING (user-gated): 120k-depth validation lane; game-free
     baseline re-anchor; depth rebracket n_max 5/6 (draft now cheap:
     28.8 ms/round, acc/pos decay says deeper may pay); launcher update
     (LLAMA_DFLASH_MIRROR_OUTPUT=1 + --spec-draft-device ROCm0);
     commit of the mirror patch (AGENTS.md approval); the checkpoint-
     min-step sparsify lane (point 3, still untested); target-pass
     overhead work (point 2, blocked on TG-phase profiler stack).

- E119.4 points 3+2 executed (user order: 3 then 2), 2026-09-04 night,
  all lanes on the mirror+-devd base (m7 reference: pp 404.7 / tg 15.8 /
  draft 28.8 ms / sha canonical):
  3. CHECKPOINTS: e119k0 (CKPT=0) protocol-confounded - tg_depth_cached
     0 (restore broken, prefix reprocessed; sha moved to the aa02f59e
     batch-state class; AB6/AB8 pathology). NOT valid TG evidence; the
     checkpoint tax is unmeasurable by this lane protocol. e119k1
     (--checkpoint-min-step 16384, restore intact): tg 15.5 / pp 393.4 /
     fill 378.7 / sha canonical = within the day's noise band of m7.
     VERDICT: perf-neutral, nothing recoverable via checkpoint density
     at this shape; prod keeps --ctx-checkpoints 30 @ default min-step.
     Point 3 CLOSED (no gain, no cost).
  2. DEPTH REBRACKET: e119n5 (SPEC_N_MAX=5): tg 11.4 (-28% vs m7!),
     draft 34.1 ms/round (+5.3), acc/pos gained position 5 at 0.133,
     but the ROUND grew 78 -> 133 ms: the 6-row verify batch fell off a
     kernel-shape cliff (~+50 ms on the target pass; the fattn narrow-
     tile gate still covers it - Q<=16, ncols2=2 - so the suspects are
     the MoE MUL_MAT_ID MMVQ/MMQ batch threshold crossing or the
     f16-shadow convert geometry at 6 rows). n6 skipped (strictly
     worse). VERDICT: n_max stays 4. Point 2 CLOSED for depth; the
     6-row verify cliff is a NEW fork-kernel finding - if ever fixed,
     n5 math becomes acc .133 x ~1 free token/round vs marginal draft
     cost; park as a possible follow-up lever.
  Session lane ledger (16k protocol, game closed): m7 404.7/15.8,
  k0 invalid, k1 393.4/15.5, n5 395.6/11.4. Rig left free after runs.

- E119.5 MAINLINE A/B @120k: MIRROR WINS ALL GATES, 2026-09-04 night:
  Two aborted attempts on the A-side (stalls at 32768 fill tokens -
  user-attributed interference, not checkpoint boundaries; the s1
  CKPT=0 discriminator lane was killed before evidence). Clean pair:
    lane  config                     pp16384  fill120k  tg1024@120k  acc    draft ms/r  sha
    e119r1 prod baseline (game off)   394.7    259.4     11.7        .644   32.2        e54019ff6b42
    e119m8 mirror + -devd            406.5    263.8     13.0        .657   18.8        e54019ff6b42
  GATES: PP +3.0% (no loss - top of band), TG +11.1% @120k depth
  (+28% at 16k depth per m7/r0), draft round -42% (32.2 -> 18.8 ms -
  single-device draft saves MORE at depth), acceptance band unchanged,
  temp-0 sha parity (bit-exact outputs).
  VERDICT: the draft output-head mirror (LLAMA_DFLASH_MIRROR_OUTPUT=1)
  + --spec-draft-device ROCm0 is a strict win on this rig. Pending
  user actions: launcher update (env + flag), patch commit (AGENTS.md
  approval), README/FINDINGS row. Note for prod restart: load_s grows
  ~2-10 s (995 MiB host-staged copy + verify at draft ctx init).

  TEMP-0 OUTPUT COMPARISON (user request, c 8192 fresh-ctx shape, full
  spec chain ngram+dflash n4, 96-token greedy continuation): baseline
  (prod binary) and mirror (engagement confirmed via -lv 4 log lines +
  identical acc 68/108) produce BYTE-IDENTICAL output - sha16
  ad1af14b78b0d01f both sides, 442 chars; eval 18.0 -> 20.2 t/s (+12%)
  at this shape. Quirk noted: init_mirror_output's INFO line did not
  appear in a default-verbosity boot (visibility artifact only - the
  mirror was active, proven by the -lv 4 rerun + identical acceptance
  + speed delta; suspect log-level gating on that pre-listening code
  path).
