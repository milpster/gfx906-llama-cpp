# Session journal - 2026-08-27 (F16-KV campaign, i1-Q6_K + DFlash2)

Living document, same contract as JOURNAL-2026-08-26.md. Companion:
bench/lane-dflash.sh (runner), bench/manual-fit.py (offline fitter),
bench/gguf-meta.py (GGUF metadata dump).

## Session constraints (user-set, binding)

- U1 Target config: llama-start-q6v-dflash2.sh config, model swapped to
  Qwen3.8-27B.i1-Q6_K.gguf, KV cache NOT quantized (no -ctk/-ctv -> f16/f16).
  Goal: best config (max tg/pp and/or max ctx) under those constraints.
- U2 Protocol: PP16384 first-batch -> SAME slot continued to 120k fill (deep
  PP) -> immediately TG1024 at 120k depth. No restart, no slot clear between
  stages (= lane-client.py with FILL2=0, TG_N=1024).
- U3 ONE server run total, no wasted time. Fit evidence and bench numbers
  must come from the same lifecycle.
- U4 Do NOT use --fit auto-adjustment. Manually fitted params, --fit off.
- U5 Always consider the skill's levers: pipeline parallelism, -sm cost ts,
  DFlash2 placement, -ot moves (dual-gpu-context-balancing skill).
- U6 Journal everything; new scripts must be reusable.

## Model facts (bench/gguf-meta.py, measured from the GGUF)

- M1 arch qwen35, 65 blocks: 48 GDN/SSM layers (attn_qkv/ssm_*, no KV cache,
  fixed recurrent state) + 17 full-attn layers (attn_k/v/q): il 3,7,11,...
  63 (16 main) + blk.64 (nextn, attn layer).
- M2 KV f16 = 4 kv-heads x 256 dim x 2(KV) x 2B = 4096 B/token/attn-layer;
  16 main attn layers -> 65,536 B/token total. (V q8_0 saves 1024 B/tok/layer.)
- M3 i1-Q6_K file 21,395 MiB; with draft-dflash blk.64 tensors are UNUSED
  (~937 MiB est, plain-Q6_K-sized attn+ffn+nextn) -> ~20,458 MiB loaded.
  UD-Q6_K_XL: 24,127 MiB file, blk.64 ~335 MiB unused (L6 log lines 13-27),
  ~23,792 MiB loaded. Delta: i1 frees 3,334 MiB of weights.
- M4 Emb/out heads ~1,989 MiB (i1): token_embd rides dev0, output rides last
  dev in layer mode.

## Fit decision (manual, L6-anchored delta method)

- D0 Simulator bench/manual-fit.py written (tensor-exact weights + skill
  layer-split formula + KV/recurrent/compute model). Calibration vs the old
  UD+MTP 190k margins (285/913/531) FAILED on Vulkan1 by ~1.4 GiB: stale-era
  logs mix -sm cost KV distribution, MTP draft-context placement and unknown
  verbosity; not worth reverse-engineering. Simulator kept for weight/KV
  arithmetic only; absolute per-device absolutes not trusted.
- D1 Anchor instead: lane L6 = SAME binary (build-dflash), drafter, ts 40,20,40
  -sm layer, b16384/ub384, -c 200192, UD model, f16 K + q8_0 V. Measured:
  server listens AND survives full deep protocol (120k fill + TG1024).
- D2 Attn-layer placement at ts 40,20,40: ROCm0 6 (il 3..23), Vulkan1 3
  (27,31,35), ROCm1 7 (39..63). (blk.64 dropped by dflash.)
- D3 Per-device delta vs L6 (see table in session log): at -c 200192 the
  i1+V-f16 swap leaves ROCm0 -183 / VK1 -67 / ROCm1 +65 MiB -> ROCm1 gets
  TIGHTER than the L6 fit (abort risk). At -c 195000: ROCm0 -297 / VK1 -124 /
  ROCm1 -68 -> every device strictly looser than L6.
- D4 DECISION (fit): -ts 40,20,40 -sm layer -c 195000, no -ctk/-ctv,
  --fit off, -lv 4. Margin policy: >= L6's implicit margins (which passed the
  deep workload), ~0.5 GiB net slack min-device vs L6. Chose proven-split
  safety over unmeasured split changes because of U3 (one run, no abort).
- D5 Skill levers, consciously evaluated (U5):
  - pipeline-parallel: OFF - measured 3x no single-request pp gain (np 1).
  - -sm cost vs layer: equal at same placement (measured 2026-08-20 header);
    layer kept (production).
  - -ot nextn->ROCm0: MOOT with draft-dflash (blk.64 tensors unused entirely).
  - DFlash2 drafter placement '.*=ROCm0' -ngld 99: kept (D15 recipe; flat
    draft cost with depth via 2048 sliding window).
  - ub 384 / b 16384: kept (validated optimum).
  - VK F16 env: no effect measured; production env kept.

## Run record

| lane | config | status | result |
|---|---|---|---|
| F1 | i1-Q6_K, f16/f16 KV, ts 40,20,40 layer, c 195000, dflash n7, --fit off | CRASHED at first PP16384 batch | ROCm0 hipMalloc OOM in fattn pool during graph capture; see post-mortem |
| F2 | F1 + -ot 'blk.39.*=ROCm1' + c 185000, fit on (pinned) | DONE | fit OK (n_ctx 185088); server-metric vs L6: PP16384 ~307 = parity, fill@depth -5..-8%, TG@120k 105.4 ms/tok vs 119.4 = +13%, acc .314/3.20 = same. Runtime free: ROCm0 ~100 MB, ROCm1 ~2.3 GB (stranded) |
| F3a | ts 40,19,41, c 185000, no -ot | HANG | GPU Hang node-1 at TG@120k start; fill before that: 226.75 t/s @103k (+17.2% vs L6) |
| F3b | same, retry | HANG | GPU Hang node-1 mid-fill @32k |
| F3c | same, retry 3 | WEDGE | silent stall in first PP16384 batch, no exception; killed |
| F3d | same, retry 4 | HANG | GPU Hang node-1 at fill start; PP16384 was 372 t/s (best) before dying |
| F4 | ts 40,19,41 + HIP_GRAPH=0 (discriminator) | ABORTED pre-evidence | killed at user request before PP completed; no conclusion |
| F5 | F3 shape + ngram-mod (24/28/64) chained | DONE | PP 335.0, fill 217.2, TG@120k 9.5, acc .689, sha = F3's (determinism). ngram: 1 draft all protocol (54 gen/45 acc, mean len 46 on a replay span); no fill tax. Verdict: free, idle on novel content, insurance for replay-heavy nights |
| F6 | F3 + -ot blk.24-26 (GDN) -> Vulkan1 | DONE | REJECTED: PP 268.9 / fill 188.0 / TG 7.4 (-18/-12/-22% vs F3). RS buffers did NOT follow the -ot (523.69/448.88/224.44 identical) -> 3 GDN layers computed on VK1 with state stranded on ROCm0 |
| F7 | F3 + --pipeline-parallel on | CRASHED | "MUL_MAT failed / ROCm error: no kernel image available" mid-PP16384: pipeline scheduler routed an op with no gfx906 kernel in this build. Build-level rejection, not config-fixable |
| F8 | F3 shape + -sm cost | DONE | PP 335.4, fill 217.7, TG@120k 9.5, acc .689, sha same. Byte-identical buffers to F3; third repeatability sample of the champion |

## Pipeline-parallel fix session (2026-08-27 night, D/T series)

User request: "fix pipeline parallel properly, use both our failure cases to
test" - G3 (UD+q8V prod shape: startup abort "failed to create MTP context")
and F7 (i1+f16: mid-PP "MUL_MAT failed / no kernel image").

Investigation trail (all evidence in bench/logs/lane-{D,T}*.log):
- D1: PP=on + HIP_GRAPH=0 runs the FULL protocol at parity -> "pipeline" runs
  are actually post-fallback non-pipeline runs (see below); also client hung
  on the final response in that run.
- D2: PP=on + HIP_LAUNCH_BLOCKING=1 crashes IDENTICALLY -> not async-error
  misattribution, not capture-only.
- Key log fact: every PP=on run (F7, D2, D4) shows exactly one 2026 MiB
  alloc failure + one "retrying without pipeline parallelism" -> the pipeline
  reserve (4x tensor copies, GGML_SCHED_MAX_COPIES=4) NEVER fit on this rig;
  pipeline was never actually active in any lane.
- The retry fallback was broken twice over: (a) it asked +117 MiB more on
  VK1 than a fresh start (backend pool debris from the failed attempt;
  destroying/recreating the sched does not reclaim it - T1 proved this with
  the destroy-first fix in place), which hard-aborts memory-tight configs
  (G3); (b) post-retry state corrupts later graph capture -> misreported
  "no kernel image" crash (F7/D2). D3/D4 showed the crash is timing-sensitive
  (an fprintf in mul_mat masked it) - i.e. a latent race we never pinned to
  a kernel, made unreachable instead.
- ggml-cuda's concurrency planner (concurrent_events / side streams) only
  activates with GGML_CUDA_GRAPH_OPT=1 AND exactly 1 CUDA device -> inert
  on this rig.

FIXES (src/llama-context.cpp + ggml-cuda.cu):
1. sched_reserve() restructured: the live scheduler is ALWAYS created
   non-pipeline and reserved first (the known-good path). Pipeline is only
   attempted afterwards, on a scratch scheduler, gated by a capacity check
   (per-device free >= 8x plain compute buffer, 8x measured from the 2026 vs
   236 MiB ratio), and adopted only on success. A failed attempt can no
   longer leave the context without working buffers or poison later capture.
2. ggml-cuda graph_compute: skip CUDA-graph capture when the stream context
   has concurrent regions (pipeline side streams); hygiene for single-GPU
   rigs where the planner can run.

Tests on the fixed build (both user failure cases):
- T4 (= G3 case): "pipeline parallelism disabled: device Vulkan1 has 575 MiB
  free, needs an estimated 1965 MiB" + buffers byte-identical to a pristine
  production startup (239.89/245.65/239.89) + LISTENS. No retry path taken.
- T5 (= F7 case): full protocol completes, zero errors: PP 331.5, fill 103612
  @ 217.8, TG@120k 9.5, acc .689, sha 694720d04453 = byte-identical to the
  F3/F5/F8 champion runs.
- T6 regression: champion config (no PP) listens, buffers healthy.

Verdict: --pipeline-parallel is now SAFE on this rig (clean, logged
degradation when it cannot fit - which is always, given the 4x copy-buffer
cost) and remains pointless at -np 1 (D1 parity + 3x historical no-gain).
Debug instrumentation (mul_mat dispatch dump) added during D3/D4 and removed
afterwards; builds verified clean.


## Production-config re-evaluation (2026-08-27 late, G/P series)

Questions: on llama-start-q6v-dflash2.sh (UD-Q6_K_XL, f16K/q8V, ts 40,20,40,
c 200000) - more ctx now that MTP is gone? ngram? sm cost? pipeline parallel?

| run | config delta | result |
|---|---|---|
| P1 probe | +ts 40,19,41, c 210000 | LISTENS; tables: ROCm0 2483 / VK1 1197 / ROCm1 535 MiB free pre-drafter -> ROCm1 ~150 post-drafter = not survivable margin |
| P2 probe | ts 40,20,40, c 210000 | aborted per user: ceiling probing unnecessary (protocol needs ~121k) |
| P3 probe | P1 + -sm cost | tables BYTE-IDENTICAL to P1 layer mode (cost = layer confirmed on UD too) |
| G1 lane | production + ngram-mod | PP 327.1, fill 185.6 (-4.1% vs L6 193.5), TG@120k 8.2, acc .686 -> fill tax, no TG gain |
| G3 lane | production + --pipeline-parallel on | CRASH AT STARTUP: "failed to create MTP context" (drafter ctx creation broken under pipeline; F7 crashed differently on i1+f16) |

Answers:
- CTX: no meaningful gain available. 200k stays the production number; 210k
  listens on a rebalanced split but with ~150 MiB margin (F1 showed runtime
  pools need several hundred). DFlash2's flat draft cost is offset by the
  drafter's 2.5 GiB spread.
- NGRAM: rejected for both stacks on this protocol (G1 -4.1% fill, TG parity;
  F5 free-but-idle on i1). Only for replay-heavy nights.
- SM COST: no-op (byte-identical placement at same ts, P1 vs P3, and F8).
- PIPELINE PARALLEL: broken with the external drafter in this build
  (startup failure G3; missing-kernel crash F7 on the other stack). Would
  need code work; also never helped single-request pp in MTP-era tests.

New artifact: llama-start-q6v-dflash2-f16.sh (F3 winner as production
launcher, i1-Q6_K + F16 KV + ts 40,19,41 + c 185000). lane-dflash.sh gained
PROBE=1 fit-evidence mode (status "${ok:+DONE}${ok:-FAILED}" prints DONE1 on
listen - cosmetic bug, harmless).

## CAMPAIGN CLOSE (2026-08-27)

Final league (stock clocks, protocol PP16384 -> 120k fill -> TG1024 @depth,
i1-Q6_K + unquantized f16/f16 KV + DFlash2 n7 unless noted):

| lane | config | PP | fill@120k | TG@120k | acc |
|---|---|---:|---:|---:|---:|
| L6 ref | production: UD model, f16K/q8V, ts 40,20,40, c 200k | 335.3 | 193.5 | 8.4 | .686 |
| F2 | ts 40,20,40 + -ot blk.39->ROCm1, c 185k | ~307 | ~190 | 9.49 | .659 |
| F3 | ts 40,19,41, c 185k | 329.0 | 213.5 | 9.4 | .689 |
| F5 | F3 + ngram-mod | 335.0 | 217.2 | 9.5 | .689 |
| F8 | F3 + -sm cost (= repeat) | 335.4 | 217.7 | 9.5 | .689 |
| F6 | F3 + -ot blk.24-26->Vulkan1 | 268.9 | 188.0 | 7.4 | .662 |
| F7 | F3 + pipeline-parallel on | CRASH (missing gfx906 kernel) | - | - | - |

WINNER: F3 shape. vs production L6 at the same protocol: deep fill +12%,
TG@120k +13%, PP parity, acc identical; ctx 185k (vs 200k with quantized V).
F3/F5/F8 spread (fill 213.5-217.7, TG 9.4-9.5, PP 329-335) = noise band.
ngram-mod optional: free, idle on novel content, occasional big wins on
replayed spans (mean accepted len 46 on its single hit).

Recommended launcher diff vs llama-start-q6v-dflash2.sh:
- -m /home/srcds/ai/ai/Qwen3.8-27B.i1-Q6_K.gguf
- remove -ctk/-ctv entirely (f16/f16)
- -ts 40,19,41 (was 40,20,40), -sm layer unchanged, -c 185000 (was 200000)
- everything else identical (dflash n7, drafter ROCm0, ub 384, pipeline off)

Reusable artifacts: bench/lane-dflash.sh (protocol runner, env-parameterized),
bench/manual-fit.py (offline placement fitter), bench/gguf-meta.py (GGUF dump).
Known limitations: manual-fit placement sim predicts 6/3/6 attn for ts 40,20,40
vs real 6/4/6 (off-by-one at VK1 boundary); constants need per-rig re-check.


- E13 ROOT CAUSE CORRECTION (user disclosure): the 5 F3 hangs were caused by
  the user's GPU OC experiments, NOT the split. After restart at stock clocks
  the SAME config completed the full protocol. All pre-restart numbers (F2,
  F3a's 226.8 fill, F4 probes) are OC-contaminated; stock-clock league table
  is authoritative. Lesson: verify clock state before blaming placement.
- E14 -ot weight overrides do NOT move recurrent state (RS allocates by split
  position) - same class as E5 (KV stays). Any -ot layer move whose layer has
  state on device (attn KV, GDN RS) creates a split layer with cross-device
  state traffic per token; F6 quantified the penalty at -12..-22%.
- E15 -sm cost = -sm layer at identical buffer placement (F8 evidence);
  earlier "fractional KV under cost" reading of calib logs was whole-layer
  6/4/6 in disguise. Cost mode offers no extra lever on this stack.
- E16 --pipeline-parallel on is broken for this workload on gfx906 in this
  build (missing kernel image in the pipeline op routing). Would need
  build/code work; not a config lever today.


- E11 F3 VERDICT: ts 40,19,41 REJECTED - 4 attempts, 4 failures, all node-1
  (ROCm1 carrying blk.38 GDN + blk.39 attn + 7th KV block), failure stage
  random (first PP / fill start / mid-fill / TG start). Placement-triggered
  gfx906 instability, not transients. The split's +17% deep-fill speed
  (226.8 vs 193.5 t/s full-fill) makes the loss painful but 4/4 hangs is
  conclusive. Do not retry this split on this build without a code-level fix.
- E12 Remaining options: (a) ship F2 config (stable, full-protocol validated);
  (b) HIP_GRAPH=0 discriminator on 40,19,41 untested to completion; (c) ngram
  lane still pending (user request), run on the stable F2 shape.


- E8 F2 result reading: client JSON had artifacts (PP payload processed twice:
  tasks 91+171 both cancelled; fill task 260 cancelled near end, a later 4-token
  task reported 119999 cached -> client fill1/pp_tps unreliable for F2). Server
  log is ground truth; use per-token eval times and cumulative PP lines.
- E9 F2 TG improvement (+13% @120k depth vs L6) with identical acceptance:
  per-token cost drop 119.4 -> 105.4 ms. Candidates: i1-Q6_K faster to
  dequant at bs=1 (UD had heavier mixes?), and/or less KV V read... V is BIGGER
  now (f16) so not bandwidth: likely quant-mix effect on TG compute. TENTATIVE.
- E10 F2 fill regression split: V-f16 doubles deep KV reads (physics, expected
  -4..-8% by old 08-20 data) PLUS possible split-layer 39 penalty (weights
  ROCm1 / KV VK1 cross-device every fill step, skill's documented -ot failure
  mode). F3 (ts 40,19,41) isolates: moves blk.38+39 weights AND KV together
  to ROCm1, un-splits, plus deep KV reads leave the 448 GB/s 3080 for a
  1 TB/s VII.


## F1 post-mortem (evidence: bench/logs/lane-F1.log)

- E1 Measured statics (target ctx): model 8047/3865/8142 MiB
  (ROCm0/VK1/ROCm1), KV 4572/3048/4572 (=6/4/6 attn layers x 4096 B/tok,
  NOT the 6/3/6 my placement sim predicted), RS 524/224/449, compute
  236/245/236, mmproj 995 on HOST (ROCm_Host, not a device).
- E2 Draft-context cost (dflash Q4_K_M on ROCm0): model 1080 + KV 30/10/10 +
  compute 415/371/364 + meta 248 -> 1772 MiB ROCm0, ~375 each other device.
- E3 F1 nominal margins with TODAY's free baselines (15676/7553/15750, i.e.
  ~300 MiB lower per VII than the calib-era numbers I had used): ROCm0 -75,
  VK1 -144, ROCm1 +1919 -> BOTH ROCm0 and VK1 oversubscribed. Vulkan defers
  binding (listens anyway); ROCm0 failed at the first fattn pool alloc during
  HIP graph capture. Not a fit-time abort because everything is pre-allocated
  pools: runtime pools hit the wall first.
- E4 --fit off also suppressed the -lv 4 memory breakdown tables (they only
  print from the fit pass). F2 keeps fit enabled with all params pinned
  (fitter adjusts nothing, D12 warning is cosmetic).
- E5 -ot moves model WEIGHTS only; the KV cache of layer 39 stayed on VK1
  (KV follows the split, not overrides). VK1 KV 2892 MiB @ c 185088 = still
  4 attn layers.
- E6 F2 ledger (fit table + draft): ROCm0 +768 / VK1 +261 / ROCm1 +1913 MiB
  nominal. VK1 261 ~= the old f16-KV working margin (285).
- E7 Runner bugs fixed en route: set -o pipefail (client failure was masked
  by tee), CTKV/CTVV assignment typo (CTVV never assigned -> "unbound
  variable" killed the F2 first attempt pre-server).


## Upstream sync + Vulkan view-alias fix + re-validation (2026-08-28, V/W series)

- E17 SYNC: merged origin/master@18443257a (39 commits: DFlash2 #27816
  reviewed rewrite with greedy p_min selection, Vulkan LIGHTNING_INDEXER
  #27453, --kv-unified-per-slot #24124, TENSOR_READ_LAZY). Conflicts
  resolved keeping our adaptive-MTP + cost_attn_weight + tensor_read_lazy
  (additive both sides); dflash.cpp taken wholesale from upstream.
- E18 VULKAN BUG ADOPTED: PR #27342 comment (Eric-A-Stalee) flags
  ggml_vk_graph_optimize missing view-alias dependencies -> spec verify
  corrupted on Vulkan devices; "Qwen3.8 recurrent state" named. Fix in
  open PR #27812 (is_src_of: compare view_src bases on both sides);
  ported verbatim to our ggml-vulkan.cpp (not yet merged upstream).
- E19 REVALIDATION on merged+fixed build, champion F16/F16 config
  (40,19,41, Q4 drafter): V2 @185k: PP 348.2, fill 238.5, TG@120k 10.5,
  acc .718, repro gate TRUE (first time ever; every prior lane was false),
  sha 694720d04453 = pre-merge champion (outputs unchanged, the gate
  failure was the bug). W2 @190k: identical numbers -> F16/F16 ceiling
  raised 185k -> 190,208, full protocol validated.
- E20 W3 (iq6v config: i1 + Q8 drafter + f16K/q8V + 35,20,45 + 250k) on
  new build: PP 362.0, fill 218.6, TG@120k 8.6 (+19% vs 7.2), acc .708
  (+.051), repro TRUE, sha = R-series (deterministic). Q4==Q8 drafter
  still equivalent.
- E21 W4 probe: user-tuned 34,20,46 @ 200k (attn 5/3/8) does NOT fit:
  VK1 -263 / ROCm1 -279 MiB post-drafter. Launcher restored to the
  validated 40,19,41 @ 190000.
- E22 q8V-vs-F16V on the new build (same protocol): fill 218.6 vs 238.5
  (V-conversion tax ~8%), TG 8.6 vs 10.5, PP 362 vs 348. Choose by
  priority: TG -> F16/F16 @190k; PP+ctx -> q8V @250k.
- E23 launchers updated (headers re-validated, F16 ceiling 190k); the
  GGML_VK_DISABLE_GRAPH_OPTIMIZE=1 A/B (V3) was superseded by the real
  fix and not completed.
- E24 q8V ctx ceiling: W5 probe + W6 full lane: 35,20,45 + OT
  'blk.(37|38).ffn_* -> ROCm0' fits and survives 260,096 (PP 323.8,
  fill 201.9, TG 8.6 unchanged, acc .708, repro TRUE, sha unchanged).
  Trade vs clean 250k: -10% PP / -8% fill for +10k ctx. Clean 250k
  recommended (W3); 260k documented as opt-in. VK1 becomes the binder
  beyond ~265k.
- E25 acceptance hunt (X series, iq6v 250k config, new build): X1
  (Q4 drafter + ngram-mod) byte-identical to W3 (Q8, no ngram): acc
  .708, TG 8.6, same sha; ngram registered zero drafts. Drafter quant
  and ngram are acceptance no-ops (2nd/3rd confirmations). X2
  (--spec-draft-n-max 4): TG@120k 9.0 (+4.7%), acc rate .646 - the
  rate drops but t/s wins at depth, as the PR-thread four-arm data
  predicted. iq6v launcher updated to n_max 4 (X2 numbers).
- E26 Y series (iq6v champion config): n_max bracket complete: 3 -> 8.5,
  4 -> 9.0 (champion), 5 -> 8.0 (single sample, anomalous), 7 -> 8.6.
  n_max 4 ships in the launcher.
- E27 ctx beyond 260k: Y5 probe + Y6 full lane: 262,144 fits with a 3rd
  FFN group (blk.36 -> ROCm0) but costs 1.0 t/s TG (7.6 vs 8.6 at 260k)
  - the VK1->ROCm0 hop lands badly in the serial pipeline. 260k stands
  as the practical q8V ceiling; 250k clean recommended.
- E28 ub revalidation on the new build: 448 -> PP 355.4/fill 213.3/TG 8.9;
  352 -> 341.2/212.4/8.5. ub 384 remains optimal (363/218.4/9.0).
- E29 one-off host-side stall (Y2 attempt 1): fill froze at 32k with idle
  GPUs, live process, clean dmesg - lost wakeup class, not a GPU hang.
  Retry completed cleanly; watch-item, gdb-on-recurrence plan noted.
- E30 pipeline parallelism at current headroom: impossible - VK1 needs
  ~2-2.9 GiB free for the 4-copy buffers, has ~0.5 at 250k; only
  engageable near c 118-122k (below the protocol floor's comfort),
  with marginal expected np=1 benefit. Closed permanently for this rig.

## Fork vs mainline A/B (2026-08-28, AB series)

Stock = upstream master 18443257a built in ~/dev/llama.cpp/build-stock
(pinned 6.1 recipe; initial configure missed GGML_VULKAN case + package
dirs - D11 applies to stock builds too). Full writeup:
FORK-VS-MAINLINE-2026-08-28.md.

- E31 AB3/AB4 (identical command, c 200192): PP fork 361.8 vs stock
  347.7-354.3 (+2-4% fork); fill stock 226.6 vs fork 218.9 (+3.5% stock);
  TG@120k STOCK 11.51 vs fork 9.0 (+28% STOCK) at equal acceptance
  (.655*/.646). *stock acc measured on the unfixed Vulkan verify path.
- E32 stock cannot fit the champion: its compute buffers are 4.7x ours
  (1302/1021/1332 vs 276/282/276 MiB) -> 250k oversubscribes VK1 and
  ROCm1 pre-drafter. Stock ceiling ~200k (215k only deferred-oversub).
  The fork's buffer frugality IS the 250-260k ctx feature.
- E33 AB5: fork + GGML_VK_DISABLE_GRAPH_OPTIMIZE=1 = TG 9.0 identical ->
  Vulkan optimizer and our #27812 fix are NOT the TG-delta cause.
  Suspect: buffer-reuse serialization of verify rounds (276 vs 1302 MiB).
  Open investigation.
- E34 stock desyncs the lane client (progress streaming API); all
  cross-binary numbers taken from server-log timings. lane-dflash.sh now
  emits --pipeline-parallel only when PP is set (stock rejects the flag).

## TG-delta decomposition (2026-08-28 night, AB6-AB8)

Fork 9.0 vs stock 11.5 TG@120k at identical config. Decomposition:
- checkpoints off (--ctx-checkpoints 0): +0.65 (AB6 hand-drive 9.65) BUT
  breaks cross-request prompt restore (AB8: TG reprocessed part of the
  prefix, tg_depth_cached 0; server-context.cpp:3436 gates the restore
  path on n_ctx_checkpoints > 0). Not shippable for the chat backend.
- cram off (CRAM=0, AB8): +0.15 more (9.8 total, partial-reprocess
  confound). Same retention caveat applies to the combination.
- Vulkan fix/optimizer: exonerated (AB5 = 9.0 with optimizer disabled).
- pipeline-as-buffer-headroom (user idea): cannot engage above ~60-80k
  ctx - the 8x-compute gate grows with the compute buffer as fast as
  free memory does; VK1 never clears it at protocol-scale ctx.
- Remaining ~1.7 t/s: code-level. Suspects: fork's 4.7x-smaller compute
  buffers serializing verify rounds (untestable without a buffer-size
  knob) vs the adaptive bookkeeping in the shared draft loop. Next tool:
  rocprof the TG rounds fork-vs-stock (bench/rocprof.sh exists), or
  bisect speculative.cpp's loop against upstream's.
- Methodology note: the hand-drive fill with cache_prompt False makes the
  FORK (not stock) drop the slot prefix -> full TG reprocess; the lane
  client retains it. Cross-checkpoint comparisons must use the lane.

## The remaining 1.7 t/s (2026-08-28 late, AC series + second sync)

- E41 second upstream sync to 50f068fff (merged 5d317d32c): #27812
  upstream (supersedes our port - took their is_empty-evolved version),
  #27877 (force fused GDN/LID ops), #27880 (qwen4exp split reduction -
  different arch, N/A), lazy-mode rename (kept cost_attn_weight + new
  lazy_mode on merge).
- E42 AC1 (fork @ new master): TG@120k 8.9 = unchanged -> #27877 is not
  the differentiator (fork's auto-resolve already enabled those fused
  ops; the delta is stable across both upstream bases 18443257a and
  50f068fff, pinning it to OUR code).
- E43 AC2 (--spec-draft-device ROCm0, intended to defragment the 23-split
  draft graph): aborts at draft ctx creation - the D15 output.weight
  borrow incompatibility persists in upstream dflash. Config-level fix
  impossible.
- E44 EXHAUSTED config levers for the 1.7 t/s: checkpoints/cram (0.8,
  unshippable), vk optimizer (0), pipeline (unengageable), draft-device
  (aborts), fused-GDN forcing (0). Remaining suspects, both code-level:
  (a) the fork's ggml-backend.cpp meta-scheduler split execution
  (per-split event/sync overhead or buffer-reuse serialization of the
  23-split draft graph, ~75 ms/round measured delta), (b) adaptive
  bookkeeping in the shared draft loop. Next session: rocprof fork-vs-
  stock TG rounds (bench/rocprof.sh stack ready) or git-bisect the
  fork's ggml-backend.cpp against upstream classic sched.

## AD series: delta fully isolated to spec-machinery execution (2026-08-29)

- E45 AD1/AD2 (SPEC_TYPE=none, both binaries, 200k champion shape):
  fork TG@120k 6.9 = stock 6.9, SAME output sha (aa02f59e3197). Base
  decode paths are equivalent; the entire 1.7 t/s delta lives in the
  speculative machinery. Spec gains: stock +67%, fork only +30%.
  Bonus: no-spec PP 397.5 (drafter taxes prefill by ~9%).
- E46 fork's own spec timer (dur b,g,a): draft generation = 22.97 s of
  the 115 s TG task (~43 ms/round over ~530 rounds) - the dominant
  overhead center. Stock cannot print this (no instrumentation).
- E47 AD3 CPU drafter (-ngld 0): fresh TG 13.07-14.62 t/s (+26-40% vs
  GPU drafter) but depth TG 8.5 (parity with 9.0, single sample). CPU
  drafting wins shallow, not deep. Keep as curiosity, not a fix.
- E48 graphs-reused counts (fork 526 vs stock 345) proved NOT comparable
  across schedulers - round-count inference abandoned.
- E49 rocprof: archived stack core-dumps at launch (bench/rocprof-stack
  bin/rocprofv2 line 287 LD_PRELOAD abort). Repair = rerun
  bench/rebuild-rocprof-stack.sh (documented, untested this session).
- E50 NEXT SESSION plan: (1) repair rocprof stack, profile one TG round
  fork-vs-stock (kernel timeline vs gaps in the 23-split draft graph);
  (2) code candidate: the draft context's frugal reserve (415 MiB vs
  stock ~1 GiB) - relax sizing for the DRAFT context only; costs a few
  hundred MiB ctx (user accepts) and should let the draft graph execute
  unfragmented.

## Adoption of upstream as the speed production path (2026-08-29, AE1)

- E51 user decision: speed over ctx. Stock rebuilt at upstream 50f068fff
  (~/dev/llama.cpp/build-stock; the clone's local master points at our
  fork via --shared - always checkout the upstream SHA detached).
  Validated with the champion command at 200k: TG@120k 11.4, fill 226.9,
  acc .658, repro gate TRUE (upstream #27812 fix works), sha e54019ff6b42
  = BYTE-IDENTICAL to the fork's output - both binaries now agree.
- E52 new launcher llama-start-iq6v-stock.sh (stock binary, 200k, n_max 4).
  Production choice matrix: speed -> stock launcher (11.4 TG, 200k);
  max ctx -> fork iq6v launcher (9.0 TG, 250-260k). Same prompts produce
  identical tokens on both (sha match) - switching between them is safe.
  Fork remains required for: ctx > ~200k, -lv 4 fit tables, pipeline
  experiments. Next session's fork-draft-reserve fix (E50) may close the
   TG gap and make the fork strictly better again.

## E50v2: FATTN gate, bisection, s0 negative (2026-08-29/30)

- E53 gate: GGML_CUDA_VEGA_TUNE master + 4 sub-options (FATTN/MMQ/TOPK/
  GRAPHS) over the fork's Vega tuning, additions-only, 23 sites repointed,
  -DGGML_CUDA_VEGA_TUNE=OFF == full novega. Builds: build-dflash (prod,
  untouched) / build-dflash-novega (test dir). Uncommitted.
- E54 bisection ladder at c 200000 (all sha e54019ff6b42, repro ok):
  AC1 fork-all-on TG 8.9 / fill 218.4 / acc .646 / target buf 238.15;
  AE1 stock 11.4 / 226.9 / .658 / 1058.61; e50v2 fork-all-OFF 11.9 /
  227.4 / .658 / 588.65; b1 FATTN-only-on 9.3 / 202.0 / .646 / 238.15.
  Verdict: the q8_0 in-kernel-dequant tile path (F16 K + q8_0 V) is the
  whole tradeoff - it alone buys the 588.65->238.15 MiB compaction
  (the 250-260k ctx fit, E32) and alone costs ~2.6 t/s TG + 11% fill
  + the acc shift. MMQ/TOPK/GRAPHS together worth <= 0.4 t/s.
- E55 s0 NEGATIVE: raised the tile batch gate Q->ne[1] > 2 to > 32 to
  send spec-verify ops (3-5 rows) back to VEC. TG collapsed 9.3 -> 5.3,
  acc .646 -> .629, fill unchanged 201.6. Reverted (diff now == gate only).
  FINDING: the small-Q Q-row heuristic is KV-blind - at deep KV the
  end-to-end ranking is F16-convert+tile > native-q8 tile >>> VEC
  (VEC re-streams the full K+V per Q row; the tile path dequants each
  q8 V element 3x per verify op - see E58). RAW kernel ordering is
  NOT yet proven: acc drifts .646 -> .629 across paths, so end-to-end
  deltas mix kernel time with acceptance. Side note: s0's output sha
  c10c2fdbdd30 != e54019ff6b42 - the VEC path changes tokens, the
  tile and convert paths are sha-identical.
- E56 rocprof status: the archived stack core-dumps at launch (E49);
  repair = bench/rebuild-rocprof-stack.sh (untested) - prerequisite for
  the PMU passes. Profile target is the VERIFY regime, not 16k-PP:
  KV pre-filled to 120k/200k, Q = 3-5 verify batches, b1 native-tile
  pass vs FATTN-OFF convert pass,   counters per FA kernel (VMEM
  transactions, VALU, LDS stalls, VGPR/occupancy) + does V dequant
  scale with the Q-tile count (3x at verify, 36x at ubatch 384 - E58).
  Collect target-eval us/verify (fork spec
  dur timers) and acc SEPARATELY - acceptance drifts across paths
  (.658/.646/.629) and can masquerade as kernel regressions.
- E57 next: (1) repair stack; (2) verify-regime PMU pair; (3) ncols
  sweep on the verify regime micro-matrix Q={3,4,5,8} x KV={64k..200k}
  before any full lane; (4) loader experiments: 8x 32-bit loads before
  chasing 128-bit (2-byte aligned q8_0 qs scalizes vector loads),
  inspect gfx906 ISA first; (5) product end-state: context-based mode
  selection (convert if the F16 workspace fits the requested ctx, else
  native) so the fork alone replaces the E52 two-launcher setup.
- E58 code-verified dequant multiplicity + PMU pair fix (2026-08-30
  review): (1) tile grid = (Q-tile x KV-chunk x head-group), fattn-
  common.cuh:1086-1089,1119-1179; block->KV head = head0/gqa_ratio
  (fattn-tile.cuh:1018); gqa 6 dispatches ncols2=2 (fattn-tile.cuh:
  1540) = 3 head-group blocks per KV head; q8_0 V dequant runs once
  per block per KV chunk (:858) inside the KV-chunk loop (:1151).
  Per FA op each q8 V element is dequantized ntiles_z_gqa x
  ceil(Q/32) = 3x for the Q<=32 verify ops (one Q tile of 32 rows,
  no per-Q-row scaling) and 36x at a 384-row ubatch. K is f16 in our
  KV config: V-only dequant. The convert path removes 2/3 of the V
  dequant, but the bytes story is the OPPOSITE (native ~3.75B/elem
  with the 3 group blocks overlapping in L2, vs convert ~9.75B/elem)
  - the TG win must be ALU/LSU-driven (2B-aligned scalar loads incl.),
  and the PMU must split SQ_INSTS_VALU vs VMEM vs LDS + stalls before
  any rewrite. (2) PMU pair confound: native vs convert runs draft
  different token streams -> different Q=3/4/5 mix -> raw PMU totals
  are not apples-to-apples. Design: 1a = UNPROFILED lane (fill 120k,
  TG 256, -v for the per-round n_draft/n_accepted spec debug lines)
  on both builds -> real us/verify + acc + Q-mix histogram (the
  authoritative timing; profiled runs serialize kernels); 1b = the
  same pair under rocprof -> counters only, with a Q-mix-adjusted
  reading; 1c = if the Q-mix diverges, a fixed-shape FA harness
  (bench/, explicit Q x KV x gqa op, K f16 / V q8_0) is the clean
  instrument for 1b and the 2a matrix (now Q={1,2,3,4,5,8}: the Q=1/2
  old-VEC branch gets a deep-KV validity check, not a rescue).
  (3) Loader experiment before any big rewrite: two adjacent q8_0
  blocks as one 68B unit (4B-aligned at the row base) = 17 aligned
  32-bit loads incl. both scales, unpack in registers; check the
  gfx906 ISA with hgobjdump - 16x 16-bit loads may simply scalarize.
  (4) Selector contract: ONE canonical AUTO/FORCE_CONVERT/FORCE_NATIVE
  decision, env-overridable for benching, resolved once per
  context/device/graph reservation and consumed by BOTH workspace
  sizing and dispatch (no independent hipMemGetInfo in each; never on
  current free memory unless the decision is stored). AUTO = convert
  iff the F16 workspace fits the requested ctx.
- E59 b1 re-interpretation + Q-mix instrumentation (2026-08-30): the
  Q-row gate (tile if Q rows > 2, else VEC) makes b1/AC1 TG a MIXED
  routing measurement until proven otherwise: n_draft=0 -> Q=1 VEC,
  n_draft=1 -> Q=2 VEC, n_draft>=2 -> Q>=3 native tile (Q =
  n_draft+1, corroborated by server logs: generate_draft #draft=N
  alongside the N+1-row decode batch; confirm in the first fa1a log).
  s0 moved the mix toward all-VEC (TG 5.3). Corollary: p_min (early
  break) and n_min (discard -> 0) are indirect kernel-routing
  parameters on gfx906. Instrumentation (private fork, debug only):
  the dflash impl in common/speculative.cpp records the post-n_min
  draft length per seq (n_last_draft, init -1 = unobserved, 0 = real
  empty draft) and logs "dflash round seq %d: n_draft=%d,
  n_accepted=%d" under -v; the server already logs generate_draft
  #draft and accepted N/M. Stage 1a leads with the histogram:
  startup params (n_max/n_min/p_min/block_size) + n_draft
  distribution + per-n_draft mean n_accepted. Outcomes: delta at
  n_max with p_min=0 -> b1 is a near-pure Q5 native-tile measurement
  and the .658 vs .646 acc gap is a target-numeric effect (Q-mix
  hypothesis retired); real n_draft=0/1 rounds despite p_min=0 ->
  something else shortens drafts, investigate; significant Q1/Q2
  share -> the tile's Q3-5 is worse than the 9.3 average implies.
- E60 fa1a pass A + spurious off (2026-08-31, 120k KV, c=155000,
  tg 256, -v, b1 config = FATTN-ON only): TG 9.2 t/s, acc 0.641,
  fill 201.9, repro sha e54019ff6b42 (same token stream as the
  c200000 lanes). dflash round log: n_draft = 4 on all 265 logged
  lines -> every verify is a Q = 5 native tile; the Q<=2 VEC
  routing mix cannot occur under dflash at these settings, so the
  Q-mix hypothesis (E59) is retired: the c200000 b1/AC1 vs
  off/stock gap (9.3/8.9 vs 11.9/11.4) is a config effect on a
  pure Q=5 workload. Acceptance mix over logged lines:
  n_accepted {0:80, 1:46, 2:41, 3:21, 4:77}, mean 1.88, ~30% of
  lines accept 0 drafts. Caveat: 265 logged lines vs ~256
  committed tokens (new n_tokens 13 -> 266): the instrumentation
  fires more often than commit rounds, use the distribution
  shape only, not raw counts. The "fa1a-off" lane was spurious:
  CMakeCache still had FATTN=ON (no reconfigure happened), so it
  re-ran the same binary - identical tokens and 9.2/9.2 confirm
  lane-to-lane reproducibility at 120k, but all-OFF@120k remains
  open. Side note: the b1 config is nearly KV-length flat from
  120k to 200k (9.2 vs 9.3 t/s). In flight: all-OFF rebuild
  (FATTN=OFF on top of the already-OFF mmq/topk/graphs) running
  detached, log /tmp/opencode/fa1off-build.log; b1 binaries saved
  at build-dflash-novega/bin-b1; next: ./bench/fa1-lane-120k.sh
  fa1a-off (true pass B).
- E61 fa1a pass B verdict: default FATTN OFF (2026-08-31, true
  all-OFF build, log 07:48 > binary 04:38, flags.make
  GGML_CUDA_VEGA_TUNE_FATTN=0): off@120k = TG 12.6 t/s, fill
  228.2, acc .672, repro sha e54019ff6b42. Full matrix now:
  b1 (native q8 tile) 9.2 @120k / 9.3 @200k vs off (convert)
  12.6 @120k / 11.9 @200k -> the FATTN cost is -3.4 t/s at 120k
  and -2.6 at 200k: BRANCH 1, not KV-length dependent, the
  native tile loses at both context lengths on this workload
  (Q=5 dflash verify, KV f16 K / q8_0 V, gqa6). Fill also
  favors off (228.2 vs 201.9 @120k). n_draft = 4 on all 258
  logged lines (pure Q=5 both passes); acceptance mix shifted
  (off {0:68, 1:52, 2:40, 3:17, 4:81} vs b1 {0:80, 1:46, 2:41,
  3:21, 4:77}) with the SAME committed token stream - the FA
  path change also perturbs the draft blocks' numerics (lossless
  verify keeps the stream identical, the acceptance trajectory
  does not), and off accepts consistently better (.672/.658 vs
  .641/.646) - second point for off. Mechanistic note: convert
  scales with KV (12.6 -> 11.9) while the native tile is flat
  (9.2 -> 9.3): the tile path's per-round cost is dominated by
  something KV-independent (candidate: fixed 32-row Q-tile work
  at Q=5 = ~6.4x useful-work padding, and/or the vega ncols2=2
  config's occupancy); splitting that is the 1b/2a question and
  is only needed for kernel work, not for the default. Decision
  per the speed-first rule: GGML_CUDA_VEGA_TUNE_FATTN default
  OFF; the native tile path remains the compact-buffer option
  for 250k ctx behind the endgame selector. Next: user confirms
  the default; then the endgame selector patch (design note in
  PROGRESS) and close-out.
- E62 endgame selector implemented (2026-08-31, user course
  update: "kernel work is the goal, as optimized as possible for
  radeon vii gfx906" + autonomous GPU testing granted, no
  delegation). Patch in fattn.cu only (104 insertions):
  (1) GGML_CUDA_FATTN_PATH env = auto | force_convert |
  force_native (parsed once, unknown values warn + auto);
  (2) per-device stored decision (mutex-guarded, resolved on
  first use) consumed by BOTH consumers through one choke point:
  the HIP quantized-KV branch of ggml_cuda_get_best_fattn_kernel
  and the shadow-skip in ggml_cuda_flash_attn_ext_get_alloc_size
  both call ggml_cuda_fattn_use_native_tile before the native
  predicates - sizing and dispatch cannot disagree;
  (3) AUTO = native iff the full-KV f16 shadow (one layer of K/V
  at reserve-time KV length, quantized tensors only) exceeds
  free VRAM at resolution time minus 20% headroom; free is read
  once per resolution (cudaMemGetInfo after set_device), stored;
  re-resolves only when a longer KV arrives while convert was
  chosen (compact choice never needs revisiting; the only flip
  direction leaves old buffers oversized = safe);
  (4) GCN multi-row (Q>2) non-native case now falls through to
  the generic flow (f16 convert tile) instead of forcing VEC -
  measured convert-tile 12.6 vs VEC 5.3 at 120k (E60/E61);
  non-GCN HIP and Q<=2 behavior unchanged. Resolution timing
  note: the server reserve graph runs at max KV so the first FA
  sizing call sees the full-ctx shadow; without reserve (cli)
  the first op decides and the KV-growth re-resolve covers it.
  Validation plan (autonomous lanes, c=155000 fill 120k):
  force_convert expect ~12.6 + 588-ish buffer, force_native
  expect ~9.2 + 238, auto expect convert at this ctx (shadow
  151 MB vs multi-GiB free) with the per-device GGML_LOG_INFO
  decision line in the log; boundary lane at C=250000 where the
  shadow must NOT fit -> native + compact buffer.
- E63 selector validated + bypass bug found/fixed (2026-08-31,
  autonomous lanes, build-dflash-novega, c=155000 fill 120k):
  ROUND 1 (selector v1, decision only in fattn.cu): fa1s-conv
  = 9.2 but reserve 838.61 MiB (shadow allocated!) while
  fa1s-nat = 9.2 with 576.11 - sizing followed the decision,
  dispatch did NOT: the native launch branches in fattn-tile.cuh
  (:~1358/:1375, kept "in sync with ggml_cuda_fattn_tile_v8_0_native()
  in fattn.cu" per the fork's own comment) re-detect type+Q>2 and
  bypassed the choke point - convert was decided, native ran, the
  shadow sat unused. Fix (selector v2): moved the decision block
  to fattn-common.cuh as inline (one symbol across TUs), both
  launch branches now call ggml_cuda_fattn_use_native_tile(id,dst)
  first. fa1s-conv (9.2) is therefore an INVALID row (old binary,
  decided-convert-ran-native); rerun with the fixed binary when
  convenient, expect ~12.6. ROUND 2 (fixed binary, linked before
  the auto lane's server start): fa1s-auto = 12.6 t/s, acc .672,
  fill 225.7 (= off-build parity), AUTO decision lines present
  with the KV-growth re-resolve firing correctly (0.00 GiB at
  first op -> 0.30 GiB at reserve -> convert). Net verified
  matrix on ONE binary: AUTO/convert 12.6 vs native 9.2 = the
  E61 attribution is CONFIRMED clean (native path itself costs
  3.4 t/s at Q=5; NOT the config table - the AMD table dispatch
  at fattn-tile.cuh:315/:328 is UNGATED, shared by ON and OFF
  builds). Leading kernel-work hypotheses (from code, untested):
  (a) native launch branches hardcode cols_per_block = DKQ<=128
  ? 64 : 32 -> 32-row Q tiles at Q=5 = ~6.4x row padding, while
  the convert tail uses cols 16 (8-row tiles); (b) 3x V-dequant
  redundancy across head-group blocks (E58). Next when resumed:
  rerun fa1s-conv (fixed binary), small-Q native geometry
  experiment (cols 16 for Q<=16 in the native branches), 68B
  pair loader, Q x KV matrix.
- E64 selector lanes complete, all on the fixed binary (2026-08-31,
  user-run, c=155000 fill 120k unless noted): fa1s-conv2
  (GGML_CUDA_FATTN_PATH=force_convert) = 12.5 t/s, acc .672, fill
  228.6, repro_ok, sha e54019ff6b42 - the invalid 9.2 fa1s-conv row
  is fully explained by the v1 launch bypass (E63); forced convert
  is now stable across three rows (12.6 off-build, 12.6 auto, 12.5
  forced). fa1s-auto250 (AUTO, C=250000, n_ctx 250112) = 12.6 t/s,
  acc .672, fill 227.9, repro_ok - AUTO stayed on convert through a
  250k ctx (shadow ~0.5 GiB vs ~15 GiB free at 155k, still far under
  the 80% line; no flip needed). Selector end-to-end validation is
  CLOSED for the 155k/250k range; the flip direction only engages
  above ~600k+ ctx on this rig (shadow > 12 GiB free). Committed as
  5ecb389f5 (selector + journal + lane rows), push still gated.
- E65 small-Q native geometry experiment launched (2026-08-31):
  hypothesis (a) from E63 - the native tile launch branches run
  32-row Q tiles (cols 64) at dk 128, so a Q=5 verify batch pads
  ~6.4x; the convert tail already uses cols 16 (8-row tiles) for
  small Q. Patch (fattn-tile.cuh only, VEGA tune block): new
  launch_fattn_tile_vega_native<DKQ,DV,ncols2,softcap,cols> helper
  templated on Q-tile width; dispatch now tries cols 16 first when
  ncols2 <= 4 and Q rows <= 16, then falls through to the original
  cols 64/32 geometry; a missing AMD config row or non-native KV
  types return false so the convert tail still catches every case -
  the selection/shadow-skip in fattn.cu and all launches see the
  same ggml_cuda_fattn_use_native_tile decision, no unallocated
  shadow path. Prediction if padding is the cost: FORCE_NATIVE tg
  climbs above 9.2 (native gains back most of the padded-row waste)
  while the convert path is untouched (12.5/12.6). Lane:
  fa1s-geom16 = FORCE_NATIVE + new build, same 120k protocol.
  Result pending.
- E66 ISA audit of the live native kernel (2026-08-31, offline, no GPU):
  disassembled flash_attn_tile<128,128,32,2,false,false,true> (the
  variant the q8-V lane actually runs; K f16 + V q8_0, ncols1=32)
  from build-dflash-novega/bin/libggml-hip.so .hip_fatbin -> amd elf
  (llvm-objdump -d). 5937 body instructions, ~128 VGPRs / ~46 SGPRs.
  Mix: 1024 v_dot2_f32_f16 + 1024 v_pk_fma_f16 (QK matmul, the only
  "dot" math on Vega20 - no mfma), 2652 vector ALU total, 832
  scalar, 786 global-mem (=407 buffer_load_dword + 338
  buffer_store_dword - dword-granularity q8/f16 staging, no wide
  loads), 365 LDS (224 ds_read_b128 + 80 ds_bpermute + ...), 248
  cvt (incl. 128 v_cvt_f32_i32_sdwa = the q8 dequant scale chain).
  REINTERPRETATION: at Q=5 with 32-row Q tiles, the per-KV-chunk
  dot/FMA work runs on 32 rows of which 27 are padding in EVERY
  ~128-KV-row chunk for the whole 120k - ~84% of the QK vector
  math is garbage at our workload, independent of KV bytes (KV
  loads/dequant are per-chunk and unchanged by Q padding). Convert
  wins (12.6 vs 9.2) because its small-Q tail uses 8-row tiles
  (cols 16) AND skips the in-kernel dequant (248 cvts + dword
  staging). So the cols-16 geometry experiment targets the single
  largest avoidable cost in the native path; expect a real but
  partial recovery (dequant cost remains). If the geometry lane
  only recovers a sliver, the 68B pair loader (byte-stream load of
  two adjacent q8_0 blocks per 32-bit word, cutting the dword
  staging and V load count ~3x across ncols2=2 head groups) is the
  next kernel experiment. Build with the geometry patch: BUILD_EXIT
  = 0 (first attempt failed on a real bug: flattening the
  if constexpr (q8_ok) guard into a runtime if made the compiler
  instantiate the q8 kernel body for configs the asserts forbid,
  e.g. dkq96 nbatch_K=48; restored the constexpr guards).
- E67 geometry lane result - WIN (2026-08-31, user-run, c=155000
  fill 120k, fa1s-geom16 = FORCE_NATIVE on the cols-16 binary):
  **12.3 t/s**, acc .68, fill 201.3, repro_ok, sha e54019ff6b42.
  vs fa1s-nat (same FORCE_NATIVE, cols-64 binary) = 9.2 and vs
  fa1s-conv2 = 12.5: the small-Q cols-16 routing recovers +3.1 t/s
  (+34%) on the native path, landing within ~1.6% of convert.
  Padding (E66: ~84% of QK dot/FMA on 27 dummy rows per 32-row
  pass, every ~128-KV-chunk) was the dominant native cost, as the
  kernel audit predicted. Fill is unchanged (201.3 vs fa1s-nat
  201.9) - the Q<=16 routing only touches TG ops (Q=5) by design;
  prompt fills still run the cols-64 geometry. Line-level acc .68
  vs .672/.641 is a small improvement (server-side joint
  distribution remains the authoritative mix metric). Consequences:
  the AUTO selector's convert->native flip is no longer a ~30%
  penalty - native now costs ~1.6% t/s at 120k while keeping the
  compact 238 MiB target buffer (vs 588) and q8-native KV traffic;
  the compact-buffer endgame is nearly free. Remaining native
  residuals: the ~0.2 t/s gap to convert (dequant staging: 248
  cvts + dword loads, per-chunk) and the 3x V-dequant redundancy
  across the ncols2=2 head-group blocks - both the 68B pair
  loader territory. Optional confirming lane: fa1s-geom250
  (FORCE_NATIVE, C=250000) - b1-native was flat vs KV-length
  (9.2 -> 9.3 @200k) so ~12.3 is expected; verifies compact
  reserve sizing at 250k.
- E68 68B pair q8_0 loader - LAUNCHED (2026-08-31, uncommitted):
  fattn-tile.cuh half2 dequant helper (VEGA-TUNE-gated, used by the
  native K and V tile staging) rewritten to process two adjacent
  q8_0 blocks (68 B) as 17 dwords when nb is even: w0.lo = first
  scale, w0.hi/w1..w7/w8.lo = first qs, w8.hi = second scale,
  w9..w16 = second qs (clean). Alignment: pair base = buffer base
  (256B-aligned) + 34*(i*stride + 2k) bytes; for nb even the row
  byte-stride is a multiple of 68, so pairs stay 4B-aligned
  unconditionally - no runtime check. nb odd (J=32,96,... e.g. the
  cols-64 native nbatch_K=32 K-loads) keeps the old per-block
  ushort path unchanged; the float-tile overload (non-VEGA path)
  is untouched. Decode math is byte-identical to the old path, so
  repro sha must stay e54019ff6b42. Expected benefit: K/V staging
  drops ~17+ load instructions per 2 blocks to 9 (2x dwords
  mergeable) with 2 scale folds - targets the 407 dwords / 248
  cvts of the E66 ISA audit on the V path (J=DV=128, nb=4).
  Validation lane after build: fa1s-pair1 (FORCE_NATIVE).
- E68 addendum - J=0 zero-div fix (2026-08-31): first pair build
  (fa1pair-build.log) warned "division by zero" from the pair path
  (p/np, p%np) in the dkq256/dkq192 instantiations with J=0
  (flash_attn_tile_load_tile_q8_0<32,8,32,0,4,...> - 0-column K/V
  staging rows, nb=0). Fixed by gating the pair path on
  (nb >= 2 && nb % 2 == 0) so J=0 falls to the original single-block
  loop. Note: the remaining 40 zero-div warnings are pre-existing
  upstream (b/nb, b%nb at :571/:572, dead when tot=0) - the
  geometry-only build (fa1geom-build2.log) warns the identical 40;
  build2 (fa1pair-build2.log) BUILD_EXIT=0 with the same footprint.
  No new warnings net. Lane fa1s-pair1 ready (NEXT-LANES-2026-08-31.md).
- E69 pair-loader lane result - NEUTRAL-NEGATIVE, REVERTED
  (2026-08-31, user-run, fa1s-pair1, c=155000 fill 120k): tg 12.2,
  acc .68, fill 195.9, repro_ok, sha e54019ff6b42. vs fa1s-geom16
  (same binary minus the pair loader): tg 12.3, fill 201.3. tg
  delta is within lane noise (convert-family spread ~0.1), but the
  fill delta is a robust same-kernel A/B: fill ops run the cols-64
  geometry (Q > 16) where the pair loader only touched V-staging
  (DV=128, nb=4; K J=32 stays on the old path) and it is -5.4 t/s
  (-2.7%) over the 120k fill. Verdict: the 17-dword pair staging
  is not a win once padding is fixed (E67) - the V pass is not
  load-issue bound, and the explicit extract chain costs as much as
  it saves. fattn-tile.cuh reverted to the committed geometry
  state (832deab2c), rebuilt, tree and binary consistent. This
  closes the dequant-staging line of work; kernel roadmap moves to
  the Q x KV matrix (regression sweep of the small-Q routing).
- E70 production slow-TG root cause - LD_LIBRARY_PATH shadowing
  (2026-08-31): 2llama-start-iq6v-dflash2.sh pointed BIN at
  build-dflash-novega but LD_LIB (default) at build-dflash/bin,
  and llama-server uses RUNPATH (not RPATH), so LD_LIBRARY_PATH
  wins resolution. ld.so --list proof: with the old env the whole
  stack (libllama, libggml, -base, -cpu, -hip, -vulkan) resolved
  from build-dflash/bin = the stale Aug-28 pre-selector,
  pre-geometry build - i.e. production was running the old binary
  end-to-end despite the new server path (the server binary is an
  18K thin driver now, everything real lives in the .so set).
  That is the ~9 t/s signature (no selector, cols-64 native).
  Fix: LD_LIB default -> build-dflash-novega/bin; ld.so --list
  now resolves the full novega set. Expect production TG
  ~12.3-12.6-class after restart (two honest deltas vs the greedy
  lanes: temp-1.0 sampling lowers draft acceptance somewhat, and
  mmproj VRAM may flip AUTO to native - now 12.3-class post-E67).
  Ops lesson: for any launcher that sets LD_LIBRARY_PATH, verify
  with ld.so --list (RUNPATH does NOT protect $ORIGIN-first
  resolution).
- E71 build-config audit + reproducible build script (2026-08-31):
  created build-dflash-novega.sh (repo root) - exact flag set recovered
  from the validated build's CMakeCache + ggml/CMakeLists defaults;
  configure smoke test in a scratch dir reproduced the cache with ZERO
  diffs on every meaningful key. Baseline pins: HIP=ON gfx906, VULKAN=ON,
  VEGA_TUNE+FATTN=ON, MMQ/TOPK/GRAPHS=OFF, HIP_NO_VMM=ON (default),
  MMQ_MFMA=ON (default), Release -O3, empty CMAKE_HIP_FLAGS.
  Audit findings (unmeasured levers, all opt-in via script env):
  (1) -ffast-math -fno-math-errno was on CMAKE_HIP_FLAGS through
      build-vega20/build-sync25, dropped since build-dflash (DFlash2
      era) with no recorded verdict; numerics change, canonical sha
      would move; HIP_FAST_MATH=1 re-enables.
  (2) VEGA_TUNE_MMQ/MMQ tile table (mmq-config-vega.cuh) in tree,
      off since E50-V2 b1 isolation build, never A/B'd on the dflash2
      stack; bench/FINDINGS notes LDS/occupancy rationale and a
      41-59% issue-rate gap from bench-dot4, PMU split blocked (E49).
  (3) VEGA_TUNE_TOPK tiled top-k (wide rows) - production samples
      temp 1.0 top-k 20, unmeasured. (4) VEGA_TUNE_GRAPHS - off,
      unmeasured (runtime HIP_GRAPH=1 already on).
  Context: rocBLAS vs MMQ vs hipBLASlt measured neutral (perf-matrix
  2026-08-10), so MMQ-tune expectations modest; the dominant remaining
  lever is the E70 launcher fix (restart pending user confirmation).
- E72 PP16384 production dip + cmp-temp0 prep (2026-08-31):
  user reports first-batch PP 395->350 (client-measured) after the E70
  restart. Controlled jsonl table says: PP16384 is 316-328 on EVERY
  fa1s path (conv2 328.4, nat 318.7, geom 321.3, auto250@250k 328.2) -
  the selector costs at most ~3% on PP16384; the big convert win is
  the 120k deep fill (228.6 vs 201.3, +13%), not the first batch.
  Production 350 sits ABOVE every controlled lane number, so vs lanes
  nothing regressed; the 395 baseline was client-side under unknown
  conditions (prompt content, cache state, template overhead) and the
  Aug-28 header number for the same protocol family was 363 (-3.6% vs
  now, selector-scale). Decisive check: grep "AUTO:" in the server
  console (lane evidence: at c=250k with draft loaded, shadow 0.48 GiB
  free 15.5 -> f16 convert); if production flipped native from mmproj
  VRAM pressure, GGML_CUDA_FATTN_PATH=force_convert in the launcher
  reclaims fill+PP. TG on the restarted server still unreported -
  expect ~12.3-12.6 (was 9.0); if it reads ~9 the restart did not
  take and that is a different bug.
  Mainline refresh done: ~/dev/llama.cpp on branch mainline-cmp at
  upstream/master 010be9683 (was fork mirror at 50f068fff/#27881 sync
  point; #27881 itself irrelevant to us - #27812 vulkan view-alias and
  #27816 M-RoPE/DFlash2 are the ones that matter). build-stock rebuild
  launched (HIP+Vulkan+gfx906, Release, log /tmp/opencode/mainline-build.log).
  Wrote bench/cmp-mainline-temp0.sh: base-model-only config (no spec,
  no vision, no fork flags) both sides, greedy PP16384 -> 120k fill ->
  TG256 + temp-0 sha per side + cross-build sha-match verdict;
  syntax-checked, awaiting mainline build + user GPU run.
- E73 pp350 probe postmortem + probe-fattn.sh (2026-08-31):
  the "failed" probe actually loaded fully (model loaded + listening;
  DONE1 status = cosmetic bug in lane-dflash.sh PROBE status write,
  fix deferred - lane script was mid-execution). Real limitation: the
  probe kills before first inference, so only the early shadow-0.00
  round logs; the decisive shadow-fit AUTO decision fires at graph
  build. Probe did capture production-scale KV at c=250000:
  4488+5236+2244 = 11.97 GiB across devices (f16K/q8_0V), free 14.96
  GiB on dev0 at start - tight fit, consistent with the checkpoints
  theory for the native flip. Wrote bench/probe-fattn.sh: standalone,
  1-token warmup forces the real decision, runs TWO variants
  (lane-nockpt vs prod-ckpt30) to isolate --ctx-checkpoints 30 as the
  trigger. Mainline build-stock rebuilt at upstream master 010be9683,
  BUILD_EXIT=0, artifacts verified (server ELF valid, hip 119 MB);
  cmp-mainline-temp0.sh ready. fa1s-auto250b lane RUNNING (mid-fill,
  healthy). Run order after the lane: probe-fattn.sh then cmp.
- E74 auto250b PASS + cmp-temp0 result + probe verbosity fix
  (2026-08-31): fa1s-auto250b DONE - pp 325.6, fill 227.7, tg 12.50,
  acc 0.67, repro True, sha e54019ff6b42. AUTO->convert re-verified at
  C=250000 on the final binary; selector release state closed GREEN.
  cmp-mainline-temp0 (base model, no spec/vision/fork flags, c=155k,
  ts 35,20,45, f16K/q8_0V): fork pp 353.7 fill 241.5 tg 7.30 vs
  mainline@010be9683 pp 356.1 fill 244.3 tg 7.40 - parity within
  noise (-0.7/-1.2%), and TEMP-0 SHA IDENTICAL ACROSS BUILDS
  (aa02f59e3197 both, repro True both): the fork's selector+geometry
  work is numerics-preserving vs upstream master greedy output. Side
  datum: base-model TG is 7.3-7.4 both sides -> the dflash drafter
  is worth ~1.7x (12.5 with spec). Note: cmp sha differs from the
  lane sha by design (no spec/template, raw prompt protocol).
  probe-fattn.sh first run: variant A (lane-nockpt) executed but
  logged 0 AUTO lines - script was missing -lv 4 (default verbosity 3
  suppresses ggml_cuda_fattn INFO; lanes show them only due to -lv 4);
  variant B never started (user interrupted). Fixed: -lv 4 added;
  re-run pending for the PP350 checkpoints-flip verdict.
- E75 PP350 root cause CLOSED - AUTO flip math + session aging
  (2026-08-31): read the selector source (fattn-common.cuh).
  Decision: native <=> shadow > free - free/5 (20% margin); with the
  production shadow 0.48 GiB the flip needs free < 0.6 GiB on the
  fattn device. Cache: re-check only when KV grows past the last
  checked length AND cached decision was convert; native never
  re-flips. Mechanism: fresh server free 15.5 GiB -> convert
  (PP 363-class client scale); checkpoints are created LAZILY during
  generation (probe exposed fork default 32 even without the flag;
  production asks 30) and deep-chat KV growth shrink free; a
  KV-growth re-check below 0.6 GiB flips native mid-session and it
  stays native -> PP 350-class for the server lifetime. Old launcher
  "faster" because old build has no selector (always convert).
  Probe lesson: round-2 AUTO lines fire at graph_reserve worst-case
  (ubatch 384); warmup prefills do execute the path but the probe
  still only logged round-1 (shadow-0.00 poison-cache entry made at
  model-load with ne[1]=0; re-check fires on first real op) - round-2
  in lanes comes from the reserve-time call, probe timing missed it;
  not worth more archaeology: the formula + probe free values close
  the case. REMEDY APPLIED: GGML_CUDA_FATTN_PATH=force_convert added
  to 2llama-start-iq6v-dflash2.sh (old build ran convert under
  identical pressure for weeks = empirical safety; tradeoff: a long
  session that would have survived on native could OOM instead -
  if that ever happens, drop checkpoints or ctx, speed stays).
- E76 PP "350 cap" matrix + 393-outlier question (2026-08-31):
  E75 force_convert did NOT lift PP (350 stands) - expected in
  hindsight: fresh AUTO already picks convert; the 363-convert client
  mapping was numerology, retracted. User matrix (client PP16384):
  llama-start (unmodified since 08-28: build-dflash, Q8_0 drafter,
  250k, ts 35,20,45) = 393; every 2llama variant (novega AND
  build-dflash, Q4_K_M AND Q8_0, 240k AND 250k) = 350. Full untruncated
  diff: launchers now differ only by ctx, BIN, force_convert - all
  tested variables. KEY FACT: 393 exceeds every controlled record
  ever taken on this rig (lanes 316-328 SSE, cmp fork 353.7 client,
  Aug-28 header 363 client). Either a real unprecedented fast path in
  the llama-start cell, or a client-measurement artifact (first-batch
  accounting under chat state). Decisive: two controlled lanes
  (lane-client synthetic 16392+4 SSE first-batch) on the exact
  llama-start config vs the same on novega. Next: pp393a/pp393b.
- E77 PP regression root cause candidate - gated-off vega MMQ tune
  (2026-08-31): user confirms 393-408 client consistently on llama-start
  (build-dflash @ 5d317d32c) vs 350 cap on every novega variant. Scale
  fix: Aug-28 header 363 was LANE/SSE scale, client ~= 1.10x -> 363 SSE
  ~= 399 client. Two independent deltas agree (client -11%, lane -9.7%):
  real regression. Bisect window 5d317d32c..HEAD = 12 commits, only
  code suspects: 68f24f402 (VEGA_TUNE gate), 5ecb389f5 (selector),
  832deab2c (geometry, PP-neutral per nat/geom lanes). Static proof:
  at 5d317d32c mmq.cuh:222/236/266 includes+calls mmq-config-vega
  UNGATED; top-k.cu:58-133 tiled topk UNGATED -> old build ran vega
  MMQ+topk ACTIVE. Gate commit wrapped them; E50-V2 b1 built with
  MMQ/TOPK/GRAPHS=OFF for isolation -> every novega build since ships
  them compiled out. PP16384 is MMQ-GEMM-bound (perf-matrix 08-10).
  Hypothesis: the ~10% PP regression IS the gated-off vega MMQ tune.
  nm-on-.so inconclusive (static inline); "VEGA_TUNE" string absent at
  pre-gate source by construction. Gate commit also ADDED +91 lines to
  fattn-tile.cuh (the FATTN tune block itself).
  ACTION: build-ab2 launched (VEGA_TUNE_MMQ=1 TOPK=1 GRAPHS=1 on
  current source, TARGETS=llama-server, /tmp/opencode/b2-build.log).
  Decisive lanes: pp393a (5d317d32c binary, all tunes) / pp393b
  (novega, none) / pp393c (ab2, all tunes restored). c~=a -> restore
  and ship b2 config; c~=b -> selector-overhead suspect, bisect
  5ecb389f5. Caveat: MMQ tile config may change reduction order ->
  greedy sha may move off e54019ff6b42 (not a correctness issue;
  repro_ok before/after must still pass).
- E78 b2 build failures root-caused - /opt/rocm is ROCm 7.15.0
  (2026-08-31): b2 restoration build (build-ab2, VEGA_TUNE
  MMQ/TOPK/GRAPHS=1) failed twice from scratch with "builtin
  functions must be directly called" in ROCm HIP headers on the
  first TU. Build #1: autodetected CMAKE_HIP_COMPILER=llvm/bin/
  clang++ + -isystem /opt/rocm/include. Build #2: compiler pin
  alone insufficient - clang fixed but -isystem injection stayed.
  ROOT CAUSE: /opt/rocm is NOT a symlink but a REAL DIRECTORY =
  ROCm 7.15.0; without package pins find_package(HIP) resolved
  hip_HIPCC_EXECUTABLE=/opt/rocm/bin/hipconfig and injected
  /opt/rocm/include (7.15 headers) into 6.1.0-clang17 HIP TUs ->
  header/compiler mismatch. FIX = full D11 recipe (HANDOFF-E50-V2)
  recovered from build-dflash cache and encoded into
  build-dflash-novega.sh: CMAKE_HIP_COMPILER pin + SIX package
  pins (hip, hipblas, rocblas, hsa-runtime64, amd_comgr,
  AMDDeviceLibs -> /opt/rocm-6.1.0/lib/cmake/*) + GGML_CUDA_FA_
  ALL_QUANTS=ON (both old caches ON; fresh configure defaulted it
  OFF - separate latent flag loss, now pinned). Cache-mining
  lesson: -D vars without type land as :UNINITIALIZED - include
  it when diffing caches or pins get missed. Build #3: 0 errors at
  32% (first HIP TUs passed) - fix proven. Open question, low
  priority: why the mainline build-stock configure survived
  without pins (different HIP resolution upstream). Next: verify
  artifacts (D11 acceptance: ldd libhipblas.so.2 not .so.3), then
  pp393c lane on build-ab2.
- E79 build-ab2 SUCCESS (2026-08-31): D11-recipe build #3 completed;
  verify section ran (script set -e => build exited 0): llama-server
  valid ELF, libhipblas.so.2 -> /opt/rocm-6.1.0 (D11 acceptance),
  cache + TU defines confirm VEGA_TUNE FATTN/MMQ/TOPK/GRAPHS all =1,
  libggml-hip.so 122.6 MB vs novega 116.9 (restored tune code
  visible in size). CMAKE_HIP_FLAGS empty (no fast-math, baseline).
  Decisive lane trio now: pp393a (5d317d32c binary, all tunes) /
  pp393b (novega, none) / pp393c (ab2, tunes restored on HEAD).
  Predictions: a ~360 SSE, b ~328; if MMQ-tune hypothesis right,
  c ~= a and the PP regression is closed as E50-V2 isolation
  collateral. If c ~= b -> hypothesis dead, next suspect = selector
  commit 5ecb389f5. TG side: a ~9.x, b 12.5, c expected 12.3-12.6
  (topk may add a little at temp-1.0). sha may move off
  e54019ff6b42 if vega MMQ tiles change reduction order; repro_ok
  must still pass either way.
- E80 pp393b/c crash isolated + repro under agent control (2026-08-31):
  user granted server run/kill rights. bench/repro-crash.sh (lane env,
  one 12-token greedy completion). Matrix on novega: A) Q8_0 drafter
  + C=250000 -> CRASH (invalid device function, launch_fattn
  occupancy check fattn-common.cuh:1206, right after round-1 AUTO
  "-> f16 convert"); B) Q4_K_M + 250000 -> CLEAN; C) Q8_0 + 240000
  -> CLEAN (user production config, matches their 350 runs).
  Crash window = exactly (Q8_0 drafter AND 250k) - the one config
  only the old build ever ran (pp393a/llama-start run it fine) ->
  in-window regression (5d317d32c..HEAD), placement-specific,
  path-branch not yet dissected (force_native run aborted by user:
  pragmatic route = drop to 240k for Q8_0 drafter). pp393a lane
  postmortem: old build survived task 1, ran fill 40003 @ 314 t/s,
  then lane-client hung post-fill (no error, no json) - client-side
  hang, separate issue, not chased. NEXT: lanes a2/b2/c2 at C=240000
  decide the MMQ-tune hypothesis.
- E81 pp393a2 postmortem + rig map + relaunch design (2026-08-31):
  a2 "old build assert" RETRACTED as a build bug - startup device map
  showed GPUs already ~full (ROCm0 418 MiB free, ROCm1 910, Vulkan1
  850) from the user production server still holding VRAM; plain
  cudaMalloc OOM (884 MiB ROCm0 alloc_tensor_range), assert(buffer)
  downstream. Lane design flaws: (1) lane-client urlopen timeout
  7200s -> hangs ~forever on server death (wedged pp393a + a2
  chains); (2) chained lanes share port 8014 with only sleep 1
  after pkill - VRAM teardown lag. Full device map documented:
  ROCm0/1 = Radeon VII pair (HIP), Vulkan2/3 = same VIIs via RADV,
  Vulkan0 = RENOIR iGPU, Vulkan1 = RTX 3080 Laptop 8 GB, CPU 5900HX.
  Q8_0-drafter toxicity: new builds crash (invalid device function,
  fattn-common.cuh:1206) at 250k / clean at 240k; old build ran
  250k+Q8_0 fine; PP attribution needs no drafter (idle in
  prefill). RELAUNCH pp394a/b/c: Q4_K_M, C=250000 (fa1s-proven),
  FILL1=40000 TG_N=256, per-lane timeout 900. Predictions: b ~328;
  a = old baseline; c~=a closes MMQ hypothesis, c~=b -> selector.
- E82 MMQ-TUNE HYPOTHESIS CONFIRMED - pp394 matrix (2026-08-31):
  Q4_K_M drafter, C=250000, same lane metric, only BIN differs:
    pp394a build-dflash  (5d317d32c, tunes in):  pp 362.2 fill 311.4 tg 12.10
    pp394b novega        (HEAD, tunes out):      pp 324.6 fill 290.8 tg 13.70
    pp394c build-ab2     (HEAD, tunes restored): pp 371.1 fill 328.8 tg 13.40
  VERDICT: c beats a on PP (+2.5%) and fill (+5.6%) while keeping the
  novega-class TG (13.4 vs old 12.1) - the vega tunes and the fa1s
  selector/geometry stack POSITIVELY. The production PP regression
  (393-408 client vs 350) = E50-V2 b1 isolation compiling out the
  ungated vega MMQ/TOPK/GRAPHS tunes (gate 68f24f402). Bonus: sha
  e54019ff6b42 CANONICAL on all three lanes - vega MMQ tiles do not
  change greedy numerics; repro gates pass; no numerics caveat for
  adoption. Measurement-model correction also closed tonight: pure
  eval print (user 395) vs client window incl ~3.8s fixed overhead
  (lane 362) vs whole-40k average (311); the old x1.10 client-scaling
  guess retired. ACTION: adopt tunes as default - rebuilding
  build-dflash-novega in place with VEGA_TUNE_MMQ/TOPK/GRAPHS=1
  (script defaults to flip after confirmation lane pp394d).
- E83 ADOPTION COMPLETE - release build restored (2026-08-31):
  confirmation lane pp394d on the rebuilt release dir (tunes ON,
  default BIN_DIR): pp 369.0, fill 327.3, tg 13.30, repro True,
  sha e54019ff6b42 - matches pp394c/ab2 within noise (+-0.6%).
  build-dflash-novega hip.so byte-count identical to ab2
  (122,600,400); correct 6.1 linkage; script defaults flipped to
  the validated config (VEGA_TUNE MMQ/TOPK/GRAPHS=1 default, E82
  header note); HIP_FAST_MATH the sole remaining unmeasured opt-in.
  Launchers need zero changes. PRODUCTION GUIDANCE: Q8_0 drafter
  stays at 240k (the Q8_0+250k invalid-device-function crash on
  new builds is NOT fixed by the tunes - lazy-load experiment
  still pending, user call); Q4_K_M drafter is clean at 250k.
  Expected client-scale after restart: PP ~395+ (was 350), TG
  unchanged. Uncommitted work awaiting push go: geometry commit
  already in (832deab2c); E69-E83 journal/docs, build script,
  lane/probe/cmp/repro tooling, launcher fixes.
- E84 PP REGRESSION - the reason the ~10% PP loss happened and was fixed:
  production PP16384 fell from the 393/395 class (launchers on the
  Aug-28 build) to the 350 class after moving to novega builds.
  Two independent causes, found in sequence:
  1. LD_LIBRARY_PATH shadowing (E70, launcher fix): launchers set
     LD_LIB to build-dflash/bin (stale build); llama-server links via
     RUNPATH so the stale .so set silently shadowed the new binary
     end-to-end. Fixed to build-dflash-novega/bin. Alone this did not
     restore 393.
  2. Tuned-out kernels (the real ~10% loss). The Aug-28 build
     (source 5d317d32c) compiled the gfx906 vega-tuned MMQ tiles and
     tiled top-k UNGATED - active by default. The VEGA_TUNE gate
     commit 68f24f402 wrapped them behind GGML_CUDA_VEGA_TUNE_* flags,
     and the E50-V2 isolation build configured MMQ/TOPK/GRAPHS OFF to
     isolate a selector crash. That off-state was never A/B'd back:
     every novega-derived build since lost exactly the GEMM path that
     PP16384 is compute-bound on (perf-matrix 08-10). Verdict matrix
     pp394 (same model/split/ctx/drafter, only the binary differs;
     all repro True, canonical sha e54019ff6b42):
       old build        pp 362.2  fill 311.4  tg 12.10
       novega untuned   pp 324.6  fill 290.8  tg 13.70
       tunes restored   pp 371.1  fill 328.8  tg 13.40
     Tunes recover ~11% PP/fill AND stack with the selector + small-Q
     geometry work (TG stays at the new-build level, sha unchanged -
     the tuned tiles do not move greedy numerics).
  Adoption closed E83: release dir rebuilt tunes-ON, confirmed pp
  369.0 / fill 327.3 / tg 13.30 (lane scale); build-dflash-novega.sh
  now defaults MMQ/TOPK/GRAPHS=ON.
  Client-scale note: 393-class figures are the server pure-eval print;
  the lane's client window carries ~3.8s fixed per-batch overhead
  (16384/362 - 16384/395), so the same binary reads ~395+ on the
  server print after the production restart.
  pp16384/pp120000/tg256 figures for the current upstream master are
  taken from bench/cmp-mainline-temp0.sh (both sides, same config):
  PENDING user run; appended here with the numbers when it lands.
